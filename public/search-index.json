[{"id":0,"title":"Quick start","section":"Overview","href":"/docs","headings":"Guide · GUI backends · Reference · Examples · Formats · Install destinations · See also","excerpt":"Open your DAW, scan for plugins, load . For a chapter-by-chapter walkthrough, follow the Guide from install through hot reload. For look-up material — every CLI flag, every key, every field — jump to ","text":"Open your DAW, scan for plugins, load . For a chapter-by-chapter walkthrough, follow the Guide from install through hot reload. For look-up material — every CLI flag, every key, every field — jump to the Reference. For working code to read top-to-bottom, browse the Examples. Build profile. Every command ( , , , , ) defaults to the cargo release profile — plugins are typically loaded into a DAW where debug-build DSP can spike CPU under load. Pass to opt into the cargo dev profile for fast-compile iteration. Guide A walkthrough of building, iterating on, and shipping a truce plugin. Read top-to-bottom on first pass; return to specific chapters as needed. # Chapter What you get --- --------- -------------- 1 install Rust + the platform compiler + + . CLAP+VST3 minimum-friction setup; AU/AAX/packaging deferred. 2 first-plugin , a tour of the generated files, , load in a DAW. 3 plugin-anatomy trait, bus layouts, state persistence. 4 parameters , smoothing patterns, meters. Attribute reference in reference/params. 5 processing patterns for effects, MIDI, sample-accurate events, synths. Includes the per-block ops + math helpers for hot paths. 6 fundsp Drop a graph into — combinator DSL, cells for sample-accurate automation, RT60 rebuild patterns. 7 workers Off-thread work: managed background tasks on a shared pool, + a dedicated for streaming analysis, and how results flow back through fields. 8 midi Reading and emitting MIDI events; per-format support; testing MIDI plugins. 9 gui Built-in GUI widgets + the alternative backends (egui, iced, Slint, Vizia, raw window handle). 10 audio-testing for in-process audio + MIDI regression tests — no DAW required. 11 presets Factory presets in every format from TOML; in-DAW authoring via ; cross-format conversion; preset packs. 12 state Params vs state, / for extra state beyond params, what hosts persist, migrating pre-truce state with . 13 shipping , signing, installers. 14 ios iOS-specific workflow: simulator iteration, device + signing, per-plugin knobs, preview-audio mute, screenshot regression. 15 hot-reload 2 second edit → hear loop with . Experimental — dev-loop only. GUI backends Pick a toolkit. The built-in widget set covers most plugin UIs; the alternatives are there when you need something the built-in widgets don't ship. Built-in widgets — builder, every widget constructor, theming egui · iced · Slint · Vizia · Raw window handle Screenshot testing — visual regression tests for the GUI Reference Not exhaustive — for the full Rust API surface, see the rustdoc. Page What's in it ------ -------------- cli Every subcommand and flag — , , , , , , , , . params and — every attribute key, range syntax, smoothing modes, meters, custom formatting. truce-toml Project-level schema: , , , packaging. Signing identities and other secrets live in env — see . cargo-config Per-developer table — every environment variable truce reads (signing identities, SDK paths, validator paths, hot-reload). Examples Short, one-file plugin examples that double as canonical references for parameter shapes, processing patterns, GUI layouts, and MIDI. Full list with screenshots: examples. Plugin Type GUI What it shows -------- ------ ----- --------------- gain Effect Built-in Minimal plugin: one , a single-channel multiply. eq Effect Built-in Multi-band biquad EQ with smoothed coefficients. synth Instrument Built-in MIDI-driven polyphonic synth with per-voice envelopes. transpose MIDI Built-in MIDI effect — rewrite note numbers in . arpeggio MIDI Built-in Sample-accurate MIDI scheduling against the host transport. tremolo Effect egui Tempo-synced LFO + the egui backend wired in. state Effect egui Custom for persistent extra state. fundsp-reverb-simple Effect Built-in Stereo plate reverb on a fundsp graph (pedagogical, inline rebuild). fundsp-reverb-worker Effect Built-in Same reverb with a background-thread graph rebuild + lock-free swap — stays alloc-free. gain-egui Effect egui Same plugin as , rendered with egui. gai"},{"id":1,"title":"Changelog","section":"Overview","href":"/docs/changelog","headings":"6.1.2 · 6.1.1 · 6.1.0 · Fixes · 6.0.0 · Breaking · Migrating from 5.x · 5.0.1 · Bus layouts · Parameters · Background tasks · Breaking · Migrating from 4.1 · 4.1.0 · Breaking · Migrating from 4.0 · 4.0.0 · Breaking · Migrating from 3.x · 3.1.4 · 3.1.3 · 3.1.2 · 3.1.0 · 3.0.0 · Breaking · Migrating from 2.x · Added · 2.0.2 · 2.0.1 · 2.0.0 · Breaking · Migrating from 1.x · MIDI 2.0 / UMP · Multiple MIDI ports · Legacy state migration · Native f64 processing · Internals · Fixes · 1.0.5 · 1.0.4 · 1.0.3 · 1.0.2 · 1.0.1 · 1.0.0 · Breaking · Other changes · 0.64.0 · 0.63.3 · 0.63.1 · 0.63.0 · 0.62.0 · 0.61.0 · 0.60.1 · 0.60.0 · 0.59.0 · 0.58.4 · 0.58.3 · 0.58.2 · 0.58.1 · 0.58.0 · 0.57.2 · 0.57.1 · 0.57.0 · 0.56.0 · 0.55.0 · 0.54.0 · 0.53.0 · 0.52.0 · 0.49.23 · 0.49.22 · 0.49.21 · 0.49.20 · 0.49.19 · 0.49.18 · 0.49.17 · 0.49.16 · 0.49.15 · 0.49.14 · 0.49.13 · 0.49.12 · 0.49.11 · 0.49.10 · 0.49.9 · 0.49.6 · 0.49.4 · 0.49.2 · 0.49.0 · Migrating layout() · Migrating customeditor() (egui / iced / slint / hand-rolled) · 0.48.11 · 0.48.10 · 0.48.9 · 0.48.8 · Migrating an existing plugin to the registry pin · 0.48.4 · 0.48.3 · 0.48.2 · 0.47.0 · 0.46.0 · 0.45.4 · 0.45.3 · 0.45.2 · 0.45.0 · 0.44.0 · 0.43.0 · 0.42.1 · 0.42.0 · 0.41.0 · 0.40.2 · 0.40.1 · 0.40.0 · 0.39.3 · 0.39.2 · 0.39.1 · 0.39.0 · 0.38.0","excerpt":"Notable changes per release. 6.1.2 A panic in your plugin's code - editor build/open/close, resize/scale, iOS touch and keyboard handling, or DSP /prepare - is now caught at the host boundary on every","text":"Notable changes per release. 6.1.2 A panic in your plugin's code - editor build/open/close, resize/scale, iOS touch and keyboard handling, or DSP /prepare - is now caught at the host boundary on every format instead of crashing the DAW, and a caught editor panic no longer freezes or leaks the editor. is safe to call from inside an editor method; it applies on the next frame. Audio-thread robustness fixes: the standalone host runs input-only plugins (analyzers, meters, sinks), a truncated or malformed LV2 transport/parameter message can't cause an out-of-bounds read, and a VST2 switch to a wider channel layout no longer risks a mid-block allocation. 6.1.1 Standalone no longer crashes on MIDI-only plugins (zero output buses, e.g. transpose / arpeggio), which previously panicked on the first audio callback. MIDI output from the standalone isn't wired up yet, so their emitted events still go nowhere. Author-code and GUI-teardown panics can't abort the host: instance create/destroy and param format/parse callbacks are panic-guarded on every format, closing the remaining gaps on VST3, AAX, and CLAP. Parameter writes clamp to the declared range (float, int, and enum) and reject non-finite floats, and the smoother self-heals from a NaN accumulator, so a corrupt project or preset can't poison the audio path. 6.1.0 Sidechain and aux inputs now reach your plugin as separate buses, so a layout gives the host a real sidechain to route into. VST3, AU (v2/v3), and AAX advertise the sidechain as its own bus/port; the plugin sees main channels first, then sidechain. AAX uses Pro Tools' mono side-chain, duplicated across the declared width. VST2 exposes the sidechain as extra input pins, now labeled \"Input N\" / \"Sidechain N\" so hosts can route to them. Standalone feeds the sidechain bus, independent of (main bus); works live and offline. Test driver: drives the sidechain independently of the main input, width auto-detected from the layout. New : stereo main + stereo sidechain with IN/SC meters and a Mix knob. LV2 sidechain plugins run main-only for now (the TTL is generated from the plugin category and can't declare the sidechain ports yet); a one-time log notes the dropped sidechain. Fixes Host text entry into a parameter field parses back through the plugin, on every format. Stepped, enum, and toggle parameters are advertised as discrete/indexed/boolean with value labels, not continuous (VST3, AU, AAX, VST2, LV2); AAX step counts are no longer off by one. clamps out-of-range indices to the last variant. Linear smoothing converges at the correct constant-increment rate. Transport position in seconds is reported on VST3, VST2, AU, AAX, and LV2 (was always zero); LV2 also reports beat/tempo position. Lock-free state save re-serializes only when state changes (opt-in ) or, for MB-scale state, publishes off the audio thread via instead of copying the buffer every block; pre-warms the inline buffer. keyed envelope: add/remove/reorder-safe across sessions, rejects unknown format versions, and rename-type-safe (a changed field type falls back to its default rather than corrupting later fields). Unconnected sidechains read as silence rather than misrouting or panicking; fixed sidechain buffer aliasing on AU v2, and output-buffer aliasing plus channel-count negotiation on VST3 wide layouts. Preset loading hardened; preset-handling fixes on CLAP and AU. AAX parameter taper corrected so non-linear ranges track in Pro Tools. Editor-lifecycle and parameter-format callbacks are panic-guarded on every format (editor build/open/close, value format/parse, reset, instantiation), matching the state save/load guards: an author-code panic returns a safe default instead of aborting the host. Per-format correctness fixes: VST3 result-code (ABI) values; CLAP latency/tail, category, modulatable-flag reporting; AU bus-layout and render-notify handling; VST2 chunk frees, time-signature, missing opcodes; LV2 latency reporting and bypass flag. 6.0.0 Saving your plugin's s"},{"id":2,"title":"Examples","section":"Examples","href":"/docs/examples","headings":"Effects, instruments, MIDI · GUI framework comparison · Gain (small editor, four widgets) · GUI Zoo (every widget kind, mixed spans + positions) · Block DSP · Out-of-tree · Reiss & McPherson effects · Utility plugins · Project structure","excerpt":"Example plugins covering effects, instruments, MIDI processors, and GUI framework integrations. Each one lives under in the truce repo and is small enough to read top-to-bottom in one sitting. Effects","text":"Example plugins covering effects, instruments, MIDI processors, and GUI framework integrations. Each one lives under in the truce repo and is small enough to read top-to-bottom in one sitting. Effects, instruments, MIDI Plugin Type GUI Screenshot -------- ------ ----- ----------- gain Effect Built-in eq Effect Built-in synth Instrument Built-in transpose MIDI Built-in arpeggio MIDI Built-in tremolo Effect egui state Effect egui envelope Effect Built-in cc-filter Effect Built-in chord Instrument Built-in stereo-utility Effect Built-in sidechain Effect Built-in dasp-bitcrusher Effect Built-in sysex-echo MIDI Built-in multiport Instrument Built-in midi-inspector Effect Iced mpe-spreader MIDI Built-in mpe-synth Instrument Built-in fundsp-reverb-simple Effect Built-in fundsp-reverb-worker Effect Built-in The two crates share a topology and signal flow but rebuild the graph differently; the fundsp integration guide walks through both. GUI framework comparison The same plugin written across five GUI backends. Compare them to see how each framework handles the same layout. Gain (small editor, four widgets) Plugin GUI Screenshot -------- ----- ----------- gain Built-in gain-egui egui gain-iced Iced gain-slint Slint gain-vizia Vizia A sixth variant, gain-egui-aspect, is the egui gain with a 2:3 aspect-ratio lock on host resize. GUI Zoo (every widget kind, mixed spans + positions) A passthrough plugin that exercises every built-in widget kind (knob, slider, toggle, dropdown, meter, XY pad) across mixed grid spans and positions. Layout / widget regressions surface here before they reach the real example plugins; it's also the natural reference when picking a GUI framework. Plugin GUI Screenshot -------- ----- ----------- gui-zoo Built-in gui-zoo-egui egui gui-zoo-iced Iced gui-zoo-slint Slint gui-zoo-vizia Vizia The slint + vizia variants live under (their own Cargo sub-workspaces — see the vizia integration guide for why) rather than the top-level directory. Block DSP Plugin Demonstrates Screenshot -------- -------------- ----------- block-gain block-drywet block-gate + block-saturate block-widen block-surround-meter Each example isolates one or shape. See the SIMD section of the processing guide for the design. Out-of-tree Larger examples live in their own repos — useful when you want to see what truce looks like at the scale of a real plugin rather than a 100-line teaching example. Reiss & McPherson effects 15 effects ported from Reiss & McPherson's Audio Effects: Theory, Implementation and Application (originally Juan Gil's JUCE implementations). Sources in reiss-mcpherson-effects. Plugin Effect Screenshot -------- -------- ----------- reiss-delay Circular-buffer delay reiss-vibrato LFO-modulated delay (pitch wobble) reiss-flanger Modulated short delay + dry sum reiss-chorus Multi-voice ensemble chorus reiss-pingpong Cross-channel ping-pong delay reiss-parametric-eq Single-band parametric EQ (7 shapes) reiss-wahwah Manual / LFO / envelope wah reiss-phaser Cascaded all-pass phaser reiss-tremolo LFO amplitude modulation reiss-ringmod Ring modulation reiss-compressor Compressor / expander / gate reiss-distortion 5-shape waveshaper + tone shelf reiss-panning Panorama+precedence / ITD+ILD pan reiss-robotization Phase-vocoder robot / whisper reiss-pitchshift Phase-vocoder pitch shifter Utility plugins Plugin What it shows -------- --------------- truce-analyzer Real-time spectrum analyzer with diff overlay; non-trivial GUI built on truce. Project structure Each example follows the same layout: GUI framework examples may have additional files:"},{"id":3,"title":"Arpeggio","section":"Examples","href":"/docs/examples/arpeggio","headings":"What it demonstrates · Parameters","excerpt":"Tempo-synced arpeggiator that sequences held notes in configurable patterns. rebuilds its sequence into reused, pre-sized buffers so never allocates on the audio thread - its test uses the rt-paranoid","text":"Tempo-synced arpeggiator that sequences held notes in configurable patterns. rebuilds its sequence into reused, pre-sized buffers so never allocates on the audio thread - its test uses the rt-paranoid checker to assert it stays allocation-free. What it demonstrates Tempo-synced MIDI processing using Enum parameter ( ) with dropdown widget Stateful MIDI effect: tracks held notes, manages step counter and gate timing Octave stacking and directional patterns (Up, Down, Up/Down, Random) Rebuilding per-block DSP state without allocating on the audio thread Parameters Name Range Unit Description ------ ------- ------ ------------- Rate 1 to 8 -- Note divisions (1=whole, 4=quarter, 8=eighth) Gate 0.1 to 1.0 % Note length as fraction of step Octaves 1 to 4 -- Octaves to stack above held notes Pattern Up/Down/Up-Down/Random -- Arp direction"},{"id":4,"title":"Block Dry/Wet","section":"Examples","href":"/docs/examples/block-drywet","headings":"What it demonstrates · Parameters","excerpt":"Parallel-saturation dry/wet plugin. Wet path is a soft-clip after a drive boost; dry and wet blend through at the user ratio. Source: . What it demonstrates as a dry/wet cross-fader (its canonical use","text":"Parallel-saturation dry/wet plugin. Wet path is a soft-clip after a drive boost; dry and wet blend through at the user ratio. Source: . What it demonstrates as a dry/wet cross-fader (its canonical use case; no other example calls it). as the wet-shaping transform. Scalar-per-call mix coefficients: read the smoothed value once per block (zero-order hold within the block; the smoother bridges block-to-block transitions). Parameters Name Range Unit Default ------ ------- ------ --------- Drive 0 to 24 dB 6.0 Mix 0 to 1 % 0.5"},{"id":5,"title":"Block Gain","section":"Examples","href":"/docs/examples/block-gain","headings":"What it demonstrates · Parameters","excerpt":"Fully SIMD-optimized gain plugin: block-rate processing plus a vectorized envelope precompute for the slow-path smoothing case. Source: . What it demonstrates Fast path on converged smoothers ( ) vs. ","text":"Fully SIMD-optimized gain plugin: block-rate processing plus a vectorized envelope precompute for the slow-path smoothing case. Source: . What it demonstrates Fast path on converged smoothers ( ) vs. slow path during active smoothing. for the slow-path envelope precompute. With many smoothed gain knobs in flight, this closes the gap between block-rate and the pre-vectorization scalar baseline. Trade-off: one extra stack scratch and one extra pass over the envelope. Net win measurable when N smoothers ≥ 4. Parameters Name Range Unit ------ ------- ------ Gain -60 to +6 dB Pan -1 to +1 pan"},{"id":6,"title":"Block Gate","section":"Examples","href":"/docs/examples/block-gate","headings":"What it demonstrates · Parameters","excerpt":"Noise gate: passes audio through when the block peak is above threshold, zeroes the output when below. Per-block detect and apply (hard gate, zero ramp) to keep the example focused on the two SIMD ops","text":"Noise gate: passes audio through when the block peak is above threshold, zeroes the output when below. Per-block detect and apply (hard gate, zero ramp) to keep the example focused on the two SIMD ops it exists to demo. Source: . What it demonstrates for the per-channel peak detection in the detect stage. for the fast silence path in the apply stage. For a production gate you'd add an attack/release envelope (a per-sample 0..1 ramp applied via ); see for the envelope shape. Stripped here to keep the diff tight. Parameters Name Range Unit Default ------ ------- ------ --------- Threshold -80 to 0 dB -40.0"},{"id":7,"title":"Block Saturate","section":"Examples","href":"/docs/examples/block-saturate","headings":"What it demonstrates · Parameters","excerpt":"Series saturator: drive → soft-clip → make-up gain. Demonstrates composing and through scratch buffers when the chain has more than one stage. Source: . What it demonstrates in series with two gain st","text":"Series saturator: drive → soft-clip → make-up gain. Demonstrates composing and through scratch buffers when the chain has more than one stage. Source: . What it demonstrates in series with two gain stages. Fast path (smoothers converged): scalar drive / output linear constants, two calls around a single . Slow path (smoothing in progress): vectorize the dB → linear envelope via , then apply the chain per channel via (per-sample envelope) instead of (scalar). Parameters Name Range Unit Default ------ ------- ------ --------- Drive 0 to 36 dB 6.0 Output -24 to +6 dB 0.0"},{"id":8,"title":"Block Surround Meter","section":"Examples","href":"/docs/examples/block-surround-meter","headings":"What it demonstrates · Parameters","excerpt":"5.1 surround pass-through with per-channel dB-scale meters. Demos [ ] on an array of per-channel peaks converted all at once. For one or two peaks a scalar is cheaper; the SIMD form starts winning aro","text":"5.1 surround pass-through with per-channel dB-scale meters. Demos [ ] on an array of per-channel peaks converted all at once. For one or two peaks a scalar is cheaper; the SIMD form starts winning around 4+ elements, which 5.1 (L, R, C, LFE, Ls, Rs) gives us exactly. Source: . What it demonstrates Non-stereo override. Advertises both stereo and 5.1 so the plugin loads in DAWs without surround support; channels beyond the host-selected layout's count are not touched. Per block: trim each channel ( ), find linear peak ( ), convert the peak array to dB in one SIMD call ( ), then clamp / re-normalize to for the meter widget. Linear meter on a logarithmic dB scale — the perceptually correct visual. Parameters Name Range Unit Default ------ ------- ------ --------- Trim -24 to +24 dB 0.0"},{"id":9,"title":"Block Widen","section":"Examples","href":"/docs/examples/block-widen","headings":"What it demonstrates · Parameters","excerpt":"Stereo widener via mid-side recombination. Decomposes input into and , then recombines as , . recovers the input; collapses to mono; exaggerates the stereo image. Source: . What it demonstrates for th","text":"Stereo widener via mid-side recombination. Decomposes input into and , then recombines as , . recovers the input; collapses to mono; exaggerates the stereo image. Source: . What it demonstrates for the mid-side recombination: lay down the mid into the output ( ), then add a scaled side ( ). No other example in the tree uses ; this is the cleanest demo of \"additive blend with a scalar coefficient\". Parameters Name Range Default ------ ------- --------- Width 0 to 2 1.0"},{"id":10,"title":"CC Filter","section":"Examples","href":"/docs/examples/cc-filter","headings":"What it demonstrates · Parameters","excerpt":"One-pole low-pass filter whose cutoff is steered by an incoming MIDI CC (default CC74, the standard \"brightness\" controller). Source: . What it demonstrates Audio effect that consumes MIDI ( in ; on A","text":"One-pole low-pass filter whose cutoff is steered by an incoming MIDI CC (default CC74, the standard \"brightness\" controller). Source: . What it demonstrates Audio effect that consumes MIDI ( in ; on AU this registers the component as an MusicEffect so hosts actually route MIDI to it) Handling events in to steer DSP state Declarative host MIDI mapping via on a attribute Logarithmic parameter range ( ) with exponential smoothing ( ) f64 DSP state (per-channel one-pole ) cast to f32 only at the output boundary Parameters Name Range Unit Description ------ ------- ------ ------------- Cutoff 20 to 20000 (log) Hz Cutoff used before any CC arrives; incoming CC overrides it CC 0 to 127 -- CC number that steers the cutoff"},{"id":11,"title":"Chord","section":"Examples","href":"/docs/examples/chord","headings":"What it demonstrates · Parameters","excerpt":"Chord instrument: play one note, hear a triad (or 7th), and the chord's notes are also emitted to the host as MIDI so they can drive a second instrument. Source: . What it demonstrates Instrument that","text":"Chord instrument: play one note, hear a triad (or 7th), and the chord's notes are also emitted to the host as MIDI so they can drive a second instrument. Source: . What it demonstrates Instrument that emits MIDI ( in so every format declares its MIDI output port/bus) with and variant labels Emitting / events via at the incoming event's Last-note priority with matched emission for every sounding voice, preventing stuck notes downstream Output-only bus layout ( ) Built-in editor with and widgets Parameters Name Range Unit Description ------ ------- ------ ------------- Chord Major, Minor, Maj7, Min7, Sus4 -- Chord quality as intervals from the played root Gain 0 to 1 % Output level, smoothed ( )"},{"id":12,"title":"dasp Bitcrusher","section":"Examples","href":"/docs/examples/dasp-bitcrusher","headings":"What it demonstrates · Parameters","excerpt":"Bitcrusher with bit-depth reduction, sample-and-hold rate reduction, and a dry/wet mix. Shows how to integrate a third-party DSP crate (dasp) into a truce plugin. Source: . What it demonstrates Third-","text":"Bitcrusher with bit-depth reduction, sample-and-hold rate reduction, and a dry/wet mix. Shows how to integrate a third-party DSP crate (dasp) into a truce plugin. Source: . What it demonstrates Third-party DSP crate integration: quantization routes through ( / ) for the depths dasp ships as concrete types, with a floor-quantize fallback for the rest with ranges and the boundary-read helpers ( ) Per-channel sample-and-hold state ( , ) reset in and carried across blocks Built-in editor: three widgets addressed by the derived enum Driver-based passthrough test ( with ) plus per-OS baselines Parameters Name Range Unit ------ ------- ------ Bits 2 to 16 (discrete, default 16) bits Hold 1 to 32 (discrete, default 1) samples Mix 0 to 1 (default 1.0) %"},{"id":13,"title":"Envelope","section":"Examples","href":"/docs/examples/envelope","headings":"What it demonstrates · Parameters","excerpt":"Envelope follower to MIDI CC: tracks the input signal's level and emits it as a control-change while passing the audio through unchanged. Drop it on a track and use that track's dynamics to modulate a","text":"Envelope follower to MIDI CC: tracks the input signal's level and emits it as a control-change while passing the audio through unchanged. Drop it on a track and use that track's dynamics to modulate another instrument or effect over MIDI. Source: . What it demonstrates Plugin-to-host MIDI output: in plus with Sample-accurate event timestamps, emitting on 7-bit value change instead of once per block De-duplication of consecutive identical CC values so a steady level does not flood the host A peak follower with instant attack and a parameterized release ( range, on the Release param) Explicit returning and bit-exact audio passthrough via + Parameters Name Range Unit ------ ------- ------ CC 0 to 127 (discrete, default 1) CC number Release 1 to 1000 (log, default 100) ms"},{"id":14,"title":"EQ","section":"Examples","href":"/docs/examples/eq","headings":"What it demonstrates · Parameters · Files","excerpt":"3-band parametric equalizer using biquad filters. Source: . What it demonstrates Biquad filter DSP (Direct Form II Transposed) Per-sample coefficient updates from smoothed parameters Logarithmic param","text":"3-band parametric equalizer using biquad filters. Source: . What it demonstrates Biquad filter DSP (Direct Form II Transposed) Per-sample coefficient updates from smoothed parameters Logarithmic parameter ranges for frequency and Q Section-based grid layout ( , , ) Auto-bypass when band gain is near 0 dB end-to-end via — biquad coefficient computation is precision-sensitive (especially at low Q × low frequency). On formats with a 64-bit wire (VST3, VST2, CLAP) the plugin processes the host's buffers directly; elsewhere the wrapper widens host at the block boundary so the DSP path stays in either way. Legacy state migration — the reference implementation: a pre-truce blob translates into truce params, tested with (and for blobs the plugin must refuse). See the state guide. Parameters Name Range Unit Description ------ ------- ------ ------------- Low Freq 20 to 1000 Hz Low band center frequency Low Gain -18 to +18 dB Low band boost/cut Low Q 0.1 to 10 -- Low band width Mid Freq 200 to 8000 Hz Mid band center frequency Mid Gain -18 to +18 dB Mid band boost/cut Mid Q 0.1 to 10 -- Mid band width High Freq 1000 to 20000 Hz High band center frequency High Gain -18 to +18 dB High band boost/cut High Q 0.1 to 10 -- High band width Output -18 to +18 dB Output trim Files — plugin logic, GUI layout with sections — standalone biquad filter implementation"},{"id":15,"title":"Fundsp Reverb (Simple variant)","section":"Examples","href":"/docs/examples/fundsp-reverb-simple","headings":"Integration patterns · Gotchas","excerpt":"Stereo plate reverb wired through a audio graph. Same topology / params / signal flow as fundsp-reverb-worker, but the graph rebuild happens inline on the audio thread instead of on a background worke","text":"Stereo plate reverb wired through a audio graph. Same topology / params / signal flow as fundsp-reverb-worker, but the graph rebuild happens inline on the audio thread instead of on a background worker. Source: . The fundsp integration guide walks through both variants in depth, explaining the rebuild trigger, why some params need a rebuild, and what the worker pattern buys you. Param Range ---------- ------------------------ Low Cut 20 Hz → 2 kHz (log) High Cut 500 Hz → 18 kHz (log) Time 0.1 s → 20 s (log) Mix 0 → 1 Integration patterns Graph rebuilt inline in . When Time crosses the hysteresis threshold, calls directly. and run on the audio thread. The worker variant moves both off-thread; everything else about the graph wiring is identical between the two crates. Hysteresis on Time changes. bakes RT60 at construction, so each crossing means a full rebuild. The 5% threshold keeps tiny drifts (smoother ramps, automation jitter) from triggering rebuilds. Read the raw target, not the smoothed value. A smoothed would crawl across the threshold for 200 ms on each knob move and rebuild every block until it settled — audible as an unstable tail. Params reach the graph through atomics. reads them per sample; the closure inside writes the smoothed truce-side value into the cell on the same tick (sample-accurate automation). for the field type. The concrete is hundreds of chars of nested generics; the vtable cost is one indirection per block. transposes truce's per-channel layout into stack-allocated 2-in/2-out frames so fundsp's callback can be called directly. It's the shorthand for , so the same call runs over any declared bus (a mono source fans into both graph inputs, a stereo bus maps 1:1) with no per-width branch. No scratch field. Gotchas Filter input order is positional and unchecked. / take . Every connection is , so compiles fine and silently feeds the filter cutoff in as audio — the resulting filter blows up the reverb FDN to peak 7000 within a second. Test against constant input + . Type-level channels. fails to compile when is stereo and is a 1-channel read; broadcast the mix to stereo manually with . fundsp's payoff (graph composition with / / ) costs this kind of explicit plumbing."},{"id":16,"title":"Fundsp Reverb (Worker variant)","section":"Examples","href":"/docs/examples/fundsp-reverb-worker","headings":"Integration patterns · Gotchas","excerpt":"Stereo plate reverb wired through a audio graph. The point of this example is the integration shape — how to hold a fundsp graph inside a truce plugin and keep it alloc-free on the audio thread. This ","text":"Stereo plate reverb wired through a audio graph. The point of this example is the integration shape — how to hold a fundsp graph inside a truce plugin and keep it alloc-free on the audio thread. This is the production-pattern variant: graph rebuilds run on truce's managed background-task pool and the audio thread picks up the result via a lock-free swap. For the simpler inline-rebuild version (rt-unsafe but easier to read top-to-bottom), see fundsp-reverb-simple. Both crates share the same topology, params, and signal flow. Source: . The fundsp integration guide walks through both variants in depth. Param Range ---------- ------------------------ Low Cut 20 Hz → 2 kHz (log) High Cut 500 Hz → 18 kHz (log) Time 0.1 s → 20 s (log) Mix 0 → 1 Integration patterns Graph built off the audio thread. builds the initial graph synchronously (host calls it off the audio path); subsequent rebuilds run on truce's managed background-task pool. never allocates, never calls , never calls , and never drops a . Managed rebuild + lock-free handoff. The rebuild is a task: posts the latest target with (wait-free, newest wins) and the pool runs the handler off-thread. Two s carry the graphs themselves: (worker → audio, the freshly-built graph) and (audio → worker, so the old graph is dropped off-thread). The pool owns the thread and its wakeup, so there's no / or join to write. Rebuilds carry their SR. Each ready graph is tagged with the sample rate it was built for. If swaps in a new SR while a rebuild is in flight, the audio thread sees the SR mismatch and reroutes the stale graph to the discard queue rather than swapping it in. Params reach the graph through atomics. reads them per sample; the closure inside writes the smoothed truce-side value into the cell on the same tick (sample-accurate automation). for the field type. The concrete is hundreds of chars of nested generics; the vtable cost is one indirection per block. transposes truce's per-channel layout into stack-allocated 2-in/2-out frames so fundsp's callback can be called directly. It's the shorthand for , so the same call runs over any declared bus (a mono source fans into both graph inputs, a stereo bus maps 1:1) with no per-width branch. No scratch field. Reverb time schedules a rebuild task when the param drifts ≥ 5% — 's argument is baked at construction. The audio thread reads the raw (not the smoothed ) so a knob ramp doesn't trip the threshold every block while the smoother crawls across it. Gotchas Filter input order is positional and unchecked. / take . Every connection is , so compiles fine and silently feeds the filter cutoff in as audio — the resulting filter blows up the reverb FDN to peak 7000 within a second. Test against constant input + . Type-level channels. fails to compile when is stereo and is a 1-channel read; broadcast the mix to stereo manually with . fundsp's payoff (graph composition with / / ) costs this kind of explicit plumbing."},{"id":17,"title":"Gain (egui, aspect-locked)","section":"Examples","href":"/docs/examples/gain-egui-aspect","headings":"What it demonstrates · Parameters","excerpt":"The egui gain example with a 2:3 aspect-ratio lock: the editor advertises the ratio so the host keeps width and height proportional on every resize edge. Source: . What it demonstrates The builder: , ","text":"The egui gain example with a 2:3 aspect-ratio lock: the editor advertises the ratio so the host keeps width and height proportional on every resize edge. Source: . What it demonstrates The builder: , , , and , with the window size and both bounds sitting exactly on 2:3 so the lock holds across the whole range Aspect-lock host coverage: honored by CLAP, VST3, AU v3, LV2, and the standalone host; VST2 and AAX ignore it : , a two-axis bound to Pan and Gain, and a stereo fields fed from via and Editor theming with and Constant-power-style pan law layered on smoothed dB gain ( , ) Parameters Name Range Unit ------ ------- ------ Gain -60 to +6 dB Pan -1 to +1 pan Two meter slots (left/right output peak) feed the level meter; they are display-only, not automatable parameters."},{"id":18,"title":"Gain (egui)","section":"Examples","href":"/docs/examples/gain-egui","headings":"What it demonstrates · Layout · See also","excerpt":"The gain plugin reimplemented with an egui GUI. Same DSP as the built-in gain example, different frontend. Source: . What it demonstrates with a closure-based UI function , , helper widgets Custom hea","text":"The gain plugin reimplemented with an egui GUI. Same DSP as the built-in gain example, different frontend. Source: . What it demonstrates with a closure-based UI function , , helper widgets Custom header using Theme color constants from Consistent window sizing with / constants Screenshot testing with Layout Header bar with plugin name, followed by two knobs (Gain, Pan), a thin stereo meter spanning the full height, and an XY pad below the knobs. See also egui integration guide gain — same plugin with the built-in GUI gain-iced — same plugin with iced gain-slint — same plugin with slint gain-vizia — same plugin with vizia (desktop only)"},{"id":19,"title":"Gain (iced)","section":"Examples","href":"/docs/examples/gain-iced","headings":"What it demonstrates · Layout · See also","excerpt":"The gain plugin reimplemented with an iced GUI. Same DSP as the built-in gain example, different frontend. Source: . What it demonstrates trait with custom method , , canvas widgets with shorthand tra","text":"The gain plugin reimplemented with an iced GUI. Same DSP as the built-in gain example, different frontend. Source: . What it demonstrates trait with custom method , , canvas widgets with shorthand trait for clean iced layout code Custom header using with styled background Default (no custom messages needed for this simple UI) Consistent window sizing with / constants Screenshot testing with Layout Header bar with plugin name. Left column with two knobs (Gain, Pan) and an XY pad. Thin stereo meter spanning the full height on the right. See also iced integration guide gain — same plugin with the built-in GUI gain-egui — same plugin with egui gain-slint — same plugin with slint gain-vizia — same plugin with vizia (desktop only)"},{"id":20,"title":"Gain (slint)","section":"Examples","href":"/docs/examples/gain-slint","headings":"What it demonstrates · Layout · Files · See also","excerpt":"The gain plugin reimplemented with a Slint GUI. Same DSP as the built-in gain example, different frontend. Source: . What it demonstrates with setup closure and per-frame sync Custom markup with , , f","text":"The gain plugin reimplemented with a Slint GUI. Same DSP as the built-in gain example, different frontend. Source: . What it demonstrates with setup closure and per-frame sync Custom markup with , , from Manual callback wiring ( , ) Formatted value text synced from Rust ( ) Header bar implemented in markup Consistent window sizing between editor and snapshot test Screenshot testing with (software renderer) Layout Header bar with plugin name. Left column with two knobs (Gain, Pan) and an XY pad. Thin stereo meter spanning the full height on the right. All defined in . Files — plugin logic, editor wiring, snapshot test — declarative UI markup — compiles to Rust See also slint integration guide gain — same plugin with the built-in GUI gain-egui — same plugin with egui gain-iced — same plugin with iced gain-vizia — same plugin with vizia (desktop only)"},{"id":21,"title":"Gain (vizia)","section":"Examples","href":"/docs/examples/gain-vizia","headings":"What it demonstrates · Layout · Files · See also","excerpt":"The gain plugin reimplemented with a Vizia GUI. Same DSP as the built-in gain example, different frontend. Source: . What it demonstrates with a setup closure receiving + , , widget helpers Shared per","text":"The gain plugin reimplemented with a Vizia GUI. Same DSP as the built-in gain example, different frontend. Source: . What it demonstrates with a setup closure receiving + , , widget helpers Shared per param — knob and XY pad bound to the same stay in sync without extra wiring Per-OS screenshot baselines with on Linux and Windows (rasterizer drift) for vizia layout shims for cross-backend visual parity Layout Left column: two knobs (Gain, Pan) over a 130×130 Pan/Gain XY pad. Right column: stereo meter spanning the full height. 176×260 logical points, aligned so the XY pad's bottom edge matches the meter's. Files — plugin logic, editor wiring, screenshot tests — standalone host entry point See also vizia integration guide gain — same plugin with the built-in GUI gain-egui — same plugin with egui gain-iced — same plugin with iced gain-slint — same plugin with slint"},{"id":22,"title":"Gain","section":"Examples","href":"/docs/examples/gain","headings":"What it demonstrates · Parameters · GUI variants","excerpt":"Stereo gain and pan utility with level metering. Uses the built-in GUI. Source: . What it demonstrates Exponentially smoothed parameters ( ) Naive pan law ( , ) Peak metering via Grid layout with knob","text":"Stereo gain and pan utility with level metering. Uses the built-in GUI. Source: . What it demonstrates Exponentially smoothed parameters ( ) Naive pan law ( , ) Peak metering via Grid layout with knobs, XY pad, and stereo meter Parameters Name Range Unit Description ------ ------- ------ ------------- Gain -60 to +6 dB Output level Pan -1 to +1 pan Stereo balance GUI variants The same gain plugin is implemented with five different GUI backends — compare them to see how each framework handles the same layout: gain (this one) — built-in grid layout gain-egui — egui immediate-mode widgets gain-iced — iced retained-mode widgets gain-slint — declarative markup gain-vizia — vizia reactive widgets (desktop only)"},{"id":23,"title":"GUI Zoo (egui)","section":"Examples","href":"/docs/examples/gui-zoo-egui","headings":"What it demonstrates","excerpt":"The widget reference rendered through . Mirrors the built-in gui-zoo's param set so each truce-egui widget counterpart is exercised against the same shape. Source: . What it demonstrates Egui counterp","text":"The widget reference rendered through . Mirrors the built-in gui-zoo's param set so each truce-egui widget counterpart is exercised against the same shape. Source: . What it demonstrates Egui counterparts of every built-in widget kind Egui panel + grid composition for laying out the same widget set"},{"id":24,"title":"GUI Zoo (iced)","section":"Examples","href":"/docs/examples/gui-zoo-iced","headings":"What it demonstrates","excerpt":"The widget reference rendered through . Mirrors the built-in gui-zoo's param set so each truce-iced widget counterpart is exercised against the same shape. Source: . What it demonstrates Iced counterp","text":"The widget reference rendered through . Mirrors the built-in gui-zoo's param set so each truce-iced widget counterpart is exercised against the same shape. Source: . What it demonstrates Iced counterparts of every built-in widget kind Iced / / composition"},{"id":25,"title":"GUI Zoo (slint)","section":"Examples","href":"/docs/examples/gui-zoo-slint","headings":"What it demonstrates","excerpt":"The widget reference rendered through . Mirrors the built-in gui-zoo's param set, with the UI defined in a declarative markup file rather than Rust code. Source: . What it demonstrates Slint markup co","text":"The widget reference rendered through . Mirrors the built-in gui-zoo's param set, with the UI defined in a declarative markup file rather than Rust code. Source: . What it demonstrates Slint markup composition for laying out the same widget set Build-time compilation via + Lives in the Cargo sub-workspace - see the slint integration guide."},{"id":26,"title":"GUI Zoo (vizia)","section":"Examples","href":"/docs/examples/gui-zoo-vizia","headings":"What it demonstrates","excerpt":"The widget reference rendered through . Mirrors the built-in gui-zoo's param set so each truce-vizia widget counterpart is exercised against the same shape. Source: . What it demonstrates Vizia counte","text":"The widget reference rendered through . Mirrors the built-in gui-zoo's param set so each truce-vizia widget counterpart is exercised against the same shape. Source: . What it demonstrates Vizia counterparts of every built-in widget kind Vizia / / composition Lives in the Cargo sub-workspace - see the vizia integration guide. Desktop only."},{"id":27,"title":"GUI Zoo","section":"Examples","href":"/docs/examples/gui-zoo","headings":"What it demonstrates","excerpt":"Passthrough plugin built around the built-in editor that exercises every widget kind in one layout. Source: . What it demonstrates Every built-in widget in one editor Auto-flow placement mixed with ex","text":"Passthrough plugin built around the built-in editor that exercises every widget kind in one layout. Source: . What it demonstrates Every built-in widget in one editor Auto-flow placement mixed with explicit positioning Cell spans across both axes Mirrored across four other GUI backends - gui-zoo-egui, gui-zoo-iced, gui-zoo-slint, and gui-zoo-vizia - same param set, different renderer."},{"id":28,"title":"MIDI Inspector","section":"Examples","href":"/docs/examples/midi-inspector","headings":"What it demonstrates · Parameters","excerpt":"Audio effect with MIDI in/out that passes the track's audio through untouched, forwards MIDI, and shows a live scrolling log of every event it receives, decoded with a raw fallback line. Source: . Wha","text":"Audio effect with MIDI in/out that passes the track's audio through untouched, forwards MIDI, and shows a live scrolling log of every event it receives, decoded with a raw fallback line. Source: . What it demonstrates MIDI 2.0 opt-in ( in ) so hosts deliver native 2.0 events ( , , ...) for full-resolution display Streaming structured realtime data (not just scalar meters) from the audio thread to the editor: a lock-free ( ) that pushes into and the editor drains each frame Sharing non-parameter state through a field on the struct, reached via the same on both threads iced editor via / , with polling the ring so a live MIDI stream repaints promptly, plus pause / clear / category filters SysEx handling: resolves payloads, and thru re-pushes them with so bytes land in the output list's own pool Stereo passthrough bus ( ) so the plugin loads as an ordinary insert in audio-centric hosts that don't host MIDI-only plugins Parameters Name Range Unit Description ------ ------- ------ ------------- MIDI Thru on/off -- Forward every MIDI event to the output (on, default) or monitor only (off)"},{"id":29,"title":"MPE Spreader","section":"Examples","href":"/docs/examples/mpe-spreader","headings":"What it demonstrates · Parameters","excerpt":"MIDI effect that promotes MIDI 1.0 to MIDI 2.0 and spreads notes across the channel address space, with per-note vibrato and brightness modes. A test vehicle for truce's MIDI 2.0 encode surface. Sourc","text":"MIDI effect that promotes MIDI 1.0 to MIDI 2.0 and spreads notes across the channel address space, with per-note vibrato and brightness modes. A test vehicle for truce's MIDI 2.0 encode surface. Source: . What it demonstrates MIDI 2.0 output opt-in ( in ): every 1.0 channel-voice message is re-emitted as its 2.0 ( , , , , , ) Value promotion via the upscale helpers ( , , ) MPE-style voice allocation: round-robin note fan across a configurable channel count Per-note expression output: a vibrato LFO emitted as 32-bit , and the mod wheel routed to 74 (brightness) on every held note Recentering pitch bends on note-off and mode switches so no note is left detuned algorithm selector rendered as a dropdown in a built-in editor Parameters Name Range Unit Description ------ ------- ------ ------------- Algorithm Passthrough / Channel Fan / Per-Note Vibrato (2.0 only) / MPE Vibrato / Mod Brightness -- How notes are addressed and which expression is generated Channels 1 to 16 -- Fan width for channel round-robin modes Vibrato Rate 0.1 to 12 (log) Hz Vibrato LFO rate Vibrato Depth 0 to 2 st Vibrato depth in semitones"},{"id":30,"title":"MPE Synth","section":"Examples","href":"/docs/examples/mpe-synth","headings":"What it demonstrates · Parameters","excerpt":"Per-note-expressive polyphonic synth that decodes native MIDI 2.0: 16-bit velocity, 32-bit per-note pitch bend, and per-note brightness. The render half of the MPE Spreader - it makes the spreader's p","text":"Per-note-expressive polyphonic synth that decodes native MIDI 2.0: 16-bit velocity, 32-bit per-note pitch bend, and per-note brightness. The render half of the MPE Spreader - it makes the spreader's per-note expression audible. Source: . What it demonstrates MIDI 2.0 input opt-in ( in ) so the host delivers native 2.0 events at full resolution Per-voice expression decode: 16-bit velocity to amplitude, (32-bit) to per-voice detune, 74 to per-voice filter cutoff, to channel-wide bend 1.0 fallback arms for every 2.0 event ( , , ) so the synth plays on VST2 / AU v2 / AAX / LV2 at 7/14-bit resolution 74 driving a host-visible parameter ( ) as a 32-bit macro MIDI channel mapped to equal-power stereo pan, so a channel-fanned chord spreads across the field; 32-voice pool with release-aware stealing Sample-accurate event dispatch inside the render loop, , smoothed params via Parameters Name Range Unit Description ------ ------- ------ ------------- Master Cutoff 0 to 1 % Master filter cutoff macro scaling every voice's brightness Release 0.01 to 4 s Amp envelope release time Volume -60 to 6 dB Output level"},{"id":31,"title":"Multiport","section":"Examples","href":"/docs/examples/multiport","headings":"What it demonstrates · Parameters","excerpt":"Multi-timbral instrument with two MIDI input ports: the host routes a separate track to each port, and each port plays its own independently shaped sound (port 0 defaults to a sine pad, port 1 to a sa","text":"Multi-timbral instrument with two MIDI input ports: the host routes a separate track to each port, and each port plays its own independently shaped sound (port 0 defaults to a sine pad, port 1 to a saw lead) from a single instance. Source: . What it demonstrates Multiple MIDI input ports ( in ); VST3 event buses and CLAP note ports carry the port, other formats clamp to one Reading to key voices by and select the per-port patch Nested parameter structs with / for stable flattened param ids Sample-accurate event handling: draining against inside the render loop Block-rate smoothed reads with and voice stealing that prefers releasing voices when all voices are silent, plus in tests Parameters Name Range Unit Description ------ ------- ------ ------------- P0 Wave Sine, Saw, Square, Triangle -- Port 0 oscillator waveform (default Sine) P0 Cutoff 0 to 1 % Port 0 one-pole low-pass cutoff P0 Release 0.01 to 4 s Port 0 release time P0 Volume -60 to 6 dB Port 0 lane volume P1 Wave Sine, Saw, Square, Triangle -- Port 1 oscillator waveform (default Saw) P1 Cutoff 0 to 1 % Port 1 one-pole low-pass cutoff P1 Release 0.01 to 4 s Port 1 release time P1 Volume -60 to 6 dB Port 1 lane volume"},{"id":32,"title":"Reiss Chorus","section":"Examples","href":"/docs/examples/reiss-chorus","headings":"What it demonstrates","excerpt":"Multi-voice chorus - several modulated delay lines summed in parallel for an ensemble effect. Source: . What it demonstrates Several LFO-modulated delay voices summed in parallel A per-voice phase off","text":"Multi-voice chorus - several modulated delay lines summed in parallel for an ensemble effect. Source: . What it demonstrates Several LFO-modulated delay voices summed in parallel A per-voice phase offset on the shared LFO Voice count as an"},{"id":33,"title":"Reiss Compressor","section":"Examples","href":"/docs/examples/reiss-compressor","headings":"What it demonstrates","excerpt":"Three-mode dynamics processor - compressor, expander, or gate - sharing a common envelope detector and gain computer. Source: . What it demonstrates Envelope detection with separate attack / release t","text":"Three-mode dynamics processor - compressor, expander, or gate - sharing a common envelope detector and gain computer. Source: . What it demonstrates Envelope detection with separate attack / release time constants A gain computer with soft-knee curvature Mode switch ( ) that flips the gain-computer shape without changing the envelope path"},{"id":34,"title":"Reiss Delay","section":"Examples","href":"/docs/examples/reiss-delay","headings":"What it demonstrates","excerpt":"Circular-buffer delay line with feedback and dry/wet mix. Source: . What it demonstrates Per-channel circular buffer indexed by read / write pointers Sub-sample delay times via interpolated read A smo","text":"Circular-buffer delay line with feedback and dry/wet mix. Source: . What it demonstrates Per-channel circular buffer indexed by read / write pointers Sub-sample delay times via interpolated read A smoothed time parameter to avoid pitch artifacts"},{"id":35,"title":"Reiss Distortion","section":"Examples","href":"/docs/examples/reiss-distortion","headings":"What it demonstrates","excerpt":"Five-shape waveshaper with input drive, output tone shelf, and dry/wet mix. Source: . What it demonstrates Five waveshaping functions selected by Input drive pre-shape and a tone shelf post-shape Dry/","text":"Five-shape waveshaper with input drive, output tone shelf, and dry/wet mix. Source: . What it demonstrates Five waveshaping functions selected by Input drive pre-shape and a tone shelf post-shape Dry/wet mix for parallel-style blending"},{"id":36,"title":"Reiss Flanger","section":"Examples","href":"/docs/examples/reiss-flanger","headings":"What it demonstrates","excerpt":"Modulated short delay summed with the dry signal - the sweeping comb-filter notches that produce the flanger sound. Source: . What it demonstrates LFO-modulated short delay summed with the dry input A","text":"Modulated short delay summed with the dry signal - the sweeping comb-filter notches that produce the flanger sound. Source: . What it demonstrates LFO-modulated short delay summed with the dry input A feedback path Switchable LFO waveform via"},{"id":37,"title":"Reiss Panning","section":"Examples","href":"/docs/examples/reiss-panning","headings":"What it demonstrates","excerpt":"Two stereo-pan algorithms selectable at runtime: Panorama + Precedence - tangent-law gain combined with a per-side time delay derived from the pan position. Aimed at loudspeakers. ITD + ILD - spherica","text":"Two stereo-pan algorithms selectable at runtime: Panorama + Precedence - tangent-law gain combined with a per-side time delay derived from the pan position. Aimed at loudspeakers. ITD + ILD - spherical-head model producing an interaural time difference (via fractional delay) and an interaural level difference (via a first-order shelf). Aimed at headphones. Source: . What it demonstrates Two pan implementations sharing the same parameter surface, selected by Per-channel short delay lines for the time-difference half A simple shelf filter for the level-difference half"},{"id":38,"title":"Reiss Parametric EQ","section":"Examples","href":"/docs/examples/reiss-parametric-eq","headings":"What it demonstrates","excerpt":"Single-band parametric EQ with seven filter shapes selectable at runtime: low-pass, high-pass, band-pass, notch, low-shelf, high-shelf, peaking. Source: . What it demonstrates Biquad coefficient gener","text":"Single-band parametric EQ with seven filter shapes selectable at runtime: low-pass, high-pass, band-pass, notch, low-shelf, high-shelf, peaking. Source: . What it demonstrates Biquad coefficient generation for the seven filter shapes Mode switch via Frequency / Q / gain controls shared across shapes"},{"id":39,"title":"Reiss Phaser","section":"Examples","href":"/docs/examples/reiss-phaser","headings":"What it demonstrates","excerpt":"Cascaded all-pass filters whose center frequency is swept by an LFO, summed with the dry signal - phase cancellation produces the moving notches. Source: . What it demonstrates An all-pass filter chai","text":"Cascaded all-pass filters whose center frequency is swept by an LFO, summed with the dry signal - phase cancellation produces the moving notches. Source: . What it demonstrates An all-pass filter chain LFO-modulated center frequency A feedback path"},{"id":40,"title":"Reiss Ping-Pong","section":"Examples","href":"/docs/examples/reiss-pingpong","headings":"What it demonstrates","excerpt":"Cross-channel feedback routing on top of a stereo delay - left output feeds the right channel's delay and vice versa, producing the bouncing stereo image. Source: . What it demonstrates Per-channel de","text":"Cross-channel feedback routing on top of a stereo delay - left output feeds the right channel's delay and vice versa, producing the bouncing stereo image. Source: . What it demonstrates Per-channel delay lines with cross-channel feedback A single time + feedback control driving both sides"},{"id":41,"title":"Reiss Pitch Shift","section":"Examples","href":"/docs/examples/reiss-pitchshift","headings":"What it demonstrates","excerpt":"Phase-vocoder pitch shifter - shifts the input by a configurable ratio while preserving the time scale. Source: . What it demonstrates An STFT-based phase vocoder with bin-shift resynthesis Phase-cohe","text":"Phase-vocoder pitch shifter - shifts the input by a configurable ratio while preserving the time scale. Source: . What it demonstrates An STFT-based phase vocoder with bin-shift resynthesis Phase-coherent reassembly for sustained tones Latency reporting for the analysis window"},{"id":42,"title":"Reiss Ring Modulator","section":"Examples","href":"/docs/examples/reiss-ringmod","headings":"What it demonstrates","excerpt":"Multiplies the input signal by a sine carrier, producing sum and difference frequencies. Source: . What it demonstrates Sample-by-sample multiplication of input by a sine oscillator Log-scaled carrier","text":"Multiplies the input signal by a sine carrier, producing sum and difference frequencies. Source: . What it demonstrates Sample-by-sample multiplication of input by a sine oscillator Log-scaled carrier frequency control"},{"id":43,"title":"Reiss Robotization","section":"Examples","href":"/docs/examples/reiss-robotization","headings":"What it demonstrates","excerpt":"Phase-vocoder effect with two operating modes - robotization (flat phase, retaining only magnitude) and whisperization (randomised phase, retaining only magnitude). Source: . What it demonstrates An S","text":"Phase-vocoder effect with two operating modes - robotization (flat phase, retaining only magnitude) and whisperization (randomised phase, retaining only magnitude). Source: . What it demonstrates An STFT-based phase vocoder (window, FFT, manipulate, IFFT, overlap-add) Two phase-treatment modes selected by Latency reporting for the analysis window"},{"id":44,"title":"Reiss Tremolo","section":"Examples","href":"/docs/examples/reiss-tremolo","headings":"What it demonstrates","excerpt":"LFO amplitude modulation - the classic pulsing volume effect. Source: . What it demonstrates Input multiplied by a unipolar LFO scaled by a depth control Switchable LFO waveform via A stereo phase off","text":"LFO amplitude modulation - the classic pulsing volume effect. Source: . What it demonstrates Input multiplied by a unipolar LFO scaled by a depth control Switchable LFO waveform via A stereo phase offset on the LFO"},{"id":45,"title":"Reiss Vibrato","section":"Examples","href":"/docs/examples/reiss-vibrato","headings":"What it demonstrates","excerpt":"LFO-modulated short delay - produces a periodic pitch wobble. Source: . What it demonstrates A short delay line whose read offset is driven by an LFO Switchable LFO waveform via Switchable interpolati","text":"LFO-modulated short delay - produces a periodic pitch wobble. Source: . What it demonstrates A short delay line whose read offset is driven by an LFO Switchable LFO waveform via Switchable interpolation mode for the fractional read"},{"id":46,"title":"Reiss Wah-Wah","section":"Examples","href":"/docs/examples/reiss-wahwah","headings":"What it demonstrates","excerpt":"Resonant band-pass filter swept by one of three sources: manual position, an LFO, or an envelope-follower on the input. Source: . What it demonstrates A resonant band-pass biquad with a sweepable cent","text":"Resonant band-pass filter swept by one of three sources: manual position, an LFO, or an envelope-follower on the input. Source: . What it demonstrates A resonant band-pass biquad with a sweepable center frequency Three modulation sources selected by An envelope-follower with attack / release"},{"id":47,"title":"Sidechain","section":"Examples","href":"/docs/examples/sidechain","headings":"What it demonstrates · Parameters · Testing the routing","excerpt":"Reference for wiring a separate sidechain input bus. Declares a stereo Main input, a stereo Sidechain input, and a stereo output; meters both input levels and blends them with a Mix knob, so the routi","text":"Reference for wiring a separate sidechain input bus. Declares a stereo Main input, a stereo Sidechain input, and a stereo output; meters both input levels and blends them with a Mix knob, so the routing is easy to verify by eye (the meters) and ear (the blend). Source: . What it demonstrates to declare a second input bus the host surfaces as a real sidechain (a bus on VST3, a CLAP port, extra VST2 pins, Pro Tools' mono side-chain on AAX) Flat channel indexing across buses: / is main L/R, / is sidechain L/R, in declaration order Detecting an unconnected sidechain with and gating the reads (the wrapper hands unconnected sidechain channels silence, so it never panics) to read one input channel while writing one output channel in the same loop Block-constant smoothing with , which advances the smoother across the whole block so the blend settles in 10 ms rather than 10 blocks Metering both buses into s drawn side by side in the built-in editor Parameters Name Range Unit ------ ------- ------ Mix 0 to 1 (default 0) % At Mix 0 you hear the main input; at 1, the sidechain. Testing the routing Without a host, drive the sidechain from a file with the standalone runner: , independent of the main (see standalone). In a DAW, route another track into the plugin's sidechain bus and watch the SC meter follow it."},{"id":48,"title":"State","section":"Examples","href":"/docs/examples/state","headings":"What it demonstrates · Parameters · State","excerpt":"Hello-world for — the per-instance non-numeric data plumbing. Source: . Guide: chapter 11 → state. What it demonstrates The split between params (numeric atoms — automatable, host knows about them) an","text":"Hello-world for — the per-instance non-numeric data plumbing. Source: . Guide: chapter 11 → state. What it demonstrates The split between params (numeric atoms — automatable, host knows about them) and state (anything else — strings, file paths, view modes, nested structs — the framework round-trips bytes on / but the host never sees the shape). on a -bound struct. A custom egui editor that reads + writes the instance label. An audio-thread param ( ) sitting alongside the state-only label. Parameters Name Type Description ------ ------ ------------- Active Pass-through enable toggle State Field Type Description ------- ------ ------------- User-entered plugin instance label"},{"id":49,"title":"Stereo Utility","section":"Examples","href":"/docs/examples/stereo-utility","headings":"What it demonstrates · Parameters","excerpt":"Stereo utility with independent gain, polarity invert, and up to 50 ms of delay per side. Its real subject is parameter-group reuse: one struct drives both channels. Source: . What it demonstrates par","text":"Stereo utility with independent gain, polarity invert, and up to 50 ms of delay per side. Its real subject is parameter-group reuse: one struct drives both channels. Source: . What it demonstrates param groups: is declared once and pulled in twice; bare rebases each slot to its own id range derived from the field name, so left and right resolve to distinct controls with no hand-written ids Addressing reused-group params in the editor by flattened id ( ) instead of the derived enum Built-in editor with two groups mixing and widgets Per-channel delay lines sized in from the sample rate, with block-rate delay/polarity reads and per-sample smoothed gain ( , ) The broad assertion suite: param id uniqueness, normalized round-trips, AU metadata, state resilience, editor lifecycle, and screenshot baselines Parameters Name Range Unit ------ ------- ------ Left Gain -60 to +12 (default 0) dB Left Invert off/on toggle Left Delay 0 to 50 (default 0) ms Right Gain -60 to +12 (default 0) dB Right Invert off/on toggle Right Delay 0 to 50 (default 0) ms"},{"id":50,"title":"Synth","section":"Examples","href":"/docs/examples/synth","headings":"What it demonstrates · Parameters · Files","excerpt":"16-voice polyphonic synthesizer with oscillator, filter, and ADSR envelope. Source: . What it demonstrates Instrument plugin with MIDI input (stereo output, no audio input) Sample-accurate MIDI event ","text":"16-voice polyphonic synthesizer with oscillator, filter, and ADSR envelope. Source: . What it demonstrates Instrument plugin with MIDI input (stereo output, no audio input) Sample-accurate MIDI event handling ( / ) Per-voice ADSR envelope and one-pole low-pass filter Enum parameter ( ) with dropdown widget Voice stealing (oldest-first) and tail mode Section-based grid layout ( , ) audio path via — returns , is the defaulted alias for , and the format wrapper widens host → plugin at the block boundary (voice phase accumulators want the precision headroom over long-running sessions). Parameters Name Range Unit Description ------ ------- ------ ------------- Waveform Sine/Saw/Square/Triangle -- Oscillator shape Volume -60 to 0 dB Master output level Cutoff 20 to 20000 Hz Low-pass filter cutoff Resonance 0 to 1 -- Filter resonance Attack 0.001 to 5 s Envelope attack time Decay 0.001 to 5 s Envelope decay time Sustain 0 to 1 -- Envelope sustain level Release 0.01 to 10 s Envelope release time Files — plugin logic, MIDI dispatch, GUI layout — , ,"},{"id":51,"title":"SysEx Echo","section":"Examples","href":"/docs/examples/sysex-echo","headings":"What it demonstrates · Parameters","excerpt":"MIDI effect that echoes every received SysEx message back to the host unchanged, exercising the plugin-to-host SysEx output path and per-format framing. Source: . What it demonstrates Audio-less MIDI ","text":"MIDI effect that echoes every received SysEx message back to the host unchanged, exercising the plugin-to-host SysEx output path and per-format framing. Source: . What it demonstrates Audio-less MIDI effect ( with no audio I/O; in ) Matching and reading variable-length payloads with Emitting SysEx via with , dropping (never truncating) on a full byte pool MIDI 2.0 output ( ) so the echo exercises SysEx-over-UMP on AU v3 while input stays MIDI 1.0 -based integration test proving SysEx survives a mid-block sub-block split from a Transport event Parameters Name Range Unit Description ------ ------- ------ ------------- Enabled off/on -- When off, incoming SysEx is not echoed"},{"id":52,"title":"Transpose","section":"Examples","href":"/docs/examples/transpose","headings":"What it demonstrates · Parameters","excerpt":"MIDI note transposer with semitone and octave controls. Source: . What it demonstrates MIDI effect plugin (processes note events, no audio processing) Discrete integer parameters ( ) Active note map t","text":"MIDI note transposer with semitone and octave controls. Source: . What it demonstrates MIDI effect plugin (processes note events, no audio processing) Discrete integer parameters ( ) Active note map to prevent stuck notes when transposition changes Output event generation via Parameters Name Range Unit Description ------ ------- ------ ------------- Semitones -12 to 12 st Transpose by semitones Octave -3 to 3 -- Transpose by octaves"},{"id":53,"title":"Tremolo","section":"Examples","href":"/docs/examples/tremolo","headings":"What it demonstrates · Parameters","excerpt":"Beat-synced amplitude LFO. Demonstrates the host-transport feature end-to-end: the DSP reads to lock the LFO phase to the host's beat grid, and the editor reads to show the current tempo / play state ","text":"Beat-synced amplitude LFO. Demonstrates the host-transport feature end-to-end: the DSP reads to lock the LFO phase to the host's beat grid, and the editor reads to show the current tempo / play state / beat position live in the UI. When the host does not report transport (standalone, hosts that don't expose it), the DSP falls back to a free-running 2 Hz LFO so the effect stays audible, and the readout shows placeholders. Source: . What it demonstrates Reading tempo + beat position on the audio thread from . Reading transport on the editor thread from . with a dropdown widget. Graceful fallback when the host does not populate transport. Parameters Name Range Description ------- ------------------------------------ --------------------------------- Depth 0 to 1 LFO → amplitude modulation depth Rate 1/1, 1/2, 1/4, 1/8, 1/16, 1/32 LFO cycle length in note values Shape Sine / Triangle / Square LFO waveform"},{"id":54,"title":"AAX","section":"Formats","href":"/docs/formats/aax","headings":"Status · Enable · Requirements · 1. Avid AAX SDK · 2. A compiler · 3. (Retail only) PACE wraptool + iLok signing account · Install paths · Signing · Build / install / package · Validate · Pro Tools error -7054 · truce.toml fields · Hosts · Gotchas","excerpt":"AAX (Avid Audio eXtension) is the only plugin format Pro Tools loads. Shipping into pro-audio studios means shipping AAX. The format is proprietary to Avid, requires a licensed SDK, and — for retail d","text":"AAX (Avid Audio eXtension) is the only plugin format Pro Tools loads. Shipping into pro-audio studios means shipping AAX. The format is proprietary to Avid, requires a licensed SDK, and — for retail distribution — PACE/iLok signing via . Status Opt-in. macOS and Windows; no Linux support (Avid doesn't ship an AAX SDK for Linux). Truce's AAX support has been tested in Pro Tools Developer Build; retail Pro Tools requires PACE signing. Enable Requirements Avid AAX SDK Obtain the AAX SDK from [developer.avid.com]. Requires a free developer account. Unpack to a path of your choice — common conventions are on macOS / Linux or on Windows. [developer.avid.com]: https://developer.avid.com/ You don't need to pre-build the SDK's yourself. invokes the SDK's own cmake on first use into a sibling directory, with the right for whatever you're building (host-only for , universal for ). The build is cached — subsequent runs are a no-op. Tell where the SDK lives via the env var: Or export it: A compiler macOS: Xcode CLI tools. Windows: Visual Studio 2019+ with the \"Desktop development with C++\" workload. CMake and Ninja for the AAX SDK's own build. (Retail only) PACE wraptool + iLok signing account Pro Tools retail refuses to load AAX plugins that aren't signed by PACE. For developer testing you can use Pro Tools Developer (free from Avid with a developer license on your iLok) and skip PACE signing entirely. For retail signing: on / . Environment variables for your PACE account (read at time): If any of these are missing, prints a warning, skips PACE signing, and includes the unsigned AAX in the installer. Pro Tools Developer still loads it; retail Pro Tools doesn't. Install paths AAX is always system-only — Pro Tools' loader scans only the system root on every platform. falls back to system silently with a one-line note. Platform Path (system-wide, sudo / admin required) ---------- ----------------------------------------------- macOS Windows Like VST3, is a bundle directory ( on macOS; on Windows). Signing Three layers, in order: Codesign / Authenticode. codesigns the macOS bundle with . Windows binaries are Authenticode-signed at time via . PACE wrap. Runs before Authenticode on the binary when / are set and is on PATH. PACE wraps the binary; Authenticode signs the wrapped result. Installer signing. On macOS the is Developer ID Installer-signed and optionally notarized; on Windows the is Authenticode-signed. Build / install / package Note: AAX is not in the scaffold default feature set. You must either add it to or pass explicitly every time. Validate No first-party AAX validator is wired into . Verify manually by opening Pro Tools Developer and loading the plugin. Pro Tools error The retail Pro Tools equivalent of \"plugin unsigned\" is error code . If you hit this, the plugin isn't PACE-signed. Either: Use Pro Tools Developer with a developer iLok license (free for testing); or Set up a paid PACE signing account and re-package with / set. truce.toml fields Valid values: , , , , , , , , , , , , , , . Hosts Host Platform Status ------ ---------- -------- Pro Tools Developer macOS / Windows working — loads unsigned AAX Pro Tools (retail) macOS / Windows requires PACE signing Pro Tools First — no longer supported by Avid Any non-Avid host — AAX is Pro Tools only Gotchas Linux is unsupported. Avid doesn't ship an AAX SDK for Linux. on Linux fails with a clear error before building. must point to the SDK root, the directory that contains , , . Pointing at a subdirectory fails with an unhelpful build error from the AAX SDK's own CMake. Windows AAX is host-arch only. The AAX SDK doesn't support cross-compiled universal binaries on Windows — builds for the host architecture only. macOS produces universal (arm64 + x8664) bundles normally. PACE signing is not optional for retail. Pro Tools \"real\" (the paid user-facing product) refuses unsigned plug-ins. Budget the time/cost of a PACE account before committing to a retail release. System-wide install re"},{"id":55,"title":"Audio Unit v3 (iOS / iPadOS)","section":"Formats","href":"/docs/formats/au-ios","headings":"Enable · Requirements · Per-developer credentials · Per-plugin truce.toml fields · Container app · Install · Simulator (most iteration happens here) · Device · Package · xcframework redistribution · Identifiers · Hosts · Gotchas · iOS screenshot regression · Programmatic regression test","excerpt":"iOS only hosts AU v3 plugins, delivered as an App Extension inside a container . Logic Pro for iPad, GarageBand, AUM, Cubasis, BeatMaker, Loopy Pro all consume this shape. Every other format truce sup","text":"iOS only hosts AU v3 plugins, delivered as an App Extension inside a container . Logic Pro for iPad, GarageBand, AUM, Cubasis, BeatMaker, Loopy Pro all consume this shape. Every other format truce supports — CLAP, VST3, VST2, LV2, AAX, standalone host — is unviable on iOS by platform contract. drives the same Rust plugin code your macOS / Windows / Linux build produces — same DSP, same params, same editor — through the iOS Simulator. Add for a tethered iPhone / iPad. The build flow is identical apart from signing identity. Enable Same feature flag as macOS. The framework's crate gates iOS-specific entry points via — no extra opt-in. Requirements Path Toolchain Signing ------ ----------- --------- Simulator Xcode CLI tools + a booted iOS Simulator Ad-hoc ( ) works Device Full Xcode + paired & trusted iPhone / iPad Apple Development identity + Full Xcode Apple Distribution identity + (App Store / TestFlight) must point at a real . The CLI tools alone don't carry the iPhoneOS / iPhoneSimulator SDKs. Per-developer credentials Put these in (gitignored): Simulator installs ignore all three and ad-hoc sign with . Per-plugin truce.toml fields Container app wraps the in a container built from a Swift template truce ships at install time ( ). The container handles AU discovery, hosts the embedded editor, shows a Play button that drives from mic input (effects) or a test note (instruments / MIDI processors), and switches between portrait chrome and a landscape hamburger sidebar. Custom container apps are not yet supported. Plug-ins that need a bespoke shell (custom branding, multi-screen flows, App Store-grade marketing UI) currently hand-author one against the framework's C ABI, build it outside , and load the same artifact. Wiring the install pipeline to accept a user-supplied container template is on the roadmap. Install Simulator (most iteration happens here) The container app discovers the AU through , instantiates it, and hosts its editor in-process. runs end-to-end (100 ms silent buffer by default — pass at launch for live audio). Device Needs on ( ) or Xcode 15+'s . Pair + trust the device first ( to verify). Package Produces , signed with the identity and the embedded . Upload through / Transporter to TestFlight or the App Store. xcframework redistribution Produces containing both device + simulator slices. Useful if you're shipping the framework as a building block for someone else's Xcode project; the path embeds the framework directly and doesn't need this artifact. Identifiers iOS uses the same AU manufacturer / subtype codes as macOS — they're in the AU spec, not the platform: Hosts Host Status ------ -------- GarageBand for iPad ✅ Logic Pro for iPad ✅ AUM ✅ Cubasis ✅ BeatMaker 3 ✅ Loopy Pro ✅ Gotchas No hot reload on iOS. mode relies on -ing freshly-built logic dylibs at runtime; iOS App Extensions reject of anything outside the signed bundle's . Shell mode stays macOS-only. Simulator + remote-VC quirks. Under ad-hoc signing, the iOS Simulator's remote view-controller bridge doesn't always fire on the appex's . The container app links the framework directly and calls itself — same rendering pipeline as a real AU host, just without the remote-VC indirection. Production hosts use the registered factory; this workaround is simulator-only. MIDI 2.0 forwarding. iOS 17+ / macOS 14+ AU hosts deliver MIDI via (UMPs) rather than the legacy 3-byte . Truce decodes both. MIDI 2.0 channel-voice messages (per-note expression, 32-bit resolution) land as the appropriate variant; MIDI 1.0 UMPs are downconverted to the legacy shapes. Single-pointer touch routing. Multi-finger touches don't hijack an in-progress drag — the editor pins the gesture to the that started it and ignores other simultaneous touches. True multi-widget multi-touch (two knobs grabbed at once) is a framework-wide change queued for follow-up. App Store review. The container app must present a meaningful UI; Apple rejects \"stub\" container apps. Truce's default container s"},{"id":56,"title":"Audio Unit (AU v2 and AU v3)","section":"Formats","href":"/docs/formats/au","headings":"Status · AU v3 and the standalone are one app · Enable · Requirements · AU v3 signing · Install paths · Identifiers · Build / install / package · Validate · Logs · Hosts · Gotchas","excerpt":"Audio Unit is Apple's native plugin format on macOS. It's the only format Logic Pro and GarageBand accept, so shipping into the Apple pro-audio market means shipping AU. Truce supports both AU v2 (the","text":"Audio Unit is Apple's native plugin format on macOS. It's the only format Logic Pro and GarageBand accept, so shipping into the Apple pro-audio market means shipping AU. Truce supports both AU v2 (the legacy bundle loaded in-process) and AU v3 (the sandboxed App Extension loaded out-of-process). Status Opt-in. Tested in Logic Pro, GarageBand, Ableton Live, and Reaper. Both AU v2 and AU v3 ship from the same feature flag and the same Rust staticlib. What differs is only the bundle layout and the host integration path. AU v3 and the standalone are one app On macOS an AU v3 appex can't ship on its own — Apple requires it inside a container . Truce makes that container do double duty: if your plugin ships a standalone binary, the container is the standalone host. and produce a single that is both the AU v3 bundle the DAW loads and the playable app you can double-click to run — there is no second standalone app, and the Standalone format collapses into this one. The installer reflects it too: the pair shows up as a single \"AU3 + Standalone\" choice rather than two. A plugin with no standalone binary still ships its appex inside a minimal, informational stub app. Enable Requirements Format Toolchain Signing -------- ----------- --------- AU v2 Xcode CLI tools ( ) (ad-hoc works locally) AU v3 full Xcode ( required) Developer ID Application — ad-hoc signing is rejected AU v3 requires full Xcode because generates an Xcode project and drives to build the Swift appex into a container app. The CLI tools alone don't include . AU v3 signing The AU v3 appex must be signed with a Developer ID (or development) identity that Apple's will accept. Ad-hoc signing ( ) is rejected at registration. Set: Or export it: If is or unset when you try to install AU v3, the install bails with a clear error. AU v2 falls back to ad-hoc and works with . Install paths Format User (default for AU v2) System ( ) -------- -------------------------- --------------------- AU v2 (sudo) AU v3 system† (sudo) = falls back to system silently. registration of is unreliable on macOS ≤ 13, so AU v3 is treated as system-only. lands at with a one-line note. The AU v3 appex is embedded inside a container at . Apple requires this — App Extensions cannot ship on their own. When your plugin has a standalone binary that container app is the playable standalone (double-click to run); otherwise it's a minimal stub. See AU v3 and the standalone are one app. After an AU v3 install, (the PluginKit daemon) picks up the new extension. If a host doesn't see it, run to flush caches and restart the daemon for a clean re-scan. Identifiers From : AU v3 can use a different subtype from AU v2 ( ). This matters if you want both versions installed at once without hosts colliding on the component ID — Logic is particularly strict about this. Build / install / package Both and produce complete, signed bundles in . is a pure copy + register step — the xcodebuild / framework-assembly / inside-out codesign work all happens at build time, no sudo or write needed to produce a bundle. Validate runs : (Type code from ; subtype and manufacturer from .) AU v3 is validated by walking and running against each. Logs AU v3 runs in a separate appex process. output from the extension doesn't land in the DAW's log — it goes to unified logging. Stream it live with: Useful when the AU v3 GUI is black, the appex crashes on load, or parameters don't sync. Hosts Host AU v2 AU v3 ------ ------- ------- Logic Pro ✅ ✅ GarageBand ✅ ✅ Ableton Live ✅ ✅ Reaper ✅ ✅ Pro Tools — (uses AAX) — Gotchas is mandatory for AU v3. Full Xcode, not just CLI tools. must point at a real ; if it points at , v3 installs fail. Run . AU v3 ad-hoc signing is rejected. The install path refuses and prints a clear message. AU v2 is lenient and works ad-hoc. AU v3 always installs to (system-wide, needs sudo). falls back with a one-line note. AU v2 honors (default) and writes to with no sudo; pass for the system path. Host caches are sticky. If a plugi"},{"id":57,"title":"CLAP","section":"Formats","href":"/docs/formats/clap","headings":"Status · Enable · Requirements · Install paths · Signing · Build / install / package · Validate · Hosts · Double precision · Gotchas","excerpt":"CLAP (CLever Audio Plug-in) is the open plugin standard from Bitwig and u-he, licensed under MIT. truce's CLAP wrapper is native Rust (no C++ shim) and supports parameter modulation. Status Production","text":"CLAP (CLever Audio Plug-in) is the open plugin standard from Bitwig and u-he, licensed under MIT. truce's CLAP wrapper is native Rust (no C++ shim) and supports parameter modulation. Status Production. Shipped in the scaffold defaults. Tested in Reaper on macOS, Windows, and Linux. Enable Already on in scaffolded plugins. Otherwise: Requirements Nothing beyond a working Rust toolchain. No SDK, no env vars, no external signing for user-scope installs. Install paths User-scope by default on every platform — no admin / sudo needed. Pass to for the system-wide path (sudo on macOS, Administrator on Windows). Platform User (default) System ( ) ---------- ---------------- --------------------- macOS (sudo) Windows (admin) Linux same (Linux is user-only) On Linux and Windows the bundle is just the built cdylib renamed with a extension — no hierarchy, no , no resources. On macOS the is a proper loadable bundle directory ( + ), which is what Apple's bundle conventions require and what strict CLAP hosts (Bitwig Studio in particular) look for. Signing macOS: codesigns with (ad-hoc by default). CLAP doesn't require Developer ID for local installs. Windows: no signing for ; wraps the bundle in an Authenticode-signed installer. Linux: no signing. Build / install / package Validate runs [clap-validator] in-process if it's on your , or set to point at the binary. The validator exercises / / / / / lifecycles, parameter queries, state round-trips, and buffer polarity. [clap-validator]: https://github.com/free-audio/clap-validator Hosts Host Status ------ -------- Reaper (macOS / Windows / Linux) primary testbed Bitwig Studio tested on macOS (loads the bundle-layout directly) MultitrackStudio should work; validation pending Double precision plugins flag their audio ports (and ); a host that takes the offer delivers buffers the plugin processes directly - no conversion at the boundary. The wire precision is per-port and per-activation, and the f32 path stays as the fallback. plugins are unaffected. Gotchas Parameter modulation ( events) is CLAP-specific. Plugins that consume will see modulation in CLAP hosts and nothing from VST3/AU/AAX hosts (those formats don't expose CLAP-style per-voice modulation). from (or auto-derived from vendor + plugin ) is what CLAP hosts use to identify your plugin. Do not change it after release — automation and preset associations are keyed on it."},{"id":58,"title":"LV2","section":"Formats","href":"/docs/formats/lv2","headings":"Status · Enable · Requirements · Install paths · Plugin URI · Supported extensions · Signing · Build / install / package · Validate · Hosts · Gotchas","excerpt":"LV2 is the open plugin standard that dominates Linux-native DAWs (Ardour, Qtractor, Carla, Zrythm, Jalv) and is also supported by Reaper and Bitwig on other platforms. Truce ships hand-rolled C bindin","text":"LV2 is the open plugin standard that dominates Linux-native DAWs (Ardour, Qtractor, Carla, Zrythm, Jalv) and is also supported by Reaper and Bitwig on other platforms. Truce ships hand-rolled C bindings ( , no dependency), so the crate itself has no runtime dependencies beyond libc. Status Opt-in. Working on macOS, Windows, and Linux. Tested in Reaper; other LV2 hosts (Ardour, Carla, Jalv, Zrythm) not yet validated but expected to work. Enable Opt-in. Either add to in , or pass . Requirements Nothing beyond the usual platform toolchain. The shim is pure Rust; no SDK, no env vars, no external signing. Install paths User-scope by default; pass for the system-wide path. Platform User (default) System ( ) ---------- ---------------- --------------------- Linux same (Linux is user-only) macOS (sudo) Windows (admin) is the plugin name lowercased with hyphens (e.g. \"Truce Gain\" → ). The slug lives in the bundle directory name, the / / filename, and the Turtle IRI references in . The slugging avoids the need for percent-encoding in TTL IRIs and keeps strict LV2 hosts happy. Bundle contents: The TTL files are generated at install time by calling the plugin's FFI entry point. They're regenerated on every install and don't need to be committed. Plugin URI The plugin URI is derived from + + : Many LV2 hosts (lilv's reference loader, Reaper) require an HTTP URI. Don't change or after release — the URI is the LV2 host's identity for the plugin. Supported extensions — audio in/out. with — MIDI input/output as LV2 Atom messages. — preset save/restore via truce's / . UI types: Linux: (GUI hosted directly on the parent X11 window ID). macOS: (GUI hosted on an ). Windows: (GUI hosted on an ). Signing None. LV2 has no signing requirements on any platform. still emits the bundle as part of the platform installer. Build / install / package Validate No first-party LV2 validator is wired into yet. For manual checking, Carla or Jalv will print any TTL parse errors when they try to load the bundle: Hosts Host Platform Status ------ ---------- -------- Reaper Linux / macOS / Windows primary testbed Ardour Linux should work; not yet validated Qtractor Linux should work; not yet validated Carla (plugin host) Linux should work; not yet validated Jalv (CLI host) Linux should work; not yet validated Zrythm Linux should work; not yet validated Bitwig Studio any LV2 support is nominal; prefer CLAP there Gotchas Plugin URI is immutable. Once a plugin ships, the URI is the handle by which hosts refer to it in saved sessions. Changing or will break every saved session using your plugin. Choose them carefully. X11UI = parent window ID. truce's X11UI reports the plugin's parent window as its own widget (rather than creating a distinct child). Works in Reaper, expected to work in Ardour / Jalv; stricter hosts may object and require a follow-up. Flag bugs against . No MIDI 2.0. LV2 Atom carries MIDI 1.0 byte streams. Plugins exposing MIDI 2.0 variants only see the MIDI 1.0 mapping when loaded as LV2. Editor-scheduled background tasks run on a separate instance. LV2 loads the UI as its own module, so truce backs the editor with a distinct plugin instance from the DSP. A background task scheduled from the editor therefore runs against that UI instance's params - fine for preparing data the same GUI reads, but it can't hand results to the audio thread's state the way the single-instance formats (CLAP, VST3, AU) can. Tasks scheduled from are unaffected; they run on the DSP instance as normal. Bundle slugs are case-insensitive. , , and all produce the same slug . If two plugins in the same project slug to the same name they will overwrite each other — differentiate by editing / in ."},{"id":59,"title":"Standalone","section":"Formats","href":"/docs/formats/standalone","headings":"Status · Enable · Run · CLI flags · playback feature: WAV in / WAV out · Presets · In-window hotkeys · Settings precedence · MIDI · Transport · Integration tests · Distribution · Limitations · See also","excerpt":"A standalone binary mode of your plugin: drives the DSP against your OS audio driver directly and opens your plugin's editor in its own window. Not a plugin format a DAW loads — a way to run the same ","text":"A standalone binary mode of your plugin: drives the DSP against your OS audio driver directly and opens your plugin's editor in its own window. Not a plugin format a DAW loads — a way to run the same crate as an app. Status Shipped on all three platforms. Every documented example ships a standalone binary. Platform Audio backend GUI ---------- --------------- ----- macOS CoreAudio (via cpal) ✅ Windows WASAPI (via cpal) ✅ Linux ALSA or PipeWire-JACK (via cpal) ✅ (X11; Wayland via XWayland) Enable Plugins scaffolded with (or ) ship the standalone host enabled out of the box — lists in and is pre-written. If you opted out at scaffold time with , or you're adding standalone to an existing crate, do the two mechanical additions below. No edits. : is your crate's — the convention (e.g. → ) is what the scaffolder emits. reads your and picks the entry whose contains , so the actual name can be anything; the convention just keeps the cargo-built path predictable. : Run handles feature + bin selection and stages the binary into alongside every other truce-produced artifact. CLI flags When the plugin declares more than one bus layout, a Settings > Bus Layout menu switches between them live. The host runs the exact of the selected layout while keeping the audio device at a width the hardware supports, so a mono or asymmetric (mono-in / stereo-out) layout plays through a stereo-only device instead of failing to open. feature: WAV in / WAV out The optional feature on adds three flags for piping files in and out of the plugin without an audio interface — useful for snapshot regression tests, batch rendering, and CI runs on headless build agents. Enable from your plugin crate with: …then build the binary with . Common shapes: In offline mode the runner inherits the input WAV's sample rate and channel count by default (override with if needed); output WAV is always 32-bit float at the resolved SR. Mute ( ) silences the speakers but does not affect what captures — bounce-to-disk behavior matches what every DAW does. Presets The standalone host browses and saves your plugin's preset library through a native Presets menu (macOS and Windows): Load — pick a preset. The list also shows your own user presets and any installed packs, deduped by identity. Previous / Next — step through the list to audition sounds. Save — update the preset you're editing in place. With a factory preset loaded (or nothing) there's nothing of yours to overwrite, so Save is grayed and points you at Save As — you can't clobber a stock sound by accident. The menu label shows the file you'd write, e.g. . Save As… — name a new preset. macOS / Windows open a native save panel (which confirms overwrites); the new preset is selected once saved. Presets you save show up in the menu the next time you open it — no relaunch — and land in the same per-OS user location your DAW reads, so a sound you dial in here is immediately available in your projects. Keyboard shortcuts work on every platform, and are the whole preset interface on Linux (the X11 standalone has no menu bar): Cmd-S / Ctrl-S to Save, Cmd-Shift-S / Ctrl-Shift-S to Save As. You can also load a sound straight from the command line with , or list what's available with . See the presets guide for authoring libraries. In-window hotkeys SPACE — toggle transport play / stop Cmd-S / Ctrl-S — save the current preset; Cmd-Shift-S / Ctrl-Shift-S — Save As (see Presets above) I — toggle mic input (effect plugins only). First press on macOS triggers the system permission prompt. Cmd-O / Ctrl-O — toggle audio output (mute / unmute). Plugin keeps processing — meters, transport, MIDI all still tick — only the speaker output is zeroed. Z / X — shift QWERTY-MIDI octave down / up A S D F G H J K L ; — white keys (C D E F G A B C D E) W E T Y U O P — black keys The QWERTY-to-MIDI mapping is keyed on physical key positions, so AZERTY / Dvorak / etc. map to the same piano keys. Settings precedence Each setting resolves first-match-wins: CLI "},{"id":60,"title":"VST2","section":"Formats","href":"/docs/formats/vst2","headings":"Status · ⚠️ Licensing · Enable · Requirements · Install paths · Signing · Build / install / package · Validate · Hosts · Double precision · Gotchas","excerpt":"VST2 is the legacy Steinberg plugin format. It's been superseded by VST3 but is still the format of choice for older Reaper projects, Ableton Live 10 and earlier, and FL Studio's \"prefer VST2\" setting","text":"VST2 is the legacy Steinberg plugin format. It's been superseded by VST3 but is still the format of choice for older Reaper projects, Ableton Live 10 and earlier, and FL Studio's \"prefer VST2\" setting. Truce ships clean-room C bindings (no Steinberg SDK headers) so the crate itself is Apache-2.0 licensed. Status Opt-in. Working on all three platforms. Tested in Reaper on macOS, Windows, and Linux; Ableton Live 11 on macOS; FL Studio on Windows. ⚠️ Licensing Steinberg deprecated the VST2 SDK in October 2018 and no longer issues new VST2 license agreements. Distributing VST2 plugins may require an existing Steinberg agreement. Truce's clean-room C shim ( ) uses no Steinberg SDK headers and carries no Steinberg copyright, so the crate itself is redistributable under Apache-2.0. What is still licensed by Steinberg: The VST trademark and logo. The VST2 identifier codes you register (if any). Hosting / display of VST2 plug-ins in Steinberg's own products. If you're shipping a VST2 plugin commercially in 2026, consult counsel. For personal use, internal tooling, or open-source plugins targeting hobbyist users the risk is low — but this doc is not legal advice. Enable this feature only if you understand the licensing implications. Enable VST2 is opt-in. Either add it to in or pass to . Requirements A C compiler. Xcode CLI tools (macOS), MSVC (Windows), or GCC/Clang (Linux). The shim is a single file. No Steinberg SDK required. Install paths Platform User (default) System ( ) ---------- ---------------- --------------------- macOS (sudo) Windows system† (admin) Linux same (Linux is user-only) = falls back to system silently. Windows VST2 has no agreed-upon per-user path that every host scans, so lands at (Administrator shell required) and prints a one-line note. Unlike VST3, VST2 has no bundle hierarchy on Windows / Linux — a single shared library file is the plugin. On macOS we wrap the dylib in a bundle with . Signing macOS: codesigned with on install. Windows: Authenticode-signed at packaging time via . Linux: no signing. Build / install / package Validate runs against installed VST2 bundles (strictness 5), same as VST3. Hosts Host Platform Status ------ ---------- -------- Reaper macOS / Windows / Linux working (Linux: plugin installs correctly; Reaper's browser may not surface it — investigation open) Ableton Live 10 / 11 macOS / Windows working FL Studio Windows working Logic Pro — AU only, no VST2 Pro Tools — AAX only, no VST2 Double precision plugins set and wire , so hosts that process in 64-bit hand the plugin buffers directly - no conversion at the boundary. 32-bit hosts and plugins are unaffected. Gotchas Four-char plugin ID: in becomes the VST2 unique ID (cast to ). Must be stable across releases — hosts key project state on it. First character is traditionally uppercase ASCII to stay above the VST2 \"manufacturer-reserved\" cutoff. No MIDI 2.0: VST2 has no UMP pipeline. Truce plugins will only see MIDI 1.0 events when loaded via VST2, even if the plugin's enum supports MIDI 2.0 variants on other formats. No sample-accurate automation: VST2's parameter model is block-rate. If your plugin relies on events landing between audio samples (CLAP, VST3), VST2 will only deliver them at block boundaries. Reaper-Linux browser bug: VST2 bundles install to correctly, but Reaper for Linux may not surface them in its plugin browser. Workaround: symlink into Reaper's custom VST search path."},{"id":61,"title":"VST3","section":"Formats","href":"/docs/formats/vst3","headings":"Status · Enable · Requirements · Install paths · Signing · Build / install / package · Validate · Hosts · Double precision · Gotchas","excerpt":"Steinberg's VST3 is the most widely-supported plugin format in commercial DAWs. Truce implements it through a thin C++ shim ( ) that implements the COM vtables required by the VST3 ABI and forwards ev","text":"Steinberg's VST3 is the most widely-supported plugin format in commercial DAWs. Truce implements it through a thin C++ shim ( ) that implements the COM vtables required by the VST3 ABI and forwards every callback into Rust. Status Production. Shipped in the scaffold defaults. Passes pluginval at strictness level 5. Tested in Reaper, Ableton Live, FL Studio on macOS and Windows, and Reaper on Linux. Enable Already on in scaffolded plugins. Otherwise: Requirements macOS: Xcode CLI tools for the C++ compiler. . Windows: MSVC toolchain (Visual Studio 2019+ with the \"Desktop development with C++\" workload). Linux: GCC or Clang with C++17 support. No Steinberg SDK required — the shim is a clean-room COM implementation under Apache-2.0. Install paths User-scope by default; pass for the system-wide path. Platform User (default) System ( ) ---------- ---------------- --------------------- macOS (sudo) Windows (admin) Linux same (Linux is user-only) The on disk is a real bundle directory with a proper hierarchy: builds the bundle and signs the binary for macOS. The user-scope default writes to your home directory with no elevation; pass to land in the system-wide directory (sudo on macOS, Administrator shell on Windows). Signing macOS: bundles are codesigned with during install. Host loaders on Apple Silicon refuse unsigned VST3 bundles; ad-hoc ( ) is accepted for local use. Windows: binaries aren't signed by ; Authenticode-signs them via before bundling into the Inno Setup installer. Unsigned VST3 on Windows just produces a SmartScreen prompt for end users; DAWs still load it. Linux: no signing. Build / install / package Validate invokes Tracktion [pluginval] if installed ( env var to override path). Strictness 5 exercises: channel layouts, parameter ranges, preset I/O, silent-input behavior, real-time safety heuristics. [pluginval]: https://github.com/Tracktion/pluginval Hosts Host Platform Status ------ ---------- -------- Reaper macOS / Windows / Linux primary Ableton Live macOS / Windows working FL Studio macOS / Windows working Cubase — not yet tested Studio One — not yet tested Double precision plugins advertise ; hosts with a 64-bit processing mode (Reaper, Cubase) then hand the plugin buffers directly - no conversion at the boundary. 32-bit hosts and plugins are unaffected. pluginval reports it as and runs its audio tests in both precisions. Gotchas Class ID ( ) in (auto-derived from vendor + plugin bundleid if not set) must not change after release. VST3 hosts key automation and presets on it. System-scope install needs elevation. macOS: requires . Windows: requires an Administrator shell. The user-scope default (no flag, or ) writes to / and skips elevation — every modern VST3 host scans both roots. IRunLoop on Linux: Reaper doesn't require the VST3 IRunLoop timer integration; Bitwig and Ardour on Linux may. Not yet verified — a known-possible risk for those hosts on Linux."},{"id":62,"title":"Audio testing","section":"Guide","href":"/docs/guide/audio-testing","headings":"Quick start · How it works · Inputs · Scripted MIDI and automation · State files · Captured meters and output events · Assertions · Reading post-run state · Writing captured audio to disk · Catching audio-thread allocations · API surface · What's next","excerpt":"Audio tests catch DSP regressions by running your plugin against scripted input — silence, a constant tone, a buffer you generated, or MIDI events — for a fixed duration, then asserting on the capture","text":"Audio tests catch DSP regressions by running your plugin against scripted input — silence, a constant tone, a buffer you generated, or MIDI events — for a fixed duration, then asserting on the captured output, meters, and emitted events. No DAW, no host, no audio device — the driver instantiates the plugin, calls in a loop, and hands you a . The same engine powers three places: Tests via — adds assertion helpers on top. Custom bins — batch CI renders, demo audio, preset rendering pipelines. 's offline path — parses CLI flags and feeds them to the driver. If you need different lifecycles for each consumer, you'll get silently-different bug surfaces — so they all share one driver. Quick start Add to : Drop a test into your : The macro builds a with wired to your crate's (so resolves against your crate, not wherever was launched). How it works A run is a builder followed by : constructs a with defaults — 44.1 kHz, 2 channels, 512-sample blocks, 1 s, silence input, no script, audio + final-meter capture. Builder methods configure the run: sample rate, duration, input, transport, MIDI / param-automation script, state-file load, param overrides, setup closure, capture spec. does the lifecycle in this order: → → (the shell sets the sample rate and snaps the smoothers). If was supplied, . shortcuts apply via , then . The closure runs (most general escape hatch). Block loop: pull script events that fall inside into an , allocate per-block channel buffers from [ ], call , append the output, advance. Capture meters / output events / param snapshots according to . The carries the captured data plus the post-run plugin instance — read params or custom state from for assertions. Inputs The enum covers the four common shapes: — every channel zero (default). — DC on every channel. — , . Channel count must match the driver's. — for sweeps, noise, derived signals. Instruments ignore — set their notes via . Scripted MIDI and automation advances a sample-accurate cursor; events land at the current offset. is a no-op (events stack). The script's emits a event the plugin sees inline — same delivery path CLAP / VST3 / AU automation lanes use. For automation during a run, use the script: For one-shot params before the run starts, use the builder shortcut: calls compose; multiple are applied in declaration order, before the closure. State files The standalone host's / writes a file — your full session state at that moment. Feed it to the driver via : Path is relative to your crate's directory (the macro wires that for you), or absolute. Loaded via — the same path CLAP / VST3 / AU hosts use to restore session state. State load runs before overrides and the closure, so you can use the state as a baseline and tweak from there: The second arg is a [ ] carrying the resolved channel count, sample rate, and block size: Captured meters and output events By default the driver captures audio + a final meter snapshot (per-meter readings at end-of-run). Opt into per-block snapshots or output-event capture as needed: Meters keyed by their parameter ID; the driver pulls and in the order the params were declared. Assertions covers the standard claims. They all take : Whole-run What it checks --- --- At least one sample > audible threshold Every sample Meter final reading < Plugin emitted exactly events For anything custom, walk directly — the captured audio is plain . Reading post-run state holds the post-run plugin instance, so assertions can inspect param values, meters, or custom fields the framework doesn't know about: Writing captured audio to disk For preset rendering or visual debugging, the driver can write the captured audio out as a 32-bit float WAV. Enable the feature on (already on whenever has ): Same engine the standalone host's offline path uses — your test output and your CI render matrix produce byte-identical results. Catching audio-thread allocations Allocating on the audio thread is the classic cause of dropouts, and a run is the ideal place to c"},{"id":63,"title":"Best practices","section":"Guide","href":"/docs/guide/best-practices","headings":"The audio thread is sacred · Precision: f64 state, f32 wire · Params vs. DSP state · Smooth every audible parameter · Respect event timing · Set up in reset(), not construction · Report latency and tail · Keep bundleid stable · The editor is a function of the params · Save state off the audio thread · Offload heavy work to a worker · Kill denormals in feedback paths · Test and validate before shipping","excerpt":"A distilled checklist for plugins that behave well in every host. Each item links to the chapter that covers it in depth; this page is the short version you can scan before shipping. The audio thread ","text":"A distilled checklist for plugins that behave well in every host. Each item links to the chapter that covers it in depth; this page is the short version you can scan before shipping. The audio thread is sacred runs under a hard deadline set by the host's buffer size. A single missed deadline is an audible click, so the audio thread must never do anything with unbounded or unpredictable latency: no allocation, no , no lock, no syscall, no file or network I/O; no unbounded loop (bound every iteration by the block size or a fixed count); no / logging, no , no in hot code. Everything the audio thread touches is sized up front. Allocate scratch buffers in (it receives the maximum block size) or in construction, and size them to that maximum. The per-instance is already pre-allocated; and refill it, never build a fresh one per block. See processing. Precision: state, wire The host wire is , but single precision drifts over a long session. Keep DSP state - phase accumulators, filter memory, delay-line positions, modulation math - in , and cast to only at the sample-write boundary. An phase counter accumulates error audibly over a multi-hour set. Pick a prelude per file: for , for end-to-end (the framework advertises 64-bit support to hosts that can use it). Don't import two preludes in the same file. See precision. Params vs. DSP state Two kinds of mutable data, two homes: Parameters are automatable, host-visible, and shared across threads. They live in your struct with atomic storage. Read them each block; don't cache their values in your logic struct. DSP state is per-instance, mutated every sample, and touched only by the audio thread. It lives in a plain struct named by your logic's (built by ), not in the params struct. Keeping the two separate is what lets the editor be a pure function of the params (below). See plugin anatomy and state. Smooth every audible parameter A raw parameter jump zippers. Declare (or a suitable time) on any parameter that scales audio - gain, cutoff, mix - and read the smoothed value per sample or per block. Snap smoothers to their targets in so the first block after activation isn't a ramp up from zero. See smoothing and sample-accurate automation. Respect event timing Note and parameter events carry a into the block. Apply them at that offset, not all at the top of the block, or automation and note timing smear by up to a buffer. The framework can split the block at event boundaries for you. See sample-accurate event splitting and MIDI. Set up in , not construction runs once; runs on every activation and whenever the sample rate or block size changes. Put sample-rate-dependent setup there: clearing delay lines, sizing scratch to the new maximum block. (The shell sets the sample rate and snaps the smoothers for you before runs.) just wires up the DSP state's fields. See lifecycle. Report latency and tail If your plugin looks ahead - a lookahead limiter, a linear-phase EQ, an FFT frame - report it from so the host delay-compensates and your plugin stays time-aligned in the mix. If it rings out after input stops - reverb, delay - report so the host doesn't cut the tail when the transport stops. may change at runtime: return a new value when the user flips a lookahead or oversampling mode and the host is notified automatically (CLAP, VST3, AU v2, AU v3, LV2; best-effort on VST2, static on AAX). On CLAP and VST3 a latency change is a restart-class event - the host may briefly deactivate and reactivate the plugin to apply it - so change latency on deliberate mode switches, not continuously per block. Keep stable A plugin's identity (its CLAP / VST3 ids and its state-envelope hash) derives from , not the display name. Pick it once and never change it: renaming the display name is free, but changing orphans every saved session and preset that referenced the old identity. Porting from another framework? Use to keep loading the old blobs. The editor is a function of the params takes only the params by design, "},{"id":64,"title":"Your first plugin","section":"Guide","href":"/docs/guide/first-plugin","headings":"Scaffold · Tour the generated code · 1. Parameters — what the user controls · 2. Plugin logic — what happens to the audio · 3. The export macro — makes it a plugin · Tour the generated config · truce.toml · Cargo.toml features · Build and install · Load in a DAW · Edit and rebuild · What's next","excerpt":"Scaffold, build, install, load, iterate. End state: a stereo gain plugin with a GUI loaded in your DAW. Prerequisites: chapter 1 → install. Scaffold writes a minimal, self-contained project: No . No s","text":"Scaffold, build, install, load, iterate. End state: a stereo gain plugin with a GUI loaded in your DAW. Prerequisites: chapter 1 → install. Scaffold writes a minimal, self-contained project: No . No separate GUI crate. The host is a 4-line call so works out of the box; pass to skip it (drops the file, the bin entry, and the feature/dep). Shipping a suite? Pass to create one Cargo workspace with a shared and one sub-crate per plugin: You get , each with its own (and unless you passed ), plus one with three entries. Every command below works workspace-wide; add (the cargo crate name, e.g. ) to target one plugin. Tour the generated code has three parts. Open it alongside this section. Parameters — what the user controls One line of attributes per parameter. generates a enum with typed variants, and the trait impl. Parameter IDs auto-assign by field order ( , then 1, 2, ...). See chapter 4 → parameters for the full attribute reference. Plugin logic — what happens to the audio A gain keeps no per-instance state between blocks, so implements - the stateless leaf trait. That means no , no , and no argument threaded through : the method is a pure function of params and input. runs every block on the audio thread; returns the GUI on the main thread. Those two are the only required methods - the shell snaps the parameter smoothers for you, so a pure plugin needs no either. A plugin that does keep DSP state (filter memory, a delay line, an oscillator phase) implements instead and names that state as . See chapter 3 → plugin-anatomy. The export macro — makes it a plugin Generates all format entry points (CLAP, VST3, VST2, LV2, AU v2/v3, AAX via Cargo features), state serialization, parameter hosting, and the hot-reload shell. One macro. Default bus layout is stereo and mono (so the effect shows up on both mono and stereo tracks); override for instruments, sidechains, or a stereo-only effect. Tour the generated config is the single source of truth for plugin identity across every format. The macro reads it at compile time so doesn't need any of this in code. Full schema in . Per-developer secrets (signing identity, AAX SDK path, notary credentials) go in (gitignored), not here. features Scaffolded plugins enable CLAP + VST3 + standalone by default. Add more formats to , or opt in per-command ( ). Per-format detail (SDKs, env vars, install paths, signing) is in docs/formats/. Build and install This builds the crate, bundles each enabled format, codesigns on macOS, and drops bundles into your per-user plug-in directories. You should see something like: No / Administrator prompt — user-scope is the default on every platform. Pass to install into the system-wide plug-in directories ( on macOS, on Windows), which prompts for sudo / admin once per run. Defaults to the cargo release profile — installing usually means audio-testing in a DAW, where debug-build DSP can CPU-spike under load. Pass for fast-compile iteration when DSP correctness isn't what you're checking; never ship a bundle. Explicit format selection works too: Install destinations per platform live in docs/formats/README. To stage bundles into without writing to the system plug-in directories — useful for CI, packaging dry-runs, or just inspecting the produced artifact — use : Same defaults as , same opt-in, but never touches host plug-in paths. Load in a DAW Open your DAW (Reaper is a good first test — free trial, loads CLAP / VST3 / VST2 / LV2). Rescan plugins (Reaper: ). Insert My Gain on a track. Play audio; drag the knob. Volume should change. Expected: (Rendered headlessly with — the same path the screenshot regression tests in gui/screenshot-testing use.) Edit and rebuild Add a pan parameter. In : Use it in : Show it in the GUI: Rebuild: Close and reopen the plugin in your DAW. You now have two knobs. What's next Other parameter kinds — boolean, int, enum, groups, meters, custom formatting → chapter 4 → parameters. Non-trivial processing — transport, sample-accurate events, i"},{"id":65,"title":"Integrating fundsp","section":"Guide","href":"/docs/guide/fundsp","headings":"The shared topology · Sharing parameters with the graph · When do you have to rebuild? · Hysteresis on Time · Variant 1: inline rebuild (the simple crate) · Variant 2: background rebuild + lock-free swap (the worker crate) · Scheduling the rebuild · SR-tagging ready graphs · Optimistic lastbuilttimes update · No thread to own · Which one should you ship? · What's next","excerpt":"is a Rust audio DSP library whose operator-overloaded combinators ( , , , , ) read like a small DSP DSL. truce gives you the host plumbing — formats, params, GUI, state — and leaves to you. The two co","text":"is a Rust audio DSP library whose operator-overloaded combinators ( , , , , ) read like a small DSP DSL. truce gives you the host plumbing — formats, params, GUI, state — and leaves to you. The two compose cleanly because fundsp is agnostic about where its runs, and truce is agnostic about what you do inside . This chapter walks through both fundsp reverb examples in the repo. They share the same signal flow, params, and UI. They differ only in how the fundsp graph gets rebuilt when the user moves the Time knob: — rebuilds the graph inline on the audio thread. Easy to read end-to-end; not safe to ship. — rebuilds on a dedicated worker thread and swaps the finished graph in via lock-free queues. stays allocation-free. The shared topology Both crates wire the same stereo plate reverb: a dry path bussed against a wet path of (high-pass → low-pass → ), crossfaded by a mix knob. In fundsp combinator form: A few fundsp-specific things worth flagging up front: SVF inputs are positional, every input is . Stacking instead of compiles fine and feeds the filter cutoff as audio. The reverb FDN downstream then amplifies it well past peak inside a second. There's an regression test in both crates pinned to exactly this trap. is mono. To feed a stereo node, stack two reads of the same so the channel counts line up. is Bus. is \"share input, sum outputs\" — i.e. the classic dry/wet crossfade. Sharing parameters with the graph The fundsp graph is built once and then ticks forever. Params move continuously. The bridge is a cell per knob the graph reads via : In you push the latest smoothed param value into each every frame, just before (params arrive as a argument, DSP state as ): transposes channel-major into 2-in/2-out frames so fundsp's slots in. It's the shorthand for , so the same call runs over any declared bus - a mono source fans into both graph inputs, a stereo bus maps 1:1 - with no per-width branch. The writes are atomic stores, which the audio thread is fine with. When do you have to rebuild? Most fundsp parameters can be plumbed through and live without rebuilding the graph. A few cannot: Sample rate. recomputes filter coefficients top-down through the graph. is called off the audio thread, so this rebuild is free; you just need to remember to call it. Reverb Time (RT60). bakes RT60 into the FDN's feedback gains at construction. There's no setter — changing Time means a fresh , which means a fresh and . That's a heap call. That's the problem. Anything you want to \"live-tweak\" continuously needs a (cutoffs, mix). Anything that's baked into the graph topology needs a rebuild on change (Time, structural choices). Hysteresis on Time The Time param is declared . Two reasons: The graph rebuild is discrete — there's no smoother on the reading side because nothing reads continuously. A smoothed value would crawl across whatever rebuild threshold we pick over 200 ms and trigger a rebuild every block until it settles — audible as an unstable tail. Both crates also impose a 0.05 s threshold so tiny drifts (automation noise, knob jitter) don't fire: Variant 1: inline rebuild (the simple crate) The simple variant just calls directly inside when the threshold trips: does and , both of which can block on the system allocator. That's a real-time-safety violation. On a CoreAudio buffer of 128 frames at 48 kHz you have 2.67 ms; if takes a hard page-fault or contends on a lock, you get an audio dropout — maybe a click, maybe silence for a buffer, depending on the host. The simple variant exists because the integration shape is visible in one file, with no thread-handoff machinery to follow. It is not safe to ship and the crate's docstring says so. Variant 2: background rebuild + lock-free swap (the worker crate) The worker variant moves the rebuild off the audio thread. It schedules the rebuild as a truce background task - the managed pool owns the thread, the wakeup, and the teardown - and hands the finished graph back through a lock-free swap: The re"},{"id":66,"title":"GUI","section":"GUI backends","href":"/docs/guide/gui","headings":"The built-in GUI · Declaring a layout · Resizable and maximizable · Locking the aspect ratio · The six widgets · Spanning cells · Meters · Interaction for free · Rendering and theming · The framework backends · Screenshot tests · What's next","excerpt":"truce ships a built-in GUI designed for audio plugins. You declare a layout — rows of widgets — and the framework draws it, routes input events, and keeps everything in sync with the parameter . Zero ","text":"truce ships a built-in GUI designed for audio plugins. You declare a layout — rows of widgets — and the framework draws it, routes input events, and keeps everything in sync with the parameter . Zero pixel math. The built-in GUI is a starting point: it gets a prototype on screen in a dozen lines and is fine to keep for a simple utility. For a production plugin, ship one of the framework backends - egui, iced, Slint, or Vizia - or bring your own renderer through the raw-window-handle escape hatch. See the framework backends below. The built-in GUI Declaring a layout — each section is either a row or a labeled group. Widgets flow left-to-right; use / to span cells. By default there's no header, resolves to the widest section's widget count, and the cell size is 50 logical points. Override any of those: — adds a header band with a title only. — header band with the right-aligned subtitle slot only. — both slots at once. / / / cover every combination. — force a specific column count (useful to wrap a long row into a grid). — bigger / smaller cells. — both at once. Resizable and maximizable Editors are fixed-size by default. Opt into host-driven resize with , and clamp the range with / : lets the standalone host's window be maximized. It's off by default for a reason: maximizing grows the window to the whole screen, past the editor's , leaving the clamped GUI in an empty margin. Leave it off and the maximize affordance is removed; turn it on only for editors that render correctly at any size. When a window does end up larger than the editor, the GUI is centered and the extra space painted black rather than stretched or pinned to a corner. The same four methods exist on the egui, iced, and slint editor builders, where / are logical points rather than grid cells. Ship vizia plugins fixed-size — don't call there. Locking the aspect ratio The egui, iced, and slint builders can also constrain resizes to a fixed shape: (the default) means free resizing. The ratio is an integer pair rather than a float to sidestep a Cubase aspect-rounding quirk. CLAP, VST3, AU v3, LV2, and the standalone honor the lock; VST2 and AAX silently ignore it. Hosts that resize the embedded window directly without negotiating (Bitwig on X11) still get fitted: the editor renders at the nearest on-ratio size within its bounds and the leftover margin is painted black rather than stretched. The built-in grid has no aspect lock - its resizes snap to whole cells instead - and vizia editors are fixed-size, so the lock is moot there. The six widgets Constructor Widget Typical use ------------- -------- ------------- rotary gain, cutoff, resonance, any linear slider pan, mix, sometimes easier to read than a knob pill on/off , bypass click-to-open list / vertical level meters peak / RMS output 2-axis pad two continuous params on one surface Spanning cells Explicit positions work too: . Meters Declare meters as fields alongside your params (see parameters.md § Meters), push from with , and render them in : The DSP side is atomic and realtime-safe. The GUI reads the latest value every frame. For a single scalar, is all you need. For continuous streaming data - a spectrum analyzer, an oscilloscope - the audio thread taps samples into an , a background worker runs the analysis, and it publishes the result into a field the editor reads. See chapter 7 → workers. Interaction for free Drag on a knob / slider → change the param. Scroll-wheel on a knob → fine-tune. Double-click a knob → reset to default. Click a toggle / dropdown → set / open. Right-click a widget → host context menu (automation, reset, enter value). You don't write any of this. The framework knows the widget kind from the layout and the parameter behavior from the . Rendering and theming The built-in GUI rasterizes on the CPU through tiny-skia by default. Opt into GPU rendering (wgpu → Metal on macOS, DX12 on Windows, Vulkan on Linux) with the feature on . Colors come from a named theme - dark by default, for light"},{"id":67,"title":"GUI Backends","section":"GUI backends","href":"/docs/guide/gui","headings":"Starting out · Shipping a production plugin · If you need full control · Parameter communication · Screenshot testing","excerpt":"truce supports several GUI backends. They all produce the same plugin formats (CLAP, VST3, VST2, LV2, AU, AAX) and share the same parameter system, so switching between them is straightforward. Starti","text":"truce supports several GUI backends. They all produce the same plugin formats (CLAP, VST3, VST2, LV2, AU, AAX) and share the same parameter system, so switching between them is straightforward. Starting out The built-in GUI is the fastest way to get a working editor. You define a layout in code and truce renders the widgets for you — no custom editor code required. That gives you a working GUI with knobs, mouse interaction, automation, and host integration. Treat it as a starting point: good for prototypes and simple utilities. Shipping a production plugin For production plugins, the recommended path is a framework backend - text input, lists, custom drawing, and a look of your own: egui — immediate-mode. Write UI code that runs every frame. Large widget library, lots of third-party crates. Iced — Elm architecture. Define a model, emit messages, update state, render a view. Good for complex state management. Slint — declarative markup. Design your UI in files, wire properties in Rust. Has an IDE live preview. Vizia — retained-mode with reactive data binding and CSS. Per-param keeps widgets in sync. Desktop only (no iOS, no Windows ARM64). All four integrate the same way: return the backend's editor from , finishing with . The rest of your plugin (params, DSP, format export) stays the same. If you need full control For an existing rendering pipeline, web views, or anything else, implement the trait directly. See raw window handle. Parameter communication Every backend hands you a typed for your plugin's (vizia exposes the same surface through ). The same method API works in egui, iced, slint, vizia, and raw-window-handle editors: The gesture protocol tells the DAW that the user is dragging a control, so it records smooth automation. For single-click actions (toggles), handles everything (begin + set + end in one call). also s to , so works directly when you need to peek at parameter metadata. Screenshot testing All backends support headless rendering for visual regression tests. See screenshot testing."},{"id":68,"title":"Built-in GUI","section":"GUI backends","href":"/docs/guide/gui/built-in","headings":"GridLayout::build · Widgets · Sections · Spanning and positioning · Meters · Theming · Interaction · A full example · Moving beyond the built-in GUI","excerpt":"The built-in GUI renders widgets from a layout you define in code. No framework dependency — declare what you want and truce draws it. Build a and return it from with . For a first walkthrough see the","text":"The built-in GUI renders widgets from a layout you define in code. No framework dependency — declare what you want and truce draws it. Build a and return it from with . For a first walkthrough see the GUI chapter. This page is the reference for every option. Signature: Defaults: No header band. Add a title with , a right-aligned subtitle with , or both with . Each slot is independently optional. = the widest section's widget count (extended to fit any explicitly-positioned widget). Override with to force wrapping — e.g. on a 4-widget section produces a 2×2 grid. = (50.0 logical points). Override with . Set both at once with . Widget constructors accept , so both typed enum IDs (recommended — ) and raw values work. Widgets Constructor Widget Default span Typical param type ------------- -------- -------------- -------------------- rotary knob 1×1 , horizontal slider 1×1 pill on/off 1×1 click-to-open list 1×1 , level meter (one bar per ID) 1×1 2D control pad 2×2 two s If you don't specify a widget type, the default is inferred from the parameter type: → toggle, → dropdown, everything else → knob. Sections Group widgets under labeled headers with . Use for the ungrouped rows. auto-resolves to 3 here (the widest section has 3 widgets), so each section renders as one row. Each starts a new row with a header strip. Widgets inside a section flow left-to-right within that section's row. Spanning and positioning Widgets default to 1×1 cells. Override with , , and : is useful when you want a widget anchored (e.g. a tall meter in the corner) while the rest flow freely. Meters Declare meter slots in your params struct with : Meter IDs auto-assign starting at 256 and appear in the generated enum alongside parameters. Push from (realtime-safe atomic write): Draw in the layout: Theming Colors come from a named theme. Dark is the default. Switch themes or override individual colors: Fonts: fontdue rasterization with JetBrains Mono Regular embedded at compile time. No font file on disk, no runtime load. Rendering: tiny-skia CPU rasterization by default. Opt into GPU rendering through wgpu (Metal on macOS, DX12 on Windows, Vulkan on Linux) with the feature on . Interaction The framework handles all of the following automatically — you don't wire any of it by hand: Knob / slider: drag to adjust. Scroll-wheel to fine-tune. Double-click to reset to default. Toggle: click to flip. Dropdown: click to open the popup list, click an option to select. XY pad: drag anywhere on the pad to set both parameters. Right-click: opens the host's context menu (automation, reset, enter value). A full example Moving beyond the built-in GUI The built-in widget set covers the common audio-plugin shape — knobs / sliders / meters / dropdowns. When you need text input fields, lists, tables, analyzer curves, or a specific visual style the theme system can't reach, switch to a framework backend: egui — immediate-mode, fast to prototype. iced — retained-mode, Elm architecture, good for complex custom UIs. slint — declarative markup with data binding. vizia — retained-mode with reactive data binding and CSS; desktop only (no iOS, no Windows ARM64). raw-window-handle — full control: Metal, OpenGL, Skia, anything."},{"id":69,"title":"egui Integration","section":"GUI backends","href":"/docs/guide/gui/egui","headings":"Setup · PluginContext · Widgets · Using raw egui widgets · Theme · Stateful UIs (EditorUi trait) · Custom persistent state · Screenshot testing · Examples","excerpt":"embeds egui into a plugin window via wgpu + baseview. You get the full egui API — , , , , third-party crates — with parameter hosting handled for you. Setup Return an from : The closure is your egui f","text":"embeds egui into a plugin window via wgpu + baseview. You get the full egui API — , , , , third-party crates — with parameter hosting handled for you. Setup Return an from : The closure is your egui frame function — it runs every frame, same as . is the window size in logical points. The you receive is the root UI for the editor window — nest , , etc. with if you want them. is typed for direct access to the plugin's fields ( etc.) inside the closure. PluginContext is the bridge between egui and the DAW's parameter system. It wraps / / into an ergonomic API; IDs use 's generated enum and convert to through : Widgets provides parameter-aware widgets that handle the gesture protocol internally. Use these or roll your own with raw egui widgets. Typical layout: Using raw egui widgets Any egui widget works — just wire the gesture protocol manually: Theme A dark theme is applied by default. Pass any to override it — use egui's built-in light/dark themes, the truce defaults, or your own: You can also call inside your frame function to switch themes at runtime. The truce theme exports color constants for consistency with the built-in GUI widgets: Stateful UIs (EditorUi trait) The closure API works for simple UIs. For state across frames (tabs, caches, animations), implement : has three methods: Method When Use for --- --- --- Editor window opens Initialize , load resources Every frame Draw your UI Preset recall, undo, session load Re-sync cached state All have default no-ops. Only is required. Custom persistent state If your plugin has state beyond parameters ( / ), use to keep the editor in sync: Write state back from the GUI: For the closure API, use : If your plugin only uses fields, skip this section — parameters sync automatically every frame. Screenshot testing See screenshot testing for more. Examples — complete plugin with knobs, XY pad, meter, header, custom font, and screenshot test. truce-analyzer — non-trivial out-of-tree example built on egui: real-time spectrum analyzer with a diff overlay. Shows what scaled-up egui code looks like for a plugin GUI."},{"id":70,"title":"Iced Integration","section":"GUI backends","href":"/docs/guide/gui/iced","headings":"Getting started · Custom UI · The .el() shorthand · Reading and writing parameters · Widgets · Handling custom messages · Custom state · Screenshot testing · Example","excerpt":"Iced is a retained-mode GUI framework following the Elm architecture. wraps it so you can use iced's layout system and canvas widgets inside a plugin window. Getting started Add the dependencies: The ","text":"Iced is a retained-mode GUI framework following the Elm architecture. wraps it so you can use iced's layout system and canvas widgets inside a plugin window. Getting started Add the dependencies: The simplest approach is auto mode — generate a UI from your layout with no custom code: This renders the same widgets as the built-in GUI but through iced's rendering pipeline. Custom UI For full control, implement . Here's a minimal example: Wire it up: A few things to note: is your custom message enum. Use if you don't need custom messages. is required. defaults to a no-op if you don't override it. converts a truce-iced widget into an iced . Import to use it. The shorthand Iced's type system requires explicit conversions when pushing widgets into rows and columns. Without you'd write: With : Import from and call on any widget. Reading and writing parameters is a per-tick read-only snapshot — iced's function can't have side effects, so the cache is what widgets read from. The host bridge (gestures, automation writes) lives on and is passed to : The built-in widgets ( , , , etc.) emit their own variants — the iced runtime forwards those to the host via the underlying , so direct writes are only needed for custom widgets and handling. Widgets All use builder pattern. Call to push into iced layouts. Handling custom messages If your UI has buttons, tabs, or other interactive elements, define messages and handle them in : Parameter messages (from knob drags, toggle clicks) are handled automatically — you never see them in . Custom state If your plugin has persistent state beyond parameters, use in your model. Initialize it as and sync on first update: is called when the DAW restores state (preset recall, undo, session load). It re-reads custom state so the UI stays in sync. To write state from the GUI: You can also access state directly via / on the passed to . If your plugin only uses fields, you don't need any of this — parameter values sync automatically through . Screenshot testing See screenshot testing for more. Example has a complete plugin with custom header, knobs, XY pad, meter, usage, and screenshot test."},{"id":71,"title":"Keyboard input","section":"GUI backends","href":"/docs/guide/gui/keyboard","headings":"Where keys come from · egui · iced · Slint · Vizia · Other backends · See also","excerpt":"Plugin editors can read the keyboard — text fields, shortcuts, a guitar-tab grid, QWERTY-as-MIDI, whatever. Two things have to line up: the editor window has to receive OS key events, and your backend","text":"Plugin editors can read the keyboard — text fields, shortcuts, a guitar-tab grid, QWERTY-as-MIDI, whatever. Two things have to line up: the editor window has to receive OS key events, and your backend has to route them to your code. Where keys come from Standalone owns its window, so it always gets keys. It also ships QWERTY-as-MIDI, a SPACE transport toggle, and Cmd/Ctrl-S preset shortcuts out of the box. Embedded in a DAW, the editor only sees keys when the host hands the plugin's window OS focus — which is host-dependent. Reaper and Bitwig generally grant focus on click; some hosts never do, and most reserve global keys (spacebar / transport) for themselves. Treat embedded-editor keyboard as best-effort, host-matrixed: it works where the host cooperates and is silent where it doesn't. Keys flow in through baseview as soon as the window has focus; clicking the editor is usually enough to take it. egui egui is immediate-mode — read the keyboard each frame from . iced Keys reach iced two ways. Widgets. A focused , or any custom whose matches , receives keys directly — no extra wiring. That covers text fields and a whole-keyboard capture widget. Subscriptions. For shortcuts or capture that isn't tied to a focused widget, implement with or . truce drives the subscription each frame and routes its messages back through your . An iced carries both the layout-resolved logical key and the layout-independent , so shortcuts (logical) and whole-keyboard input (physical, e.g. a tab editor) are both covered. Slint Keys flow into Slint's own input system, so handle them the Slint way in your markup — a focused / receives typed text, and a with a callback captures keys when focused: Vizia Keys flow into Vizia's own reactive event system. A focused receives typed text automatically; for shortcuts or a key mirror, catch in a (it propagates up to the root even while a textbox holds focus): Other backends The built-in GUI editor doesn't route keyboard to your code yet. Reach for egui, iced, Slint, or Vizia when the editor needs keys. (The native window may still receive them; truce just doesn't surface them to the built-in editor.) See also egui integration · iced integration · Slint integration · Vizia integration Standalone — its in-window hotkeys and QWERTY-MIDI mapping"},{"id":72,"title":"Raw Window Handle (Bring Your Own Renderer)","section":"GUI backends","href":"/docs/guide/gui/raw-window-handle","headings":"When to reach for this · Implementing the Editor trait · Reading and writing parameters · Connecting to your plugin · Useful helpers · Scale factor · baseview for windowing · Reference implementations","excerpt":"If none of the built-in backends fit, you can implement the trait directly. The host gives you a window handle — what you draw inside it is entirely up to you. When to reach for this This is the lowes","text":"If none of the built-in backends fit, you can implement the trait directly. The host gives you a window handle — what you draw inside it is entirely up to you. When to reach for this This is the lowest level of GUI integration. You probably want it if: You have an existing rendering pipeline (Metal, OpenGL, Skia) You want a web view (CEF, wry) You need a framework truce doesn't wrap yet You want absolute pixel-level control For most plugins, start with the built-in GUI or one of the framework integrations (egui, iced, slint, vizia). Implementing the Editor trait Here's the minimum implementation. The host calls with a parent window handle, and you create your UI as a child of that window: Reading and writing parameters exposes its host bridge as methods. IDs use 's generated enum and convert to through , so you can pass either the enum variant or a literal : Always wrap drag gestures in / so the host records automation correctly — is the convenience wrapper for one-shot edits where the gesture and value arrive together. Connecting to your plugin Same as every other backend: Works with all formats (CLAP, VST3, VST2, LV2, AU, AAX) without any format-specific code. Useful helpers Scale factor Query the display scale on macOS (2.0 on Retina, 1.0 otherwise): baseview for windowing If you want cross-platform window management without building it yourself, use baseview directly. It handles macOS/Windows/Linux child window creation, event dispatch, and DPI scaling: See the truce-egui and truce-slint source code for complete baseview integration examples. Reference implementations The existing backends are good examples of real implementations: Backend Source Approach --------- -------- ---------- Built-in (CPU) baseview + tiny-skia CPU raster + wgpu blit Built-in (GPU) baseview + wgpu (via truce-gpu) egui baseview + egui-wgpu Iced baseview + iced-wgpu Slint baseview + software renderer + wgpu blit Vizia baseview + vizia (skia + GL); desktop only"},{"id":73,"title":"Screenshot Testing","section":"GUI backends","href":"/docs/guide/gui/screenshot-testing","headings":"Quick start · How it works · State-dependent screenshots · Promoting a render · Tolerance · Cross-OS rendering · API reference · screenshot! macro · ScreenshotTest<P> builder · Editor::screenshot trait method · cargo truce screenshot · Texture format gotchas","excerpt":"Screenshot tests catch visual regressions by rendering your GUI to an image and comparing it against a committed reference. If something changes unexpectedly — a widget moves, a color shifts, a label ","text":"Screenshot tests catch visual regressions by rendering your GUI to an image and comparing it against a committed reference. If something changes unexpectedly — a widget moves, a color shifts, a label disappears — the test fails and points at the freshly-rendered PNG so you can compare visually. The test API is a builder constructed by the macro, which takes the plugin type and the explicit reference-PNG path: The path is resolved relative to your crate's directory (or used as-is if absolute). The macro never picks a path on your behalf — every test names its own reference, in whatever directory layout suits the project. Quick start Add to : Drop the test into your (or wherever your lives): If the reference doesn't exist, the test fails and points at the CLI command that creates one: The standard flow: is decoupled from the test surface — it works on any crate built with whether or not the codebase has a test. How it works constructs a with the reference path resolved against the calling crate's (when relative) or used as-is (when absolute). calls (plus and any closure you supplied), then asks the editor for via . The current render is always written to — gitignored, so it never accidentally gets committed. The committed reference is loaded from the path you passed. If the reference doesn't exist, the test panics with a hint. If the reference exists and the diff exceeds tolerance, the test fails with both PNG paths and the exact command to accept the new render as the new baseline. References save at 2× resolution with 144 DPI metadata so they look right on GitHub and in image viewers. State-dependent screenshots The default test renders against output (a fresh plugin at default params). For shots that need a specific configuration — a knob at a particular value, a panel toggled open, a meter reading a particular level — the builder's closure runs after and before the render: The builder applies, in order, (if any), shortcuts, then the closure — same lifecycle the audio uses, so the same vocabulary works for both audio and GUI tests. For more than one-shot param tweaks, drop down to the closure ( ): Drive to populate meters or animations. Mutate custom (non-param) state on the plugin struct. For state you'd rather author interactively than spell out in code, the standalone host's / saves a file. Load it via : paths are crate-manifest-relative or absolute. The blob just gets fed to — the same path CLAP / VST3 / AU hosts use to restore session state. Promoting a render The test's failure log includes the exact command. A typical flow: is the faster path when you want to write directly to the baseline location: For state-dependent baselines, lets the CLI feed a blob to the renderer: (The CLI can't run setup closures — those live in the test binary, not the cdylib dlopens. Use + for closure-driven baselines.) Tolerance Comparison runs at pixel granularity (one count per RGBA pixel, not per byte) with two independent knobs: — how many noticeably different pixels are allowed. = strict. — how big a per-channel delta has to be before a pixel counts as \"noticeable\" at all. = strict (any byte difference counts). A pixel only consumes the budget if at least one of its R/G/B/A channels differs from the reference by more than . This lets you absorb sub-perceptual drift (rasterizer / driver / font-hinting wobble of 1–3/255) without inflating to numbers that would also hide real regressions: Practical values for : – rides out the drift you get across machines; + starts to mask things a human would notice. Typical bumps are 50–500 pixels for local AA flake on top of a sane threshold. When a tolerance gate fails, the panic message includes the largest single channel delta seen in the diff — useful for deciding whether to bump (drift was sub- perceptual but slightly above your current threshold) or fix the regression (a few pixels jumped by a lot). Cross-OS rendering Per-backend wgpu rasterization differs across GPU/OS combinations (Metal / DX12"},{"id":74,"title":"Slint Integration","section":"GUI backends","href":"/docs/guide/gui/slint","headings":"Getting started · A simple plugin UI · Adding more parameters · Showing formatted values · Available truce widgets · Mixing manual wiring with bind! · PluginContext · Custom state · Screenshot testing · Licensing · Example","excerpt":"Slint is a declarative GUI toolkit. You write your UI in markup files, Slint compiles them to Rust at build time, and handles the window embedding and parameter communication. Getting started Add the ","text":"Slint is a declarative GUI toolkit. You write your UI in markup files, Slint compiles them to Rust at build time, and handles the window embedding and parameter communication. Getting started Add the dependencies: Create : wraps with the truce widget library and the bundled JetBrains Mono font already wired in, so and in your files just work. A simple plugin UI Create : Wire it up in Rust: The setup closure receives a (typed for direct access to fields) and returns a . Slint calls the sync function every frame (60fps) to push host values into the UI. Adding more parameters For each parameter, you need three things: A property in the file ( ) A callback in the file ( ) Wiring in Rust (callback + sync) The macro handles steps 2-3 for simple float and bool params: This generates all the callbacks and the sync closure. You still need the properties and callbacks in your file. Showing formatted values By default, the widget shows the raw normalized value. To show formatted text like \"0.0 dB\", add a string property: Available truce widgets Import from : Knob — 270-degree rotary control with arc, pointer, label Meter — dual-channel vertical level meter XYPad — 2D drag pad for two parameters ParamSlider — horizontal slider Toggle — on/off switch Dropdown — popup list for enum / discrete int params (click-to-cycle) is deprecated since 0.56.0; use instead. You can also use Slint's built-in widgets ( , , ) from and wire them manually. Mixing manual wiring with For parameters that need custom conversion (log scales, enums), wire them before the macro: PluginContext Same as the other backends — exposes its host bridge as methods. IDs use 's generated enum and convert to through : is -able (cheap — internally an ), so Slint callbacks can capture copies. to is also available, so works directly when you need to peek at param metadata or smoothed values inside the sync closure. Custom state If your plugin has persistent state beyond parameters, read it via in the sync callback: To write state back (e.g., user renames an instance): See the state persistence section for the full pattern. If your plugin only uses fields, you don't need any of this — parameter values sync automatically through . Screenshot testing Slint screenshots use the software renderer — no GPU needed: See screenshot testing for more. Licensing Slint has a royalty-free license for proprietary desktop applications, which covers audio plugins. See slint.dev for terms. Example has a complete plugin with knobs, XY pad, meter, formatted values, and screenshot test."},{"id":75,"title":"Vizia Integration","section":"GUI backends","href":"/docs/guide/gui/vizia","headings":"Setup · ParamLens · Widgets · Using raw vizia widgets · Meters · Theming · Screenshot testing · Platform support · Example","excerpt":"Vizia is a declarative GUI framework with reactive data binding and CSS styling. embeds it via baseview and bridges parameter reads/writes through a whose per-param signals keep every widget bound to ","text":"Vizia is a declarative GUI framework with reactive data binding and CSS styling. embeds it via baseview and bridges parameter reads/writes through a whose per-param signals keep every widget bound to the same in sync. Vizia is the least stable of the GUI backends. It's desktop only: macOS (x8664 + aarch64), Windows (x8664), and Linux (x8664 + aarch64) work; iOS and Windows on ARM64 are unsupported - see Platform support below. Editor resize is currently a no-op: records the new logical size but the running view doesn't repaint at it (vizia's has no resize entry point callable from outside its event loop; pending a upstream patch), so ship vizia plugins fixed-size and don't call . The crate also rides an unreleased vizia main-branch rev - no crates.io release. Pick egui, iced, or Slint for new projects. Setup is not published on crates.io — vizia upstream hasn't tagged a release that ships the feature flag (it's main-branch only), and crates.io rejects git-only deps without a shadow. Pull the crate from git instead: Bump the tag in lockstep with your other deps on each release; the sub-workspace inside the truce repo guarantees the git-pinned vizia rev tracks the rest of the framework's version. Vizia itself is a transitive dep — no direct line is required. re-exports it as so brings the full API into scope. Linux: Vizia renders with Skia, whose build links and , so on top of the shared GUI dev libraries you also need the EGL + Wayland-EGL dev packages ( ). Without them the link fails with . The other GUI backends don't link Skia and don't need these. Return a from : The setup closure is bounded (not ) — hosts may close and re-open the editor, and each open call invokes the closure once to build a fresh widget tree. is the window size in logical points. is recommended when you use the built-in widgets — it carries vizia compatibility shims (the collapsing knob head, dropdown popup overflow) without prescribing any palette. matches the look of every other built-in editor; omit it to inherit vizia's default font stack. ParamLens is the bridge between truce's atomic param store and vizia's reactive runtime. Every widget bound to the same shares a , so a knob and an XY pad pointing at the same param update each other on the next frame without any extra wiring. is cheap to clone — hand out clones freely to widgets, they all share the same underlying store and the same per-param signals. Widgets ships param-binding widgets that handle the gesture protocol internally: Typical layout: Discrete params ( , ) snap to grid steps on each gesture so the host sees integer / enum positions, not the raw continuous value baseview hands the widget. Using raw vizia widgets Any vizia widget works — just bind it to the shared signal and echo writes back through the lens: is shared across every widget for the same ; echoing the new value into it keeps the rest of the UI in sync. Meters Meter widgets use a separate reactive surface. reads — a per-meter driven by a root polling timer (30 Hz) inside that fans the latest store values into every registered meter signal. The reactive graph then re-evaluates the Memos driving the bar heights. Declaration matches every other backend (see parameters § Meters): Push from with , draw with . Theming Widgets render against vizia's default theme and prescribe no colors themselves. The constant carries only the minimum CSS needed for establishing a working baseline. Plugins that want a particular palette layer their own stylesheet via : Stylesheets are applied in the order they were added, after vizia's defaults. Each widget tags its outer container with a class ( , , , …) for targeted styling. Screenshot testing Vizia screenshots render against a CPU-backed Skia raster surface — no active OS window or GL context required: See screenshot testing for the full flow. Platform support Platform Supported Notes --- --- --- macOS (x8664 + aarch64) Yes Universal slices Windows (x8664) Yes Linux (x8664 + aarch64) Yes iOS No"},{"id":76,"title":"Hot reload","section":"Guide","href":"/docs/guide/hot-reload","headings":"Setup · What reloads · What does not reload · How it works · Reload sequence · Troubleshooting · What's next","excerpt":"Edit DSP or layout code, rebuild, hear the change in 2 seconds. No DAW restart. No plugin window close. Same source file, same macro — just a Cargo feature. Experimental. Hot reload is a dev-loop conv","text":"Edit DSP or layout code, rebuild, hear the change in 2 seconds. No DAW restart. No plugin window close. Same source file, same macro — just a Cargo feature. Experimental. Hot reload is a dev-loop convenience, not a shipping mechanism. It works well day-to-day for most plugins, but state-format migrations, GUI backend reloads, and certain host edge cases (Pro Tools / AU v3 sandboxing) can still break reload mid-session. If you hit one of those, restart the DAW. Always sanity-check final builds without before shipping. Setup The default scaffold produces a single-crate plugin with the feature + pre-wired: flips the feature on, which makes expand into a dynamic shell that loads your impl out of a separate dylib. The shell watches the dylib for content changes and swaps in the new one while the plugin is live. Logic profile defaults to release for closer-to-shipped DSP perf. Pass to flip the logic to debug profile (faster compile, slower DSP) for tight iteration. The shell binary itself is always built into via and never collides with your regular / outputs. When you're done iterating, ship the release build: Zero code changes between dev and release. What reloads What you edit How fast? --------------- ----------- DSP algorithm 2 s MIDI handling 2 s Widget layout (built-in GUI) 2 s Meter logic 2 s Built-in GUI reloads too. Editing your body and rebuilding swaps the new layout into the running editor without closing the window. The wrapper delegates to the renderer returned by and spawns a background thread watching the dylib. On change, the new is installed via a shared mutex — no flicker. Custom editors (egui, iced, Slint, Vizia) do not hot-reload the UI itself — they reload the DSP, but you still need to close and reopen the plugin window to see layout changes in the custom UI. What does not reload What Why ------ ----- Parameter definitions (adding / removing a ) The host caches parameter count + IDs + names at scan time. Plugin name / IDs Host caches metadata at scan. Bus layout Host configures at init. Changing any of these requires rebuilding the shell ( ) and having the host rescan. That's rare — most iteration is on DSP and GUI layout. How it works The macro expands differently when the feature is on: Without : embeds the directly. Zero overhead, ships in production. With : loads the from a separate dylib via native Rust ABI. A file watcher thread monitors that dylib. Reload sequence File watcher detects the dylib changed (mtime poll every 500 ms). CRC32 hash confirms content actually changed. Audio thread serializes the current plugin state. Old dylib handle is leaked — never (a TLS destructor segfault on macOS, plus stale pointers in TLS on every OS). New dylib copied to a versioned temp path to defeat macOS's dyld cache. On macOS the copy is ad-hoc codesigned (required by SIP). loads the new dylib. An ABI canary verifies type layouts match. A vtable probe verifies trait-method dispatch order. returns a new . The new instance is reset with the current sample rate, then state is restored. The plugin instance is behind a (on macOS that is , which donates the waiting audio thread's priority to the lock owner). Audio thread locks for , main thread locks for . Mouse event handlers release the lock before calling host callbacks, to avoid deadlocks. Troubleshooting Plugin doesn't notice the new dylib. The shell looks for , where is the one baked in at time (release by default; debug if was passed). The path is captured at compile time from (which honors ), so you don't need any runtime env to be set in the DAW process — but it does mean the shell is tied to the workspace it was installed from. Re-run if you change or move the workspace. For ad-hoc overrides (point the shell at any dylib), set in the env that launches the host. Caveat: DAWs launched from Finder / Spotlight / Start don't inherit terminal env, and AU v3 sandboxing strips most vars; this override only works when the DAW is started from the same shell or via (macOS"},{"id":77,"title":"Install","section":"Guide","href":"/docs/guide/install","headings":"Rust · Platform compiler · Install the CLI · Sanity check · Extra setup · Standalone host audio (Linux) · AU v2 · AU v3 (macOS only) · AAX · Signed installers (cargo truce package) · Dual-arch on Windows · sccache · Pin a specific CLI version · What's next","excerpt":"Set up your machine to build truce plugins. Three things: Rust, a system C/C++ compiler, and the CLI. First-timer path. This chapter covers the minimum needed to build CLAP and VST3 — the two formats ","text":"Set up your machine to build truce plugins. Three things: Rust, a system C/C++ compiler, and the CLI. First-timer path. This chapter covers the minimum needed to build CLAP and VST3 — the two formats that work on every OS, that every modern host loads, and that scaffolded plugins enable by default. Other formats (AU, AAX, signed installers, hot-reload sccache) layer on top; their setup lives in extra setup at the bottom and in the reference. Rust Install Rust 1.92+ via [rustup.rs]. If you already have it, . [rustup.rs]: https://rustup.rs Platform compiler Pick the one for your OS — this is the only platform-specific step. macOS Installs the Xcode command-line tools (clang, system headers). All system frameworks (Cocoa, AudioToolbox, …) come with macOS — no package manager needed. Windows Install Visual Studio 2019+ with the \"Desktop development with C++\" workload. The Windows SDK and MSVC toolchain come with it. WSL users: install Rust on Windows itself, not in WSL — plugin hosts can't load ELF binaries from WSL paths. To ship a universal x64 + ARM64 installer, see Dual-arch on Windows. Linux That's enough for a headless CLAP/VST3 plugin. For a plugin with a GUI, also install the X11 + GL + Vulkan + font dev libraries (every truce GUI backend goes through ): (Standalone-host audio adds ALSA + JACK; see extra setup.) Vizia GUI backend additionally needs the EGL and Wayland-EGL dev libraries — Vizia renders with Skia, whose build links and . The runtime files are usually already present; these dev packages add the bare symlinks the linker needs (a missing one shows up as ): The other GUI backends (egui, iced, slint, the built-in renderer) don't link Skia and don't need these. Install the CLI Pulls from crates.io. Cargo picks it up as the subcommand. To upgrade later, run with . Sanity check Reports what's present and what's missing — Rust version, compiler, optional SDKs, validator binaries. Green rows are ready; yellow are optional; red block something. Run any time a build behaves oddly — it's usually faster than reading the error. You're ready for chapter 2 → first-plugin. Extra setup You don't need any of this to build CLAP+VST3 plugins. Pull these in only when you actually need them. Standalone host audio (Linux) The standalone host needs ALSA + JACK headers (or the PipeWire JACK shim) on Linux. macOS and Windows have these in the OS: Modern PipeWire distros: install and don't also run — they fight for the same socket. The shim replaces . AU v2 Ships with macOS — no extra setup. Just opt in: . AU v3 (macOS only) Needs full Xcode (not just CLI tools) — builds the . Switch the active developer directory once: AU v3 also requires a real Developer ID for Apple to load the appex — see . AAX Needs the Avid AAX SDK (point at it) plus PACE/iLok for retail Pro Tools releases. macOS and Windows only. Details in formats/aax. Signed installers ( ) macOS: signing identities + Apple notary credentials. See . Windows: Inno Setup 6 for , plus an Authenticode source (Azure Trusted Signing, cert thumbprint, or ). See . Linux: no signed-installer support yet. Dual-arch on Windows ships a universal x64 + ARM64 installer by default. You need two things: The Rust target: The MSVC cross-tools: VS Installer → Modify → Individual components → check \"MSVC v143 - VS 2022 C++ ARM64/ARM64EC build tools\" (or v145 on VS 2026). reports whether both are in place. Pass to if you want to skip ARM64 for a quick iteration build. sccache If sccache is on , auto-uses it as — significant rebuild speed-up. reports when it's active. Set to skip. Pin a specific CLI version What's next Chapter 2 → first-plugin — , a tour of the generated files, install into your DAW, hear it work. Format details — per-format prerequisites, host coverage, and gotchas in formats/. Environment variables — every signing, SDK, notary, and hot-reload variable in ."},{"id":78,"title":"iOS","section":"Guide","href":"/docs/guide/ios","headings":"Prerequisites · Simulator workflow · Device workflow · Bootstrap from scratch · Per-plugin iOS knobs in truce.toml · GUI considerations on iOS · Preview-audio mute · Screenshot regression · What doesn't ship on iOS · What's next","excerpt":"iOS is the one platform where your truce plug-in runs as something other than a / / in a desktop DAW: it ships as an AU v3 App Extension ( ) wrapped inside a container . Same Rust DSP, same params, sa","text":"iOS is the one platform where your truce plug-in runs as something other than a / / in a desktop DAW: it ships as an AU v3 App Extension ( ) wrapped inside a container . Same Rust DSP, same params, same editor — but the deployment shape, the signing model, and the GUI dimensions are different enough to need their own chapter. This chapter covers the iteration loop. The wire-format details (host coverage, AU identifiers, the simulator's remote-VC quirks) live in formats/au-ios; return there once you're past the first install. Prerequisites What Why ------ ----- Full Xcode (not just CLI tools) , , the iPhoneOS / iPhoneSimulator SDKs Booted iOS Simulator Iteration target; Apple Developer team ID + Required only for device installs and packaging — simulator installs ad-hoc sign with Paired & trusted iPhone / iPad For ; to verify must point at a real bundle. The CLI-tools-only install ( ) doesn't carry the iOS SDKs. Simulator workflow This is where 90% of iteration happens. Sub-second install, no signing, no device pairing. The container app discovers the AU via , instantiates it, and hosts the editor in-process. A Play button drives from the device's mic (for effects) or a single test note (for instruments / MIDI processors), so you can hear DSP changes without wiring the appex into a real iOS DAW. The container is built from a Swift template truce ships at install time — title bar, embedded editor, Play button, status label, plus a landscape sidebar overlay. Custom container apps (your own SwiftUI / UIKit shell with bespoke layout, branding, multi-screen flows) aren't supported yet. Plug-ins that need a richer container hand-author one against the framework's C ABI, build it outside the pipeline, and load the same truce produces. Native first-class support for swapping the container template is on the roadmap. The plug-in shown above is the same you'd run as a CLAP on desktop, with no iOS-specific code in the plug-in crate — the container chrome around it is emitted entirely by . with no installs every in — useful in a multi-plugin workspace, but each install runs a fresh + signing pass so 12 examples is several minutes. Pick a plugin with for iteration. Device workflow Same pipeline as but signs against a real Apple Developer identity and installs through . Three env vars are required ( , , ) and one is optional ( ). Set them in (always gitignored; never put profile paths or identities in — that file is checked in): The simulator path ( ) ad-hoc signs with and ignores all four — none of this matters until you want to push to a real device or build a . Bootstrap from scratch If you've never done iOS device signing before, here's the full end-to-end. Skip whichever steps you've already done. Everything below lives at developer.apple.com/account → Certificates, Identifiers & Profiles (the same UI Apple calls \"developer portal\"). A paid Apple Developer Program membership ($99/year) unlocks device installs and App Store distribution; the free tier (\"personal team\") only signs to your own devices and refreshes profiles every 7 days. 1. Generate a Certificate Signing Request (CSR). This is the public half of a key pair you generate locally; the Apple portal signs it to create a certificate you can use for code signing. You only need one CSR per cert type (development vs. distribution). Easiest path — Keychain Access: Enter your email + a common name (e.g. ), select Saved to disk, and Key Pair Information → RSA → 2048 bits. The wizard drops a file on disk and the matching private key into your login keychain. CLI equivalent (if you'd rather not use the GUI): Keep the file safe — if you lose it, the corresponding certificate is unrecoverable and you start over. 2. Find your . On the developer portal, Membership Details → Team ID — 10 chars, mixed alphanumeric (e.g. ). For personal teams, the ID is auto-generated and shown in Xcode → Settings → Accounts → Manage Certificates instead. 3. Create the signing certificate. Portal → Certificate"},{"id":79,"title":"MIDI","section":"Guide","href":"/docs/guide/midi","headings":"Declaring a MIDI plugin · MIDI 2.0 · Multiple MIDI ports · The event model · Reading MIDI input · Reading values as floats · Emitting MIDI output · Format coverage · Host MIDI compatibility · Bitwig Studio · Logic Pro · Cubase · REAPER · Ableton Live · AUM (iOS) · The pattern · Testing MIDI plugins · What's next","excerpt":"MIDI events ride the same as parameter automation — they're just variants of . Reading them is a on the body; emitting them is . The framework hands you wire-native integers (7-bit , 14-bit , MIDI 2.0","text":"MIDI events ride the same as parameter automation — they're just variants of . Reading them is a on the body; emitting them is . The framework hands you wire-native integers (7-bit , 14-bit , MIDI 2.0 16/32-bit) so values round-trip exactly with the host. Float helpers are one function call away when you want them. Declaring a MIDI plugin Every plugin sees MIDI input that the host sends to it. What varies is whether the plugin produces audio, MIDI, or both. Plugin shape Example --- --- --- Audio effect (may also accept MIDI) EQ, compressor, synth-style filter Instrument (MIDI in, audio out) Synth, sampler Note effect (MIDI in, MIDI out) Arpeggiator, transpose, chord generator Analyzer (no audio out) Spectrum, level meter Set for note effects so each format wrapper opens its MIDI I/O path: VST3 / CLAP: registers a MIDI input and output bus. AU: builds as (MIDI FX), routed to Logic's MIDI FX slot. AAX: registers + MIDI nodes. LV2: emits an output port in addition to the input. Set for synths so wrappers register MIDI input + audio output (and AU builds as ). By default a plugin's MIDI I/O follows its : instruments and note effects accept MIDI input, and only note effects emit MIDI output. Override either direction per plugin with the and keys in (see the reference). They apply consistently across CLAP, VST3, VST2, AU, AAX, and LV2: An audio effect that reacts to MIDI (a CC-controlled filter) sets . On AU this registers it as an MusicEffect so the host routes MIDI to it. An instrument or effect that also emits MIDI (a chord generator, an envelope-to-CC follower) sets so every format declares its MIDI output port/bus. MIDI 2.0 The wire dialect is opt-in per plugin. Without the opt-in everything is MIDI 1.0: a host speaking MIDI 2.0 gets its channel voice down-converted before delivery, so a plugin that didn't ask never sees the 2.0 variants. covers both directions; / override one at a time (absent, each follows ). The keys require a MIDI port in the direction they name: on a plugin with no MIDI input is a compile error, and the blanket needs at least one port in either direction. What the opt-in buys per format: CLAP advertises the MIDI2 note dialect and carries the native 16/32-bit and per-note variants both ways ( UMP in; CLAP-native notes, expressions, and raw MIDI out). AU v3 declares = 2.0 so the host delivers native UMP; output rides the host's block in the host's protocol, with the byte block as fallback (macOS 12+ / iOS 15+). VST3 has no UMP transport; the per-note subset crosses as note expression instead - see the coverage table below. VST2, AU v2, AAX, LV2 stay MIDI 1.0 regardless. decodes every event truce can deliver into a live scrolling log - useful for eyeballing what a host actually sends on each dialect. Multiple MIDI ports Most plugins have one MIDI in and/or out port. A plugin that needs more - a router, a merger, a per-destination sequencer - declares the count with / in : A port count is authoritative: a non-zero / enables that direction's MIDI capability on every format by itself, disables it, and a count that contradicts / is a compile error. Each carries a field. Inbound, filter by ; outbound, stamp the target port with (the plain uses port ). CLAP (N note ports), VST3 (N event buses), and LV2 (N atom ports) carry more than one MIDI port in both directions. On VST3 this includes the channel controllers (CC / pitch bend / pressure), which VST3 delivers as parameter changes rather than events: truce advertises a separate bank of hidden mapping parameters per event bus, so each port's controllers decode back onto that port instead of merging. AU v3 carries multiple MIDI output ports ( , one per declared output; host support varies); its MIDI input, plus VST2, AU v2, and AAX, clamp to a single port and route everything to port , logging a one-line skip so the truncation isn't silent. Getting MIDI onto a port in a host Declaring the ports is the easy half. Whether the host lets a user route a distinct MIDI sour"},{"id":80,"title":"Parameters","section":"Guide","href":"/docs/guide/parameters","headings":"The basic shape · Typed IDs in practice · Parameter types · Enum parameters · Meters · Attribute reference · Range shapes · Smoothing · Sample-accurate automation · Tuning the granularity · Per-parameter opt-out · What plugin code sees · Per-format coverage · Shared ownership (Arc<Params>) · What's next","excerpt":"Declare your plugin's knobs, switches, and meters in one struct. plus attributes generate the plumbing — storage, host-visible IDs, a typed enum, display formatting, and smoothing. The basic shape The","text":"Declare your plugin's knobs, switches, and meters in one struct. plus attributes generate the plumbing — storage, host-visible IDs, a typed enum, display formatting, and smoothing. The basic shape The derive generates: and a impl. A full trait impl (count, IDs, formatting, smoothing, state collection). A enum ( ) with one variant per parameter: , , — typed IDs you can pass to the GUI layout and to . IDs auto-assign from field order. If you need to rename a field after release, keep the same — change the name, not the ID, or host automation and saved presets break. Typed IDs in practice Alias the generated enum once and reuse it: Typos are compile errors. Rename-refactor is safe. Parameter types Field type Widget default Notes ------------ ---------------- ------- knob Continuous. Supports smoothing and custom formatting. toggle On / off. Auto-detected as a toggle widget. knob Integer steps within a range. dropdown Click-to-open list; is a enum. meter Read-only, written from , drawn by the GUI. Enum parameters The range is inferred from the variant count — don't pass . Use on a variant to override its display text ( → displays as , the Rust name stays ). Meters Meters are not parameters — they flow audio-thread → UI-thread instead of host → plugin. Declare them as fields with : Parameters and meters share the generated enum — , , all work. Use those typed identifiers; the derive keeps the underlying IDs collision-free. Write from , draw in : The write is realtime-safe (atomic); the GUI reads the latest value every frame. Attribute reference Every key that accepts — , , , , , custom / , host-modulation flags ( / ), default MIDI-learn bindings ( / / ), and the rest — is in the dedicated params reference. Skim that page when you're looking up syntax; this chapter focuses on patterns. Range shapes maps the host's normalized knob position onto your plain value. The shape sets how that mapping feels under the cursor: Reach for when a control spans decades and you want equal perceived motion across the range, so a cutoff sweep doesn't crawl at the low end and rush at the top. bends a linear range by a power law when you want finer control near one end without the strict ratio mapping of ; does the same but stays symmetric around a center, so a pan or EQ-gain knob keeps its detent centered. Every shape's exact syntax and argument meaning is in the range types reference. Smoothing Host automation usually arrives block-rate. Smoothing interpolates between successive target values so there's no zipper noise on continuous parameters. Pull a smoothed value per sample with — the return type (f32 or f64) follows your prelude choice; see Precision (preludes). The shell sets the sample rate and snaps the smoothers for you before runs, so there's nothing to prime. takes (the atomic smoother state is interior- mutable), so it works through without . Sample-accurate automation When the host sends a parameter change mid-block, the smoother starts ramping from the event's rather than from the top of the block. truce achieves this by chunking at event boundaries: each whose target parameter participates in chunking splits the audio block, and the smoother's runs at the sub-block boundary instead of eagerly at block start. The behavior is on by default for every parameter. Plugins reading per sample get sample-accurate behavior for free — smoothers ramp from the right sample, params snap at the right sample. Tuning the granularity The chunker has one global knob — the minimum sub-block size. Events that would produce a sub-block shorter than this are coalesced (the smoother target is set at the start of the next sub-block instead of at the event sample). Set it in : 32 (default) — sub-blocks land on SIMD-friendly strides; per-event fixed costs in are bounded to . Good fit for typical plugins. 1 — strict sample-accuracy. Pick when an event landing one sample early would matter (transient shapers, hard-cut volume automation). N where N ≥ host max block "},{"id":81,"title":"Plugin anatomy","section":"Guide","href":"/docs/guide/plugin-anatomy","headings":"The moving parts · The four kinds of plugin data · Precision (preludes) · The PluginLogic trait · DSP methods · GUI method · The descriptor and its DSP state · PurePluginLogic - the stateless leaf · Lifecycle · Per-format display names · Bus layouts · Default (stereo and mono) · Instrument (no audio input) · Multiple layouts (host picks) · Sidechain · State persistence · Option A: #[derive(State)] — recommended · Option B: bring your own serializer · How it works · Editor state · What's next","excerpt":"How the pieces of a truce plugin fit together: the trait, the macro, bus layouts, and state persistence. If you've walked through first-plugin this chapter explains why the code you just wrote is shap","text":"How the pieces of a truce plugin fit together: the trait, the macro, bus layouts, and state persistence. If you've walked through first-plugin this chapter explains why the code you just wrote is shaped the way it is. The moving parts Four things you write: A params struct with . A plugin descriptor — a stateless (usually zero-sized) struct. Any per-instance DSP state goes in a separate plain struct named by the descriptor's . A single block — DSP and GUI in one trait ( , , , , , , , , , , …). , , and are required; every other method has a default. A single macro call that wires those into every plugin format. Everything else — parameter hosting, GUI event dispatch, state envelope, format-specific lifecycle, hot-reload shell — is generated. lives in (re-exported as ). The trait covers both the audio-thread surface ( , , …) and the main-thread surface ( ); the framework guarantees the threading split — only runs on the audio thread, only on the main thread. The four kinds of plugin data Every method signature spells out exactly which kind it may read or mutate. There are four, and they live in two homes. On the struct - shared as , so the audio thread ( ) and the editor ( ) both reach it with no extra plumbing. Three kinds of field live here, told apart by two attributes. In - owned by the shell and handed only to the audio-thread methods ( ). The editor never sees it. Kind Declared as Home Host automation Saved to session / preset Reached by ------ ------------- ------ ----------------- --------------------------- ------------ Parameter field yes yes (automatic) audio ; editor Persisted state field, or / , or no yes audio ; editor , or DSP state shell no no audio ; editor none Skip state field no no audio ; editor The two right-hand behavior columns are the whole decision: Should the host draw an automation lane for it? It's a parameter ( ) - gain, frequency, mix, a mode selector. Saved automatically; see parameters. Must it survive session save and preset recall, but isn't a host control? It's persisted state. Reach for a field when it's small editor-facing config (a view mode, an instance label, a picked file path); reach for / when it's an opaque or large blob tied to your DSP state (a decoded sample bank). Both routes are covered in state. Is it audio-thread working memory the editor never touches? It's DSP state ( ) - filter buffers, oscillator phase, a voice pool. Never saved, and it lives in the shell so a hot-reload keeps it alive across a code-only swap. Is it a live channel shared between the audio thread and the editor that must not be saved? It's a field - a lock-free ring of audio-thread events for a visualizer, a shared atomic flag. Not a parameter, not persisted; both sides reach it through the they already share. See Skipped fields. A field is not DSP state. DSP state is audio-thread-exclusive - the shell never hands it to - so a value the GUI must read (a meter, an event ring) cannot live there. It belongs on the struct as a field, the one object both threads share. Precision (preludes) DSP precision is a per-file choice. is the host wire format and the cheaper option; buys mantissa bits for long delay-line accumulators, biquad cascades, and modulation math that drifts at single precision. You pick by choosing a prelude at the top of the file: The four variants follow fundsp's naming: Prelude Buffer Notes --------- -------- ---------------- ------- Default. Alias for . Explicit form. End-to-end . On formats with a 64-bit wire (VST3, VST2, CLAP) the plugin processes the host's buffers directly when the host runs a 64-bit chain; everywhere else the wrapper widens at the block boundary and narrows on the way out. Mixed precision: the buffer stays at host (no boundary widening) while reads and intermediate math run in . Write at the buffer-write site. Each prelude swaps three things in lockstep: a type alias, the / extension trait that resolves through, and the / leaf trait (re-exported as either way, so is the sa"},{"id":82,"title":"Presets","section":"Guide","href":"/docs/guide/presets","headings":"Quick start: ship factory presets · Authoring presets in your DAW · Converting presets between formats · Preset packs · Where presets live · Per-host notes · CLI reference · Where next","excerpt":"Declare a presets directory once and every format truce builds surfaces your factory presets in the host's preset browser - CLAP, VST3, AU v2 and v3, and LV2. Users save their own presets through the ","text":"Declare a presets directory once and every format truce builds surfaces your factory presets in the host's preset browser - CLAP, VST3, AU v2 and v3, and LV2. Users save their own presets through the host as usual, third parties ship preset packs as plain zip files, and you author libraries inside your own DAW: save presets with the host's native UI and converts them into the factory library. Quick start: ship factory presets Put TOML files in next to your crate. The first directory level is the category: Each file is human-readable TOML: a , optional metadata ( , , , at most one per library), and a table keyed by your struct's field names (numeric param ids also work), with plain values: Skip the field - the first install stamps one. You rarely write these files by hand: (below) generates them from presets you save in your DAW. See for a complete library. . The install pipeline validates the library (parseable TOML, unique uuids and display names, a single default) and emits the format-native preset files: native containers + runtime discovery for CLAP, the factory preset list for AU v2/v3, files in the OS preset locations for VST3, TTL in the LV2 bundle. Hosts pick them up on their next scan. The directory is picked up automatically; set in to use a different path. Anything your plugin persists is captured: presets wrap the same state blob as session recall, so plus your extras round-trip identically. The field carries the latter as base64 - another reason to author via rather than by hand. Authoring presets in your DAW Open your plugin in whatever host you like, dial in a sound, and save it with the host's own \"Save Preset\" action - Logic's preset menu, Reaper's preset dropdown, the VST3 save dialog. Then harvest the results: finds host-saved presets in the standard OS locations, checks each one actually belongs to your plugin, and converts it into a TOML in your crate's directory - with a freshly minted uuid, params keyed by field name, and display names in the comments: Move the file into a category directory, tweak the name or tags, and the next ships it as a factory preset in every format. Iterating on an existing preset works the same way: load it from the host's menu, tweak, save under the same name - updates the existing in place and keeps its identity, so saved sessions that reference it don't break. (The file is regenerated, so hand-written comments in it are replaced; pass to always create a fresh preset instead.) Converting presets between formats Every preset file truce produces wraps the same state blob, so presets convert losslessly between formats for any truce plugin - this doesn't work for other vendors' plugins, whose preset content is opaque: This is how presets saved in one DAW move to another: convert the files and place the results in the target format's preset location. Preset packs A pack is a zip of the library in every format, built with: Users install it with , which places the truce-native tree under the user preset root's directory (where CLAP discovery and the preset API find it). The zip also carries / trees to drop into the OS preset folders for hosts that scan those. Where presets live Scope Location --- --- Factory Inside the plugin bundle, written at install User Per-OS user directory (below) Pack under the user directory Per-OS user roots: The part is overridable with in , which resolves to on macOS, on Windows, and on Linux. Pick it once, before first release - changing it later orphans presets users have already saved. Full field reference in reference/truce-toml. A preset keeps its identity (a uuid in its metadata) across rename, recategorise, and scope moves, so host sessions that reference a preset keep resolving after you reorganize your library. A user preset with the same uuid as a factory preset overrides it. Per-host notes Format Where presets show up --- --- CLAP Host preset browser via the discovery factory. Bitwig indexes it today; Reaper's support is in its builds. VST3 Host"},{"id":83,"title":"Processing audio","section":"Guide","href":"/docs/guide/processing","headings":"Buffer model · Per-sample effect · Per-channel loop with input/output pairs · Per-frame processing · SIMD block operations · The ops catalog · Reading smoothed params per block · Walking the buffer in chunks · Fast path / slow path · Composing through scratch buffers · Compile-time SIMD baseline · More examples · MIDI and parameter events · Sample-accurate event splitting · Host transport · Meters (DSP → UI) · Declaring tail time · Building a synth · Offline rendering · In-place I/O (opt-in) · What's next","excerpt":"is called on the audio thread for every block. Same constraints as any audio plugin — no allocation, no locking, no I/O, no . Rust's type system catches a lot of this; the rest is up to you. The signa","text":"is called on the audio thread for every block. Same constraints as any audio plugin — no allocation, no locking, no I/O, no . Rust's type system catches a lot of this; the rest is up to you. The signature is always: Everything in this chapter is a different shape for that function. Buffer model exposes one slice per input channel and one mutable slice per output channel, both borrowing host memory. Wrappers do not copy input into output: read from and write to . For instruments, output starts wherever the host left it (typically zero, but don't assume — write every sample). The slice element type is under and under . Where the format has a 64-bit wire - VST3 ( ), VST2 ( ), CLAP (64-bit ports) - a plugin reads and writes the host's buffers directly, zero conversion; elsewhere (and whenever the host runs a 32-bit chain) the wrapper widens at the block boundary and narrows on the way back out. See Precision (preludes). The signatures below assume the default : , , , and all return slices of length — the current block, or the current sub-block if you've called . Per-sample effect The most common shape — one multiplication per sample per channel: Pull smoothed param values per sample when they need to glide cleanly (gain, filter cutoff). Pull per block for param reads that are expensive or that don't care about sample-accuracy (mode switches, enums). Per-channel loop with input/output pairs If you need separate read and write pointers (convolution, IIR filters) rather than in-place modification: Per-frame processing is channel-major: each / is a contiguous slice for one channel. But a lot of DSP is naturally frame-major — one input frame in, one output frame out — and the libraries you'd reach for expect exactly that shape: , nodes, a hand-written per-sample node. Feeding those from a channel-major buffer means either copying each frame into a scratch first (a heap allocation on the audio thread) or fighting the borrow checker over two live borrows of the buffer. The family does that transpose for you, in place, against a stack-allocated frame pair — no heap, no borrow gymnastics at the call site. deref-coerces to , so the frames pass straight to slice-taking APIs like fundsp's . requires (debug-asserted). When your DSP has a fixed frame shape that need not match the bus width, reach for the form instead: fills input slot from bus input channel , repeating the last available channel when the bus has fewer than inputs — so a mono source fans into both inputs of a stereo graph. Output slot writes bus output channel while ; frame outputs past the bus width are dropped, and a bus with no inputs (an instrument) feeds silence. That one call covers stereo and mono-in/stereo-out alike, with no per-width branch — the right tool when your plugin declares multiple bus layouts. is the shorthand for the common case (a , a stereo filter block) — same behavior, no turbofish: Under the frames are . This is the shape the fundsp chapter builds on; use (below) instead when you want SIMD-width blocks rather than single frames. SIMD block operations LLVM autovectorizes the simple per-sample shapes and the cost is invisible. The crate exists for the rest: many channels, many smoothed knobs, transcendentals in the inner loop. Its per-block primitives compile down to packed SIMD (NEON on aarch64; SSE / AVX / AVX-512 on x8664) and unlock a 4x–16x throughput win on the shapes that need it. Reach for it when you've measured a hot spot, or when you know up-front the workload will hit one of those triggers. The ops catalog — the building blocks, all : Each has a twin ( , …) that does the same work without SIMD — useful as a reference for tests. The versions live under with identical names. — vectorized transcendentals, also : These matter because 's scalar transcendentals are opaque to LLVM's autovectorizer — even with , a loop calling stays scalar. The block forms route through 's vectorized intrinsics, so a dB → linear conversion in front of an envelope (the"},{"id":84,"title":"Real-time safety","section":"Guide","href":"/docs/guide/rt-paranoid","headings":"Enabling the checker · Gating tests · Cover the conditional paths · Frees and locks · Letting a block allocate on purpose · Modes and the report · Running the checker in a host · API surface · What's next","excerpt":"Allocating on the audio thread - a that grows past its capacity, a stray , a , a mutex lock - is the classic cause of dropouts under load, and it is invisible on a quiet dev machine: the code runs fin","text":"Allocating on the audio thread - a that grows past its capacity, a stray , a , a mutex lock - is the classic cause of dropouts under load, and it is invisible on a quiet dev machine: the code runs fine until a real session pushes it. is an optional checker that flags any allocation the audio thread makes inside (and, opt-in, frees and locks too), turning that silent hazard into a loud, pinpointed failure. It is off and zero-cost by default. With the feature disabled the guard around compiles away and no custom allocator is installed, so release builds are completely unaffected. Enabling the checker Two steps, both per crate. Turn on the feature and install the checker's global allocator at your crate root: The call is unconditional; the feature does the gating. With the feature off it expands to nothing (so it never collides with a you set yourself), and with it on it installs the checking allocator. It has to be a macro you place rather than something emits, because a global allocator can only be declared in the final artifact - a library cannot set one for the binary that links it. Gating tests The primary use is turning an audio test into an allocation test. Because a run drives your real through the same path a host does, wrapping one in fails if allocated anywhere during the run: Run it with the feature on; without it the helper is a no-op, so your ordinary is unaffected: Cover the conditional paths The checker only flags allocations on code a test actually runs. A constant-input render exercises the steady-state DSP, but an allocation that only happens on a parameter change or a state load stays hidden until a test triggers it. Script those paths so they are covered: Frees and locks flags allocations. Two more real-time hazards are opt-in: Frees. also counts frees inside a value allocated in an earlier block and dropped on the audio thread. It is off by default because dropping a value moved in from a prior block is a common, benign shape; enable it to catch that class deliberately. Locks. and are drop-in replacements for the types that flag a / / taken inside . Use them for state your DSP shares with another thread and the checker catches a lock on the audio thread. Only locks taken through these types are seen - not , , or an OS primitive reached directly. bundles all three: it enables dealloc flagging for the run and fails on any allocation, free, or truce-typed lock in . Letting a block allocate on purpose If a region inside must legitimately allocate - a rare first-block lazy init, a debug-only measurement - wrap just that region in so it is not flagged: Keep the scope as tight as possible: everything inside runs unchecked. Modes and the report The scoped assertions ( , ) gate a test on their own, so most suites never set a mode. Outside them, the mode decides what a violation does. Set it once - in a test harness, , or your plugin's - and the last call wins: Mode Reaction --- --- (default) Log the count and a backtrace after the block; keep running Fail the block - gate a whole suite in one line Abort at the exact allocation, to catch the live stack in a debugger A violation reports the count and the resolved stack of the first offender (symbolication is deferred until after the block, so the capture itself stays allocation-free): Running the checker in a host Because installs the allocator in the final artifact, the check is not test-only. Build the plugin (or the standalone host) with , load it in a DAW, and violations print to stderr per the mode - catching allocations that only a real host provokes, like an automation curve or a buffer-size change mid-session. Pick the reaction from : This is a diagnostic build - ship your release without the feature so the checker (and its allocator) compile away entirely. API surface What's next Chapter 10 → audio testing - the these assertions wrap, and the rest of its capture surface. Chapter 17 → best practices - the broader audio-thread rules the checker enforces mechanic"},{"id":85,"title":"Shipping","section":"Guide","href":"/docs/guide/shipping","headings":"Enabling formats · install · build · validate · Strict mode for CI · Installing the validators · CI step (GitHub Actions) · package · Scope (--ask / --user / --system) · macOS flow · Windows flow · Universal (x64 + ARM64) Windows · Linux · Secrets belong in .cargo/config.toml · CI · macOS (GitHub Actions) · Windows (GitHub Actions) · Troubleshooting · Reference · What's next","excerpt":"From a plugin that works on your machine to a signed installer users can double-click. Four commands get you there: Per-format requirements (SDKs, env vars, signing specifics) live in docs/formats/; t","text":"From a plugin that works on your machine to a signed installer users can double-click. Four commands get you there: Per-format requirements (SDKs, env vars, signing specifics) live in docs/formats/; the install destinations table on the quick-start page lists the exact user / system path each format lands at. This chapter covers the cross-format workflow and signing. Enabling formats Scaffolded plugins ship CLAP + VST3 + standalone by default. Add more formats to in , or pass them explicitly: Flags add to the active feature set for this one invocation — they don't modify . To enable a format every time, add it to : Per-format setup (Xcode required for AU v3, PACE wraptool for retail AAX, , etc.) is in the per-format pages: clap · vst3 · vst2 · lv2 · au · aax. Builds, bundles, codesigns on macOS, and writes into the standard plugin directories on your machine. User-scope is the default on every platform — the dev loop doesn't prompt for a password. macOS user (default): . No . macOS system ( ): . Prompts for once per run. Windows user (default): , . No Administrator shell needed. Windows system ( ): . Run from an Administrator prompt. Linux: , , , . The Linux scope flags are accepted for symmetry with macOS / Windows but resolve to the same paths every host already scans. AAX and AU v3 are always system-scope (Pro Tools / pluginkit only scan the system root); for those formats falls back to the system path with a one-line . Windows VST2 is also system-only on Windows. The install scope is a per-invocation developer choice — has no override, only the CLI flag. Full per-platform path table — bolded to mark the default destination per (OS, format) — is in the quick start's install destinations section. mirrors the same flags. By default it scans both scopes (handy when you switched scope mid-iteration); pass / to limit. prints both paths per format with a writable / sudo / not-present marker, and warns when the same plugin name is installed in both scopes (hosts pick one and shadow the other). Same bundle layout as , but written to instead of the system plugin directories: Every format flag produces a complete, signed bundle in . Runs the free validators against your installed bundles: clap-validator ( ) exercises CLAP lifecycle, parameters, state, and process safety. pluginval (Tracktion, ) runs at strictness 10 (max) against the installed VST3 bundle. auval (macOS only, built into CoreAudio) exercises AU v2 + AU v3 lifecycle and parameter behavior. VST2 smoke is built in — it s the dylib and verifies returns a well-formed . Set to override auto-discovery. tells you what's found. Strict mode for CI Per-format flags fail the run if their validator is missing: Invocation Validator missing → --- --- (no flag) warning, exit 0 warning, exit 0 error, exit non-zero error, exit non-zero Wire (or whichever subset you want) into your CI so a missing validator binary fails the build instead of silently passing. Installing the validators ships with macOS — nothing to install. CI step (GitHub Actions) Drop this into the job that already builds and installs your plugin. It mirrors what truce's own CI runs on every PR: Substitute the macOS / Windows pluginval download URL on those runners. Headless Linux jobs need in front of because pluginval and clap-validator both open a window during state checks. Output: , , or (the Linux tarball ships an instead of taking a / scope at build time — see Linux below). macOS and Windows builds are optionally suffixed with / when the scope is hard-locked so a and build of the same plugin don't overwrite each other in . Version comes from or in . Scope ( / / ) (the default) lets the end user pick at install time: macOS: shows a \"Destination Select\" page with \"Install for me only\" pre-selected; \"Install for all users\" triggers the standard auth prompt. Windows: Inno Setup shows a \"Choose installation mode\" page with \"Install for me only\" / \"Install for all users\". The wizard relaunches itself elevated only if th"},{"id":86,"title":"State","section":"Guide","href":"/docs/guide/state","headings":"Params vs state · #[persist]: saved fields on the params struct · snapshotinto / loadstate · Saving without stalling audio · The load path · What's in the blob · Migrating state from a pre-truce build · Keyed formats: declaring the old keys · Testing a migration · Where next","excerpt":"Everything the host persists for your plugin - session save, preset recall, duplicating an instance - is one blob: the parameter values, serialized automatically, plus an optional extra state block yo","text":"Everything the host persists for your plugin - session save, preset recall, duplicating an instance - is one blob: the parameter values, serialized automatically, plus an optional extra state block you own. This chapter covers the extra block, what happens on load, and how to migrate state saved by a pre-truce build of your plugin. Params vs state Use a param for values the host should treat as a control: gain, frequency, mix, bypass, mode selectors. Hosts list params in their automation editors, draw lanes against them, and feed them through MIDI CC mappings. Params are numeric atoms and serialize automatically - you never write code to persist them. Use state for everything that isn't a numeric atom: strings, file paths, loaded sample buffers, lists, view modes, nested structs. State is opaque to the host - no automation, no CC mapping, no entry in the host's parameter list. The framework round-trips the bytes you hand it and nothing more. There are two ways to carry that non-param state: a field on the params struct (below), or the / pair (further down). Both are saved in the same envelope; they differ in where the value lives and who reaches it. : saved fields on the params struct For small editor-facing config - a view mode, an instance label, a picked file path, an edit counter - mark the field on the struct. The host saves it alongside the parameter values with no body at all, and because it lives on the params struct the editor reads and writes it directly through the it already holds. Pick the wrapper by the field: a scalar (a persisted , enum, or index) goes in a lock-free ; a , , or struct goes behind / . Fields are keyed ( , defaulting to the field name), so a load skips unknown keys and leaves missing ones at their defaults - old sessions open in newer plugin versions and vice versa. It works inside param structs and on every format. vs : both persist, in the same envelope. Use for small config the editor edits in place; use / (below) for an opaque or large blob that belongs with the DSP state. And note the contrast with a field: same shared- mechanism, but a field is deliberately not saved - it's a live channel like an event ring (see the parameters reference). / Two optional methods on : The defaults mean \"no extra state\" - a plugin whose entire identity is its params never touches these. When you do have extra state, generates the serialize/deserialize pair for a plain struct: Return for bytes you can't interpret - the wrapper logs it, and on formats whose state API has a failure return, reports it to the host. Saving without stalling audio is the hook for serializing extra state, and it runs off the critical path: the audio thread serializes into a reused buffer after each block and publishes it to a slot the host reads directly, so saving state - even a large blob - never stalls audio. Because it runs every block, keep it cheap and allocation-free (use , which reuses the buffer's capacity). Returning is a static opt-in: once you return , always return (write an empty if there's nothing to save), never flip back to . still exists as a convenience the framework calls off-thread (the test driver, the standalone host), and it delegates to by default - so implementing covers every path. You don't override ; overriding it no longer reaches the host. The state example is the runnable version of this pattern, including the editor side. The load path Hosts call their state-load API on the main thread, but takes , which would alias the audio thread's DSP state inside . The wrappers resolve this the same way on every format: The blob is parsed on the host thread. Parameter values apply immediately (param storage is atomic), so a host that reads params right back after the load sees the restored values. The extra block is handed to the audio thread, which runs your between blocks. Two hooks fire after a load, one per thread: - audio thread, immediately after . Invalidate anything derived from state that the next reads: a"},{"id":87,"title":"Workers","section":"Guide","href":"/docs/guide/workers","headings":"The one rule · Reaching shared state through Params · Managed background tasks · Concurrency: SERIALIZED · Mixing lanes · Shape 1: offload construction · Shape 2: stream and analyze · AudioTap · Draining on the pool · Draining on a dedicated StreamWorker · Publish results to the GUI · Pool or dedicated thread? · init and InitContext · Lifecycle · The primitives · Real-time checklist","excerpt":"Some work has no place on the audio thread: allocating or freeing memory, building a DSP graph, an FFT, resampling, reading a file - anything that can block or run for an unbounded time. The fix is to","text":"Some work has no place on the audio thread: allocating or freeing memory, building a DSP graph, an FFT, resampling, reading a file - anything that can block or run for an unbounded time. The fix is to move it to a worker: a background thread that does the heavy work while stays real-time. truce gives you two managed pieces so you rarely wire threads up by hand: Managed background tasks - implement one function, and truce runs it off the audio thread on a shared pool. For discrete work: build a graph, decode a file, run one FFT. + - a lock-free ring for streaming samples off the audio thread, and an optional dedicated thread to drain it. For continuous work: spectral analysis, a loudness meter, an oscilloscope. You still own the shape of the handoff - what the task does, what it reads and writes - because that differs per plugin. What you no longer write is the thread, the shutdown flag, the join, and the wake-up plumbing. Do you actually need one? Only if the work is both too heavy for one block and can't be pre-computed in . runs off the audio thread, so a one-time buffer allocation or a filter rebuild triggered by a sample-rate change belongs there, done synchronously - no worker needed. Reach for a worker only for continuous heavy computation (spectral analysis) or a rebuild driven by live automation. The one rule The audio thread's side of every handoff must be wait-free: no lock, no allocation, no free, no syscall, no unbounded loop. In practice: scheduling a task ( / ) and pushing to a tap ( ) are wait-free and drop-or-coalesce when full, never block; the worker does all the heavy work and all the heavy drops - a big or is never dropped in . A worker can allocate and free freely - that's the whole point of moving the work there. Blocking is the one thing to place carefully: a dedicated owns its thread and may block on I/O or a lock as long as it likes, but a handler runs on a pool shared with every other truce plugin in the host (see below), so it must stay short and non-blocking. Reaching shared state through Your task type is a small request, and receives it as - but is only that one request, not the plugin's shared channels. The handoff lives on the params instead, as a field (not a parameter, just state the param store carries). Both sides reach it through : on the audio thread, on the pool, and on the GUI thread. This is the same mechanism the GUI uses to reach a worker's results: the plugin publishes a handle (a spectrum, a meter) into a field, and reads it back. Managed background tasks For discrete off-thread work, define a task type (the request), implement on that type, and list it in . The handler is - the request arrives as ; truce runs it on a shared, bounded pool. Schedule from (or the editor) through the context, selecting the lane by task type: Two ways to schedule, both wait-free: Method Behavior Use when -------- ---------- ---------- FIFO; every task runs; returns if the inbound queue is full Each request is distinct and must run Single slot; keeps only the newest target and runs it once per drain Only the newest target matters (a knob sweep) The pool is process-wide and bounded (it never grows past ), warmed when the plugin is instantiated - so a handler first scheduled from never spawns threads on the audio thread - and shared by every plugin instance. A panic in is caught, and a worker that fails to spawn drops its task rather than taking the pool down, so one bad handler can't strand it. There is no thread to own and no to write - a plugin that lists no tasks gets no pool at all. Because the pool is shared and small (as few as one thread on a dual-core machine), keep handlers short and non-blocking. A handler that blocks on disk I/O, a network call, or a lock stalls background work for every truce plugin in the host, not just yours. Allocation and CPU-bound bursts are fine. For work that genuinely blocks or runs long, give the plugin its own thread with a instead of the pool. Concurrency: Each task"},{"id":88,"title":"Migrations","section":"Overview","href":"/docs/migrations","headings":"Upgrading to 6.0 (from 5.x) · Upgrading to 5.0 (from 4.x) · Background tasks · Bus layouts (no code change, changed behavior) · Upgrading to 4.0 (from 3.x) · No DSP state - PurePluginLogic · type DspState = Self · type DspState = MyDspState - a separate struct · Upgrading to 3.0 (from 2.x) · Upgrading to 2.0 (from 1.x)","excerpt":"Upgrade guides for truce's breaking major releases. Each section is self-contained: follow the steps for the version you're jumping to. For the full release notes behind these changes, see the changel","text":"Upgrade guides for truce's breaking major releases. Each section is self-contained: follow the steps for the version you're jumping to. For the full release notes behind these changes, see the changelog. Upgrading to 6.0 (from 5.x) 6.0 moves saving custom state completely off the audio thread, so even a large serialize can't glitch audio when the host saves during playback. The only source change is for plugins that save custom state - state beyond parameters - by overriding . Plugins that keep everything in parameters (including fields) need no change. Custom state is now published through instead of . writes into a buffer the framework hands you and reuses each block, which is what lets the host read it back without ever touching the running plugin. still exists (the framework calls it off-thread and it delegates to ), but overriding it no longer reaches the host. Move your body into : write into the buffer instead of returning a , and return (or for \"no custom state\"). is unchanged. If your custom state is a struct, its generated already fits: Keep cheap and allocation-free: it runs on the audio thread after every block. reuses the buffer's capacity, so a steady state never allocates. Upgrading to 5.0 (from 4.x) 5.0 reworks the background-task API and changes the default bus layout. The only source break is the task API, and only if your plugin uses it. Background tasks The trait - implemented once on the plugin, with a and a function - is replaced by , implemented on each task type. The handler is : the request that used to arrive as the argument is now . Each task type also carries an optional (default ); set it to for a handler that must never run on two workers at once (one that read-modify-writes a non-atomic scratch buffer or cache). now takes a bracketed list of task types, one lane each. Move the impl onto the task type and bracket the value: reads become . Scheduling is unchanged: still returns the spawner, and / are the same. To run more than one kind of task, list them all - - and select the lane by type at the call site. Bus layouts (no code change, changed behavior) now defaults to instead of stereo-only, so an effect that took the default appears on mono tracks too. Nothing to edit, but a mono track hands a one-channel buffer: loop over rather than assuming two. If you want the old stereo-only behavior back, override with . Upgrading to 4.0 (from 3.x) 4.0 turns into a stateless descriptor: its methods are associated functions - no - that take the data they touch as arguments. That separates the three kinds of per-plugin data, and a method's signature now says which it may read or mutate: Parameters ( , a struct) - the host-automatable values, passed read-only to the audio methods as and to the editor as . Saved state ( / , with ) - the opaque bytes the host persists in a project or preset. Unchanged. DSP state ( , a plain struct) - the instance-local audio memory (filter buffers, oscillator phase, a voice pool). It lives in the shell, so a hot-reload keeps it alive across a code-only edit. also now takes instead of ; read / , and branch on for offline rendering. Every plugin makes the same three moves, whatever its state: Move the non-params fields (filter memory, phase, voices) off the plugin - now a stateless descriptor - into . Parameters were already a separate struct and don't move. Replace with , which builds that state. There is no stored anymore. Most plugins ignore (write ); it's there to schedule startup background work. Give each method / instead of . The examples below show in its current form. gained the argument in 4.1; on exactly 4.0 it's with no . The only choice is where the DSP state lives. No DSP state - A pure parameter-driven effect keeps nothing between blocks, so it implements and the state plumbing disappears entirely - no , no , no argument, and no (the shell snaps the smoothers for you): A blanket impl makes it a with , so and every format consume it unchanged. (You can also stay on a"},{"id":89,"title":"Badges","section":"Reference","href":"/docs/reference/badges","headings":"The badges · Add one to your README","excerpt":"Shipped a plugin built with truce? You are welcome to show it off with a \"Built with Truce\" badge on your project's README, site, or store page. It is entirely optional - nothing about truce requires ","text":"Shipped a plugin built with truce? You are welcome to show it off with a \"Built with Truce\" badge on your project's README, site, or store page. It is entirely optional - nothing about truce requires it - but it helps other developers find the framework, and we appreciate it. The badges Copy Markdown '>Copy HTML Copy Markdown '>Copy HTML Copy Markdown '>Copy HTML Copy Markdown '>Copy HTML Add one to your README Markdown: HTML (for finer control over size): Swap the file name for whichever variant suits your page: Badge File --- --- Pill, dark Pill, light Stacked Mini All live under ."},{"id":90,"title":".cargo/config.toml reference (build-environment vars)","section":"Reference","href":"/docs/reference/cargo-config","headings":"macOS code signing · macOS notarization · iOS code signing · AAX SDK · Windows Authenticode signing · Azure Trusted Signing · Certificate thumbprint · .pfx file · Common Windows signing · PACE / iLok signing (AAX retail) · Validators (cargo truce validate) · Build / toolchain · Hot-reload · What's NOT here · Example","excerpt":"Per-developer credentials and machine-specific paths live in your project's table — gitignored and machine-local. Cargo injects everything in into the environment of any subcommand it spawns, so value","text":"Per-developer credentials and machine-specific paths live in your project's table — gitignored and machine-local. Cargo injects everything in into the environment of any subcommand it spawns, so values you set here are visible to automatically. Setting the same variable in your shell also works; table values take precedence when present (or you can opt into shell precedence with , see Cargo's docs). macOS code signing Variable Purpose ---------- --------- identity for plug-in bundles. Full or for ad-hoc (the default). Required for AU v3 — Apple won't load an appex bundle without a real Developer ID. identity for installers. Typically . Unset → installer is unsigned. Set both for a release build; ad-hoc ( , the default) is fine for local install + DAW load testing of CLAP / VST3 / AU v2. macOS notarization Used by when in . Two credential paths — the keychain profile is preferred, explicit credentials are the fallback. Variable Purpose ---------- --------- Keychain profile name for . Default . Create with . Apple ID email — fallback path when no keychain profile exists. Apple Developer Team ID — pair with . App-specific password from appleid.apple.com — pair with + . Never put this in . iOS code signing Required for and (.ipa). Simulator installs ( ) ad-hoc sign with and ignore all four. Variable Purpose ---------- --------- Apple Developer Team ID (10-character mixed alphanumeric, e.g. ). Required for device + . iOS-specific identity — for device installs, for App Store / TestFlight . Defaults to (the macOS identity) when unset — wrong for device installs, fine for simulator. Absolute path to a downloaded from developer.apple.com. Must match the bundle ID (or be a wildcard covering it) and the team you're signing with. Required for device + . Optional separate profile for the extension. Use when is bound to the container's exact bundle ID rather than a wildcard covering both container and appex. Unset → the appex reuses the container's profile. AAX SDK Variable Purpose ---------- --------- Absolute path to the AAX SDK root (the directory containing , , etc.). Required to build the format. macOS / Windows; Linux has no AAX. Windows Authenticode signing Pick one of three credential sources. First non-empty source wins, in the order Azure → cert thumbprint → . Setting none of these silently produces unsigned binaries (with a warning at the top of ). Azure Trusted Signing Variable Purpose ---------- --------- Azure Trusted Signing account name. Certificate profile name within the account. Trusted Signing endpoint. A full URL, or a region short code (e.g. ) expanded to . Defaults to (East US). Override path to . Defaults to . Azure's own auth (tenant ID, client ID / secret) is read from its standard env vars by the Azure SDK at signing time — , , . Set those alongside the vars above. Certificate thumbprint Variable Purpose ---------- --------- SHA1 thumbprint of a cert already in the Windows cert store. Cert store name. Defaults to . file Variable Purpose ---------- --------- Path to a file. Password for the . Never put this in . Common Windows signing Variable Purpose ---------- --------- RFC 3161 timestamp server. Defaults to . PACE / iLok signing (AAX retail) Variable Purpose ---------- --------- iLok account name for . Required for retail Pro Tools releases. PACE-issued signing ID. Pair with . Without these, skips PACE wrapping. Local testing in Pro Tools Developer with a dev iLok license doesn't need them. Validators ( ) Truce locates each validator on by default; these overrides point at non- installs (Mac bundles, sibling source checkouts). Variable Purpose ---------- --------- Override path to (or ). Override path to . Build / toolchain Variable Purpose ---------- --------- Minimum macOS version for emitted bundles. Defaults to . Set to to disable the auto- wrapper truce uses to speed up rebuilds. Useful when sccache misbehaves on a particular machine. Hot-reload These are read by the shell binary (the macro emits the read)"},{"id":91,"title":"cargo truce CLI reference","section":"Reference","href":"/docs/reference/cli","headings":"Conventions · new · install · uninstall · build · --target-cpu · run · validate · package · doctor · screenshot · preset · Cache resets","excerpt":"Every subcommand and the flags it accepts. For the narrative shape of the dev loop, see the guide; for per-format gotchas, see formats. Conventions All build-producing subcommands ( , , , , , ) defaul","text":"Every subcommand and the flags it accepts. For the narrative shape of the dev loop, see the guide; for per-format gotchas, see formats. Conventions All build-producing subcommands ( , , , , , ) default to the cargo release profile. Pass for the cargo dev profile (faster compile, slower DSP). selects a single plugin in a workspace by its cargo crate name. Without , the command operates on every in . Format flags ( , , , , , , ) add to the active feature set for one invocation only — they don't modify . Mix and match: . Scaffold a new plugin or workspace. Flag Notes ------ ------- Generate a Cargo workspace with one sub-crate per name. Shared . Scaffold an instrument (no audio input). Default is an effect. Scaffold a MIDI effect. Scaffold with a DSP-state struct and the argument pre-wired. The default. Scaffold the stateless - no DSP state, no argument. Vendor name. Default: placeholder (\"My Company\"). Vendor reverse-DNS prefix. Default: . Per-plugin category in workspace mode ( , , ). Skip the standalone host (drops , the bin entry, and the feature). Scaffolded plugins enable , , and in . Build, bundle, and copy plugins into your local plug-in directories. Flag Notes ------ ------- / / / / / / Enable the format for this run. Per-user paths (default on every platform; no /admin). System-wide paths ( on macOS, Administrator shell on Windows). Install the dynamic-shell build for hot reload. See hot reload. Cargo dev profile (faster compile, slower DSP). Install existing bundles from ; skip rebuild. Single plugin in a workspace. Override the x8664 SIMD baseline. See below. AAX, AU v3, and Windows VST2 are always system-scope — for those falls back to system with a one-line . Per-platform install destinations: see for the full table. Remove installed plugins. Flag Notes ------ ------- / / / / / / Limit to one or more formats. / Limit to one scope. By default scans both (handy when you've switched scope mid-iteration). Single plugin in a workspace. Target a bundle by display name when no longer lists it. Remove bundles whose entry was deleted from . Print what would be removed without touching disk. Skip the confirmation prompt. Same bundle layout as , but written to instead of system plug-in directories. Useful for CI, packaging dry-runs, and inspecting artifacts. Every format flag produces a complete, signed bundle in . Override the SIMD baseline the build targets. Threaded into so 's compile-time dispatch picks the right / path. Applied per cargo target triple, so the same setting is safe for multi-arch invocations (the x86 setting never leaks onto an aarch64 slice of a universal macOS build, and vice versa). Accepted values: Value Expands to Notes ------- ------------ ------- (omitted) on x8664; nothing on aarch64 Default. AVX2 + FMA + BMI2 — the floor modern DAW hosts already require, so the perf win is free in practice. (no flag passed) rustc's base target ( on x8664 = SSE2-only). Use when you need maximum CPU compatibility. SSE4.2 baseline. Same as the default; spell it out when scripting. AVX-512 baseline. Local dev only — the resulting binary won't run on machines without the build host's exact feature set. Any other literal passed verbatim e.g. , . Anything rustc accepts. aarch64 builds always emit NEON (it's the ARMv8 baseline); the flag is a no-op there unless you pass a CPU-specific value like . The flag is accepted by , , , and with identical semantics. Build and launch the standalone host. The standalone is built — no format wrapper is needed to preview a plugin. Use to enable extras alongside (a format, or a plugin-specific feature). Flag Notes ------ ------- Extra cargo features (comma- or space-separated) enabled alongside . Skip rebuild; run the existing staged binary. Cargo dev profile (faster compile, slower DSP). Same semantics as . Pass everything after to the standalone binary. The standalone binary's own flags ( , , , ...) live in . Run the free validators against your installed bundles. Strict mode for CI — "},{"id":92,"title":"#[derive(Params)] reference","section":"Reference","href":"/docs/reference/params","headings":"#[param(...)] attribute keys · Parameter types · Enum parameters · Meters · Range types · Smoothing · Read accessors · Flags · Modulation · MIDI binding · Custom formatting · Nested structs · Reusing a group · Pinning a base for a group · Addressing nested params in the editor · Parameter ID stability · Legacy ordinal scheme · Generated ParamId enum · Shared ownership · Skipped fields (#[skip])","excerpt":"Every attribute, range type, smoothing mode, and parameter type that the params derive accepts. For a narrative tour and recipes, see the parameters chapter in the guide. attribute keys Key Example No","text":"Every attribute, range type, smoothing mode, and parameter type that the params derive accepts. For a narrative tour and recipes, see the parameters chapter in the guide. attribute keys Key Example Notes ----- --------- ------- Optional. When omitted, auto-assigned as a stable hash of the field name, so reordering or inserting params (including in nested structs) never shifts IDs. Set it to pin a specific value; an explicit always wins. See Parameter ID stability. Required. Display name in host and GUI. Abbreviated name for narrow strips. Defaults to . Value mapping. Inferred for and . Default value in plain units. Defaults to range min. Display unit. Shapes the default formatter. Valid: , , , , , , . is display-only scaling: the plain value is a fraction and the formatter multiplies by 100, so a 0-150% param is . Every other unit formats the plain value as-is. Smoothing style + time in ms. See Smoothing. Parameter group surfaced by the host (CLAP module path / VST3 unit / AU group). Combination of: , , , . Method on the params struct that converts a value to a . Inverse of . Method that parses a host text-input back to . Opt this parameter out of sample-accurate sub-block chunking. Default . Set to for parameters too expensive to re-target mid-block (FFT sizes, lookahead lengths). See parameters § Sample-accurate automation. Default host MIDI-learn binding: a CC number . Bakes the initial CC→parameter map the host offers before the user learns their own. See MIDI binding. Mutually exclusive with . Default MIDI-learn binding to a non-CC source: , (channel pressure / aftertouch), or (program change). See MIDI binding. Mutually exclusive with . Scope a / binding to one MIDI channel . Omit to bind on every (omni) channel. The derive generates , a impl, the full trait impl, and a typed enum ( ) with one variant per parameter. Parameter types Field type Default widget Notes ------------ ---------------- ------- knob Continuous. Supports smoothing and custom formatting. toggle On / off. Range is implicit — don't pass . knob Integer steps within a range. dropdown Click-to-open list. is a enum; range is inferred from the variant count. meter Read-only, written from , drawn by the GUI. Declared with , not . Enum parameters Use on a variant to override its display text — the Rust name stays the same: Meters Meters are not parameters — they flow audio-thread → UI-thread instead of host → plugin. Declare them with : Parameters and meters share the generated enum. Write from , read in : The write is realtime-safe (atomic); the GUI reads the latest value every frame. Range types bends the knob response by a power law: a below 1 gives finer control near , above 1 near . does the same but anchored at and mirrored either side of it, so a pan or EQ-gain knob stays symmetric around its detent. wraps any of the other shapes and flips the knob axis (max at the bottom). ranges are implicit . ranges are inferred from 's variant count. Smoothing Host automation usually arrives block-rate. Smoothing interpolates between successive target values so there's no zipper noise on continuous parameters. smooths in the multiplicative (ratio) domain, so a cutoff sweep or a linear-gain fade moves at a constant perceived rate rather than a constant additive one. Both endpoints must be positive. The shell sets the sample rate and snaps the smoothers for you before runs, so you don't prime them yourself. In , pull a smoothed value per sample with : returns or depending on the prelude in scope — see Precision (preludes). The method takes (the smoother state is atomic), so it works through without . Read accessors exposes four read accessors: Method What it returns When to use --- --- --- Next smoothed sample; advances the smoother. Per-sample DSP loop. Fills the slice with the next smoothed samples; advances the smoother by . One atomic load + one atomic store per call, regardless of length. Block-rate DSP — pair with vectorized math ( , ) to amortize the atomic ov"},{"id":93,"title":"truce.toml reference","section":"Reference","href":"/docs/reference/truce-toml","headings":"[vendor] — required · [[plugin]] — one per plugin, at least one required · [plugin.presets] — optional, per plugin · [plugin.legacystate] — optional, per plugin · [[suite]] — optional, repeatable · [macos.packaging] — optional, macOS only · [windows.packaging] — optional, Windows only · [packaging] — cross-platform · [automation] — optional · Full example","excerpt":"Every field reads from your project's , grouped by table. The file lives at the project root alongside and is tracked in git — it carries project-level facts only. Per-developer credentials and machin","text":"Every field reads from your project's , grouped by table. The file lives at the project root alongside and is tracked in git — it carries project-level facts only. Per-developer credentials and machine-specific paths (signing identities, AAX SDK location, notarization Apple ID / team ID, Authenticode certs) live in 's table or shell env vars instead. See for that surface. — required Field Type Required Notes ------- ------ ---------- ------- string yes Human company name. Shows in DAWs, installers, Apps & Features. string yes Reverse-DNS prefix ( ). Used for AU/VST3/CLAP IDs and Windows installer AppId. string no Vendor website. Surfaced in the Windows installer \"Publisher URL\" field. string yes Exactly 4 ASCII characters. AU manufacturer code — must be unique per vendor. — one per plugin, at least one required Field Type Required Notes ------- ------ ---------- ------- string yes Human name. Used for bundle filenames and DAW display names. string yes Short lowercase identifier ( plus / / separators, alphanumeric at both ends, no consecutive separators; validated at compile time). The stable identity key: / / the state-envelope hash derive from it, and it names bundles, extension reverse-DNS IDs ( ), install plists, and scratch paths. Not used at the CLI. string yes Cargo package name. CLI uses this for . Hyphens become underscores in built / . string yes / / . Drives AU/VST3/CLAP category metadata. bool no Whether the host routes MIDI into the plugin. Defaults to for instruments and note effects ( ); set on an effect that reacts to MIDI (e.g. CC-controlled), or to suppress. On AU, on an effect promotes it to the MusicEffect type so the host actually routes MIDI to it; on an instrument is a no-op (an receives MIDI by type). bool no Whether the plugin emits MIDI to the host. Defaults to for note effects ( ) only; set on an instrument or effect that also emits MIDI so the host declares the MIDI output port/bus. bool no Opt the plugin's MIDI ports into MIDI 2.0 / UMP. Defaults to (MIDI 1.0). CLAP advertises the MIDI2 note dialect and carries the native 16/32-bit + per-note + UMP-group variants in and out; AU v3 declares = 2.0 and emits over the host's block (macOS 12+ / iOS 15+); VST3 maps the per-note subset through note expression; VST2 / AU v2 / AAX / LV2 stay MIDI 1.0 regardless. Requires at least one MIDI port in either direction (compile error otherwise) and applies only to directions that have one. bool no Per-direction override of for MIDI input. Absent follows . requires a MIDI input port (compile error otherwise). bool no Per-direction override of for MIDI output. Same rules as , for the output direction. int no Number of MIDI input ports. Absent → one port when the plugin accepts MIDI input, none otherwise. A count is authoritative: any non-zero value enables MIDI input on every format (no needed), disables it, and a value that contradicts is a compile error. Set for a multi-port plugin; the plugin then filters inbound events by . CLAP (N note ports), VST3 (N event buses), and LV2 (N atom ports) carry more than one; VST2 / AU / AAX clamp to one and log a skip. int no Number of MIDI output ports. See ; the plugin stamps each outbound event with the port it should leave on via . string yes† Exactly 4 ASCII chars. AU subtype + cross-format unique ID. string no Override AU type. Defaults: for instruments, for midi / note-effects, for an effect with , else . string no Synonym for . wins if both are set. string no 4-char subtype for AU v3 only. Set if v2/v3 must differ. string no AU category tag. Defaults to . Common: , , , . string no AAX category string (e.g. , , ). Maps to in the AAX SDK. string no VST3 \"Plugin Type Categories\" secondary token. Without this, hosts like Cubase bucket the plugin under \"Other\". Values from the VST3 SDK list: , , , , , , , , , , , , . Wrapper emits for effects, for instruments. string no Per-format display-name override: , , , , , , . string no Path to (workspace-root-relative). Embedded as i"},{"id":94,"title":"Roadmap & Known Gaps","section":"Overview","href":"/docs/roadmap","headings":"Known gaps · Future","excerpt":"Known gaps Resize. Host-driven (drag-to-resize) round-trips through CLAP, VST3, AU v3, and LV2. VST2. Resizable editors aren't wired, so a VST2 editor opens (and stays) at its natural size. AU v2. Res","text":"Known gaps Resize. Host-driven (drag-to-resize) round-trips through CLAP, VST3, AU v3, and LV2. VST2. Resizable editors aren't wired, so a VST2 editor opens (and stays) at its natural size. AU v2. Resizable editors round-trip under AU v3 but not the AU v2 component, so an AU v2 editor opens at its natural size. Use AU v3 for resizable GUIs on macOS. AAX (Pro Tools). A plugin can resize its own editor (the GUI's forwards to ), but the reverse direction isn't wired: AAX's exposes no inbound size-change callback like VST3's or CLAP's , so dragging the Pro Tools window edge doesn't resize the editor. . Editor resize is a no-op: records the new logical size but vizia's exposes no resize entry point callable from outside its event loop, so the view doesn't repaint at it. Ship vizia plugins fixed-size. Pending a upstream patch. Factory presets. Ship to CLAP, VST3, AU (macOS + iOS), and LV2. VST2. No emission yet. AAX (Pro Tools). Not emitted as AAX yet, so they don't appear in Pro Tools' preset menu. Sidechain / aux inputs. A layout gives the plugin a separate sidechain bus on CLAP, VST3, AU (v2 + v3), AAX, VST2, and the standalone host. LV2. Sidechain plugins run main-only. The LV2 port list is generated at compile time from the plugin category, so it can't yet read the sidechain ports from ; declaring them only at runtime would shift every port index past the main bus. The wrapper drops the sidechain to keep the runtime port map aligned with the (a one-time log notes this), so the plugin loads and processes its main bus normally - it just receives no sidechain signal. Full support requires install-time rendering from the real layout. Signing & distribution. Retail iLok / PACE round-trip. PACE wraptool is wired and exercised against a dev iLok account; we haven't yet round-tripped through a retail iLok + retail Pro Tools install. iOS GUI backends. . vizia hard-pins at a rev that doesn't build for (baseview has no UIKit platform impl), so the crate is gated . Use the built-in editor, , , or on iOS. Bring-your-own container. always emits the bundled Swift container template (title, Play, status, hamburger overlay). Plug-ins that need a bespoke shell hand-author it outside the pipeline and load the truce builds. MIDI 2.0. Full MIDI 2.0 (UMP channel-voice, high-resolution and per-note controllers) only round-trips on CLAP and AU v3, and only when the host negotiates the 2.0 protocol. A plugin that declares a MIDI 2.0 port still receives MIDI 1.0 from a host that offers only 1.0 - most DAWs today - with the host down-converting on the way in. VST3. No UMP transport. Per-note messages round-trip through VST3 note expression, but a MIDI 2.0 channel-voice message with no note-expression equivalent (an unmapped per-note CC, say) down-converts to a MIDI 1.0 channel message on output. VST2, AU v2, AAX, LV2. MIDI 1.0 only - 3-byte packets on VST2 / AU v2 / AAX, MIDI 1.0 byte streams in the LV2 Atom. Input is always MIDI 1.0, and 2.0 channel-voice output down-converts to 1.0. Any message with no 1.0 form (per-note controllers, per-note pitch bend) is dropped. MPE / per-note expression. Per-note expression - MPE (per-note pitch bend + CC spread across MIDI channels) and MIDI 2.0 per-note controllers - round-trips on CLAP, VST3, and AU hosts that route multi-channel MIDI to an instrument. Pro Tools (AAX). Pro Tools treats each MIDI channel as a separate track, so multi-channel MIDI does not survive on a single instrument track: notes reaching an instrument collapse onto one channel (a multi-channel file even imports as one track per channel). A note effect that fans notes across channels into an instrument on the same track (an MPE-style voice allocator) loses the spread in Pro Tools, though the same chain works in CLAP, VST3, and AU hosts. This is a Pro Tools MIDI-routing limitation, not a wrapper gap - truce emits and preserves the per-note channels correctly on the AAX wire. AAX-specific note expression has not been wired, so there is no per-no"}]